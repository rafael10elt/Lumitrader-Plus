This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
AGENTS.md
CLAUDE.md
docs/backend-reporting.md
eslint.config.mjs
LumitraderBridge.zip
LumitraderBridge/install_startup.bat
LumitraderBridge/logs/.gitkeep
LumitraderBridge/mt5_reporter.py
LumitraderBridge/README.md
LumitraderBridge/requirements.txt
LumitraderBridge/run_mt5_reporter.bat
LumitraderBridge/start_mt5_reporter_hidden.vbs
n8n/lumitrader-telegram-workflow.json
next.config.ts
package.json
postcss.config.mjs
proxy.ts
public/file.svg
public/globe.svg
public/next.svg
public/vercel.svg
public/window.svg
python/install_mt5_reporter_startup.bat
python/install_mt5_reporter_task.ps1
python/mt5_reporter.py
python/README.md
python/requirements.txt
python/run_mt5_reporter.bat
python/run_mt5_reporter.ps1
python/send_test_event.py
python/start_mt5_reporter_hidden.vbs
README.md
src/app/admin/actions.ts
src/app/admin/page.tsx
src/app/api/backend/bridge/accounts/route.ts
src/app/api/backend/bridge/commands/route.ts
src/app/api/backend/trading/events/route.ts
src/app/auth/confirm/route.ts
src/app/dashboard/actions.ts
src/app/dashboard/page.tsx
src/app/error/page.tsx
src/app/favicon.ico
src/app/globals.css
src/app/icon.svg
src/app/layout.tsx
src/app/login/actions.ts
src/app/login/page.tsx
src/app/manifest.ts
src/app/page.tsx
src/components/admin/admin-users-panel.tsx
src/components/auth/signout-button.tsx
src/components/dashboard/dashboard-realtime-fixed.tsx
src/components/dashboard/dashboard-realtime.tsx
src/components/dashboard/tradingview-chart.tsx
src/lib/auth.ts
src/lib/backend/auto-trader.ts
src/lib/backend/formatters.ts
src/lib/backend/n8n.ts
src/lib/backend/openai.ts
src/lib/backend/reporting.ts
src/lib/backend/risk.ts
src/lib/backend/supabase.ts
src/lib/backend/types.ts
src/lib/env.ts
src/lib/supabase/admin.ts
src/lib/supabase/client.ts
src/lib/supabase/proxy.ts
src/lib/supabase/server.ts
supabase/migrations/20260325_consolidate_mt5_server.sql
supabase/migrations/20260325_runtime_and_mt5_credentials.sql
supabase/migrations/20260325_trading_commands.sql
supabase/schema.sql
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/lumitrader-app/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
</file>

<file path="AGENTS.md">
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->
</file>

<file path="CLAUDE.md">
@AGENTS.md
</file>

<file path="docs/backend-reporting.md">
# Lumitrader Backend Reporting

Fluxo implementado:

1. MT5/Python envia evento para `POST /api/backend/trading/events`.
2. O backend valida o token de ingestao.
3. Carrega usuario, conta MT5, licenca e configuracoes da conta no Supabase.
4. Atualiza snapshot da conta e registra abertura ou fechamento da operacao.
5. Recalcula estatisticas do dia para a conta.
6. Calcula risco e chama a OpenAI diretamente no backend.
7. Monta relatorio em JSON, CSV e HTML.
8. Envia o relatorio para o webhook do n8n.
9. O n8n fica responsavel apenas por distribuir no Telegram.

Contrato principal do endpoint:

- Header: `Authorization: Bearer LUMITRADER_INGEST_TOKEN`
- URL local: `http://localhost:3000/api/backend/trading/events`
- Eventos suportados: `operation_opened`, `operation_closed`

Campos importantes do payload:

- `account.number`
- `account.currency_code`
- `account.currency_symbol`
- `account.balance`
- `account.equity`
- `operation.symbol`
- `operation.side`
- `operation.lot`
- `operation.entry_price`
- `operation.profit_loss`
- `operation.opened_at`
- `operation.closed_at`

Observacao: a atualizacao de fechamento parte da regra operacional de apenas uma operacao aberta por conta ao mesmo tempo.
</file>

<file path="eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    "lumitrader-app/**",
  ]),
]);

export default eslintConfig;
</file>

<file path="LumitraderBridge/install_startup.bat">
@echo off
setlocal

set "ROOT=%~dp0"
set "STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"
set "VBS_SOURCE=%ROOT%start_mt5_reporter_hidden.vbs"
set "VBS_TARGET=%STARTUP%\Lumitrader MT5 Reporter.vbs"

if not exist "%VBS_SOURCE%" (
  echo Arquivo nao encontrado: "%VBS_SOURCE%"
  pause
  exit /b 1
)

copy /Y "%VBS_SOURCE%" "%VBS_TARGET%" >nul
if errorlevel 1 (
  echo Falha ao copiar para a pasta Startup.
  pause
  exit /b 1
)

echo Inicializacao automatica instalada em:
echo %VBS_TARGET%
pause
exit /b 0
</file>

<file path="LumitraderBridge/logs/.gitkeep">

</file>

<file path="LumitraderBridge/mt5_reporter.py">
import json
import os
import time
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, List

import MetaTrader5 as mt5
import requests
from dotenv import dotenv_values

ENV_PATH = Path(__file__).with_name(".env")


def load_env_file() -> None:
    values = dotenv_values(ENV_PATH, encoding="utf-8-sig")
    for key, value in values.items():
        if key and value is not None:
            os.environ[key] = value


load_env_file()

EVENTS_URL = os.getenv("LUMITRADER_BACKEND_URL", "http://localhost:3000/api/backend/trading/events")
BRIDGE_ACCOUNTS_URL = EVENTS_URL.replace("/trading/events", "/bridge/accounts")
BRIDGE_COMMANDS_URL = EVENTS_URL.replace("/trading/events", "/bridge/commands")
INGEST_TOKEN = os.getenv("LUMITRADER_INGEST_TOKEN", "")
POLL_INTERVAL_SECONDS = int(os.getenv("POLL_INTERVAL_SECONDS", "3"))
STATE_PATH = Path(__file__).with_name("state.json")

TIMEFRAME_MAP = {
    "M1": mt5.TIMEFRAME_M1,
    "M5": mt5.TIMEFRAME_M5,
    "M15": mt5.TIMEFRAME_M15,
    "M30": mt5.TIMEFRAME_M30,
    "H1": mt5.TIMEFRAME_H1,
}


@dataclass
class PositionSnapshot:
    ticket: int
    symbol: str
    side: str
    lot: float
    entry_price: float
    stop_loss: float
    take_profit: float
    profit_loss: float
    opened_at: str


def ensure_symbol(symbol: str) -> None:
    if not mt5.symbol_select(symbol, True):
        raise RuntimeError(f"Nao foi possivel selecionar o simbolo {symbol}: {mt5.last_error()}")


def resolve_filling_mode(symbol: str) -> int:
    info = mt5.symbol_info(symbol)
    if info is None:
        return mt5.ORDER_FILLING_IOC

    filling_mode = getattr(info, "filling_mode", None)
    allowed = {
        mt5.ORDER_FILLING_FOK,
        mt5.ORDER_FILLING_IOC,
        getattr(mt5, "ORDER_FILLING_RETURN", mt5.ORDER_FILLING_IOC),
    }
    if filling_mode in allowed:
        return filling_mode
    return mt5.ORDER_FILLING_IOC


def utc_iso(timestamp: int) -> str:
    return datetime.fromtimestamp(timestamp, tz=timezone.utc).isoformat().replace("+00:00", "Z")


def load_state() -> Dict[str, Dict[str, Any]]:
    if not STATE_PATH.exists():
        return {"accounts": {}}
    return json.loads(STATE_PATH.read_text(encoding="utf-8"))


def save_state(state: Dict[str, Dict[str, Any]]) -> None:
    STATE_PATH.write_text(json.dumps(state, indent=2), encoding="utf-8")


def fetch_active_accounts() -> List[Dict[str, Any]]:
    response = requests.get(
        BRIDGE_ACCOUNTS_URL,
        headers={"Authorization": f"Bearer {INGEST_TOKEN}"},
        timeout=30,
    )
    response.raise_for_status()
    data = response.json()
    return data.get("accounts", [])


def fetch_pending_commands(account_number: str) -> List[Dict[str, Any]]:
    response = requests.get(
        BRIDGE_COMMANDS_URL,
        headers={"Authorization": f"Bearer {INGEST_TOKEN}"},
        params={"account": account_number},
        timeout=30,
    )
    response.raise_for_status()
    data = response.json()
    return data.get("commands", [])


def acknowledge_command(command_id: str, status: str, result: Dict[str, Any] | None = None, error: str | None = None) -> None:
    response = requests.post(
        BRIDGE_COMMANDS_URL,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {INGEST_TOKEN}",
        },
        json={
            "commandId": command_id,
            "status": status,
            "result": result or {},
            "error": error,
        },
        timeout=30,
    )
    response.raise_for_status()


def execute_command(command: Dict[str, Any]) -> None:
    symbol = command["symbol"]
    command_type = command["type"]
    ensure_symbol(symbol)
    filling_mode = resolve_filling_mode(symbol)

    if command_type in {"open_buy", "open_sell"}:
        tick = mt5.symbol_info_tick(symbol)
        if not tick:
            raise RuntimeError(f"Tick indisponivel para {symbol}")

        order_type = mt5.ORDER_TYPE_BUY if command_type == "open_buy" else mt5.ORDER_TYPE_SELL
        price = tick.ask if command_type == "open_buy" else tick.bid
        request = {
            "action": mt5.TRADE_ACTION_DEAL,
            "symbol": symbol,
            "volume": float(command.get("lot") or 0),
            "type": order_type,
            "price": price,
            "sl": float(command.get("stopLoss") or 0),
            "tp": float(command.get("takeProfit") or 0),
            "deviation": 20,
            "comment": "Lumitrader command",
            "type_time": mt5.ORDER_TIME_GTC,
            "type_filling": filling_mode,
        }
        result = mt5.order_send(request)
        if result is None or result.retcode != mt5.TRADE_RETCODE_DONE:
            raise RuntimeError(f"order_send falhou: {getattr(result, 'retcode', mt5.last_error())}")

        acknowledge_command(command["id"], "executed", {"retcode": result.retcode, "order": result.order, "deal": result.deal})
        return

    if command_type == "close_position":
        positions = mt5.positions_get(symbol=symbol) or []
        reference_ticket = str(command.get("referenceTicket") or "").strip()
        if reference_ticket:
            positions = [position for position in positions if str(position.ticket) == reference_ticket]
        if not positions:
            acknowledge_command(command["id"], "executed", {"message": "Nenhuma posicao aberta para fechar."})
            return

        position = positions[0]
        tick = mt5.symbol_info_tick(symbol)
        if not tick:
            raise RuntimeError(f"Tick indisponivel para {symbol}")

        close_type = mt5.ORDER_TYPE_SELL if position.type == mt5.POSITION_TYPE_BUY else mt5.ORDER_TYPE_BUY
        price = tick.bid if position.type == mt5.POSITION_TYPE_BUY else tick.ask
        request = {
            "action": mt5.TRADE_ACTION_DEAL,
            "symbol": symbol,
            "volume": float(position.volume),
            "type": close_type,
            "position": position.ticket,
            "price": price,
            "deviation": 20,
            "comment": "Lumitrader close",
            "type_time": mt5.ORDER_TIME_GTC,
            "type_filling": filling_mode,
        }
        result = mt5.order_send(request)
        if result is None or result.retcode != mt5.TRADE_RETCODE_DONE:
            raise RuntimeError(f"close order_send falhou: {getattr(result, 'retcode', mt5.last_error())}")

        acknowledge_command(command["id"], "executed", {"retcode": result.retcode, "order": result.order, "deal": result.deal, "closed_position": position.ticket})
        return

    raise RuntimeError(f"Tipo de comando nao suportado: {command_type}")


def process_commands(account_number: str) -> None:
    commands = fetch_pending_commands(account_number)
    for command in commands:
        try:
            execute_command(command)
        except Exception as exc:
            acknowledge_command(command["id"], "failed", error=str(exc))


def ensure_terminal() -> None:
    if not mt5.initialize():
        raise RuntimeError(f"MT5 initialize failed: {mt5.last_error()}")


def login_account(account: Dict[str, Any]) -> None:
    ensure_terminal()
    login = int(account["number"])
    password = account["password"]
    server = account["server"]
    if not mt5.login(login=login, password=password, server=server):
        raise RuntimeError(f"MT5 login failed for {login}: {mt5.last_error()}")


def account_payload(symbol: str | None = None) -> Dict[str, Any]:
    account = mt5.account_info()
    if account is None:
        raise RuntimeError("MT5 account_info returned None")

    currency_code = account.currency or "USD"
    currency_symbol = "$" if currency_code == "USD" else "R$" if currency_code == "BRL" else currency_code
    server_time = None
    if symbol:
        tick = mt5.symbol_info_tick(symbol)
        if tick:
            server_time = utc_iso(tick.time)

    return {
        "number": str(account.login),
        "broker": getattr(account, "company", None),
        "server": account.server,
        "server_time": server_time,
        "name": account.name,
        "currency_code": currency_code,
        "currency_symbol": currency_symbol,
        "balance": float(account.balance),
        "equity": float(account.equity),
        "margin": float(account.margin),
        "free_margin": float(account.margin_free),
        "margin_level": float(account.margin_level),
        "leverage": int(account.leverage),
    }


def build_position_snapshot(position) -> PositionSnapshot:
    return PositionSnapshot(
        ticket=int(position.ticket),
        symbol=position.symbol,
        side="buy" if position.type == mt5.POSITION_TYPE_BUY else "sell",
        lot=float(position.volume),
        entry_price=float(position.price_open),
        stop_loss=float(position.sl or 0),
        take_profit=float(position.tp or 0),
        profit_loss=float(position.profit),
        opened_at=utc_iso(position.time),
    )


def build_market_payload(symbol: str, timeframe_name: str) -> Dict[str, Any]:
    timeframe = TIMEFRAME_MAP.get(timeframe_name, mt5.TIMEFRAME_M5)
    rates = mt5.copy_rates_from_pos(symbol, timeframe, 0, 40) or []
    candles = [
        {
            "time": utc_iso(int(rate["time"])),
            "open": float(rate["open"]),
            "high": float(rate["high"]),
            "low": float(rate["low"]),
            "close": float(rate["close"]),
        }
        for rate in rates
    ]

    tick = mt5.symbol_info_tick(symbol)
    symbol_info = mt5.symbol_info(symbol)
    notes = [f"Sync do mercado para {symbol} em {timeframe_name}."]

    return {
        "trend": None,
        "rsi": None,
        "moving_average_20": None,
        "support": None,
        "resistance": None,
        "notes": notes,
        "candles": candles,
        "spread": float(symbol_info.spread) if symbol_info else None,
        "last_bid": float(tick.bid) if tick else None,
        "last_ask": float(tick.ask) if tick else None,
    }


def send_event(payload: Dict[str, Any]) -> None:
    response = requests.post(
        EVENTS_URL,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {INGEST_TOKEN}",
        },
        json=payload,
        timeout=30,
    )
    response.raise_for_status()
    operation = payload.get("operation") or {}
    symbol = operation.get("symbol", "sync")
    print(f"Event sent: {payload['event']} -> {symbol} -> {response.status_code}")


def sync_account(account_config: Dict[str, Any]) -> Dict[str, Any]:
    config = account_config.get("config") or {}
    symbol = config.get("ativo") or "XAUUSD"
    timeframe = config.get("timeframe") or "M5"
    market = build_market_payload(symbol, timeframe)
    payload = {
        "event": "account_sync",
        "account": account_payload(symbol),
        "session": {
            "mode": config.get("modo"),
            "breakeven_enabled": config.get("breakeven_ativo"),
            "trailing_stop_enabled": config.get("trailing_stop_ativo"),
            "profit_target": config.get("meta_lucro_diaria"),
            "daily_loss_limit": config.get("perda_maxima_diaria"),
            "operation_limit_enabled": config.get("limite_operacoes_ativo"),
            "operation_limit": config.get("limite_operacoes_diaria"),
        },
        "market": market,
    }
    send_event(payload)
    return payload


def handle_open_positions(account_config: Dict[str, Any], account_state: Dict[str, Any]) -> None:
    config = account_config.get("config") or {}
    symbol_filter = config.get("ativo")
    current_positions = mt5.positions_get(symbol=symbol_filter) if symbol_filter else mt5.positions_get()
    current_positions = current_positions or []
    current_map = {str(position.ticket): build_position_snapshot(position) for position in current_positions}
    account = account_payload(symbol_filter)
    timeframe = config.get("timeframe") or "M5"
    market = build_market_payload(symbol_filter or current_positions[0].symbol if current_positions else "XAUUSD", timeframe)

    for ticket, snapshot in current_map.items():
        if ticket not in account_state["open_positions"]:
            payload = {
                "event": "operation_opened",
                "account": account,
                "session": {
                    "mode": config.get("modo"),
                    "breakeven_enabled": config.get("breakeven_ativo"),
                    "trailing_stop_enabled": config.get("trailing_stop_ativo"),
                    "profit_target": config.get("meta_lucro_diaria"),
                    "daily_loss_limit": config.get("perda_maxima_diaria"),
                    "operation_limit_enabled": config.get("limite_operacoes_ativo"),
                    "operation_limit": config.get("limite_operacoes_diaria"),
                },
                "market": market,
                "operation": {
                    "ticket": ticket,
                    "symbol": snapshot.symbol,
                    "timeframe": timeframe,
                    "side": snapshot.side,
                    "lot": snapshot.lot,
                    "entry_price": snapshot.entry_price,
                    "stop_loss": snapshot.stop_loss,
                    "take_profit": snapshot.take_profit,
                    "profit_loss": snapshot.profit_loss,
                    "opened_at": snapshot.opened_at,
                },
            }
            send_event(payload)

    account_state["open_positions"] = {ticket: snapshot.__dict__ for ticket, snapshot in current_map.items()}


def handle_closed_positions(account_config: Dict[str, Any], account_state: Dict[str, Any]) -> None:
    config = account_config.get("config") or {}
    symbol = config.get("ativo") or "XAUUSD"
    timeframe = config.get("timeframe") or "M5"
    start_of_day = datetime.now(timezone.utc).replace(hour=0, minute=0, second=0, microsecond=0)
    history = mt5.history_deals_get(start_of_day, datetime.now(timezone.utc)) or []
    account = account_payload(symbol)
    market = build_market_payload(symbol, timeframe)
    recent_history_ids = set(account_state.get("recent_history_ids", []))
    open_positions = account_state.get("open_positions", {})

    for deal in history:
        if deal.entry != mt5.DEAL_ENTRY_OUT:
            continue
        history_id = str(deal.ticket)
        if history_id in recent_history_ids:
            continue
        position_id = str(deal.position_id)
        cached_position = open_positions.get(position_id)
        payload = {
            "event": "operation_closed",
            "account": account,
            "session": {
                "mode": config.get("modo"),
                "breakeven_enabled": config.get("breakeven_ativo"),
                "trailing_stop_enabled": config.get("trailing_stop_ativo"),
                "profit_target": config.get("meta_lucro_diaria"),
                "daily_loss_limit": config.get("perda_maxima_diaria"),
                "operation_limit_enabled": config.get("limite_operacoes_ativo"),
                "operation_limit": config.get("limite_operacoes_diaria"),
            },
            "market": market,
            "operation": {
                "ticket": position_id,
                "symbol": deal.symbol,
                "timeframe": timeframe,
                "side": cached_position["side"] if cached_position else "buy",
                "lot": float(deal.volume),
                "entry_price": float(cached_position["entry_price"]) if cached_position else float(deal.price),
                "exit_price": float(deal.price),
                "stop_loss": float(cached_position["stop_loss"]) if cached_position else 0,
                "take_profit": float(cached_position["take_profit"]) if cached_position else 0,
                "profit_loss": float(deal.profit),
                "opened_at": cached_position["opened_at"] if cached_position else utc_iso(deal.time),
                "closed_at": utc_iso(deal.time),
                "close_reason": str(getattr(deal, "reason", "mt5_close")),
            },
        }
        send_event(payload)
        recent_history_ids.add(history_id)
        if position_id in open_positions:
            del open_positions[position_id]

    account_state["open_positions"] = open_positions
    account_state["recent_history_ids"] = list(recent_history_ids)[-200:]


def main() -> None:
    if not INGEST_TOKEN:
        raise RuntimeError("Missing LUMITRADER_INGEST_TOKEN")

    state = load_state()
    state.setdefault("accounts", {})
    print("Lumitrader MT5 reporter started")

    while True:
        try:
            accounts = fetch_active_accounts()
            active_numbers = set()
            for account in accounts:
                number = str(account["number"])
                active_numbers.add(number)
                state["accounts"].setdefault(number, {"open_positions": {}, "recent_history_ids": []})
                account_state = state["accounts"][number]

                try:
                    login_account(account)
                    process_commands(number)
                    sync_account(account)
                    handle_open_positions(account, account_state)
                    handle_closed_positions(account, account_state)
                except Exception as account_exc:
                    print(f"Account {number} error: {account_exc}")

            stale = [number for number in state["accounts"].keys() if number not in active_numbers]
            for number in stale:
                del state["accounts"][number]

            save_state(state)
        except Exception as exc:
            print(f"Loop error: {exc}")

        time.sleep(POLL_INTERVAL_SECONDS)


if __name__ == "__main__":
    main()
</file>

<file path="LumitraderBridge/README.md">
# LumitraderBridge

Pacote standalone para rodar o bridge MT5 em uma VPS Windows sem depender de PowerShell.

## Arquivos

- `mt5_reporter.py`: bridge principal.
- `requirements.txt`: dependencias Python.
- `.env.example`: modelo de configuracao.
- `run_mt5_reporter.bat`: executa o bridge e grava logs.
- `start_mt5_reporter_hidden.vbs`: inicia o bridge sem abrir janela.
- `install_startup.bat`: instala o autostart na pasta Startup do Windows.
- `logs/`: pasta para os arquivos de log.

## Instalacao

1. Instale o Python para Windows e marque `Add Python to PATH`.
2. Copie esta pasta inteira para a VPS.
3. No `cmd`, entre na pasta e rode:

```bat
python -m pip install -r requirements.txt
```

4. Copie `.env.example` para `.env` e preencha:

```env
LUMITRADER_BACKEND_URL=https://lumitrader.lumitechia.com.br/api/backend/trading/events
LUMITRADER_INGEST_TOKEN=seu_token
POLL_INTERVAL_SECONDS=3
```

## Uso

Teste manual:

```bat
run_mt5_reporter.bat
```

Auto start apos login do Windows:

```bat
install_startup.bat
```

## Logs

- `logs\mt5_reporter.out.log`
- `logs\mt5_reporter.err.log`

## Observacoes

- O MT5 precisa estar aberto e logado na sessao grafica do Windows.
- A opcao Startup inicia melhor nesse tipo de VPS do que um servico puro de sistema.
</file>

<file path="LumitraderBridge/requirements.txt">
metatrader5>=5.0.5640
requests==2.32.3
python-dotenv==1.0.1
</file>

<file path="LumitraderBridge/run_mt5_reporter.bat">
@echo off
setlocal

set "ROOT=%~dp0"
set "PYTHON_EXE=%ROOT%.venv\Scripts\python.exe"
set "SCRIPT_PATH=%ROOT%mt5_reporter.py"
set "LOG_DIR=%ROOT%logs"
set "STDOUT_LOG=%LOG_DIR%\mt5_reporter.out.log"
set "STDERR_LOG=%LOG_DIR%\mt5_reporter.err.log"

if not exist "%LOG_DIR%" mkdir "%LOG_DIR%"

if not exist "%PYTHON_EXE%" (
  echo Python nao encontrado em "%PYTHON_EXE%"
  pause
  exit /b 1
)

if not exist "%SCRIPT_PATH%" (
  echo Script nao encontrado em "%SCRIPT_PATH%"
  pause
  exit /b 1
)

cd /d "%ROOT%"
"%PYTHON_EXE%" "%SCRIPT_PATH%" 1>> "%STDOUT_LOG%" 2>> "%STDERR_LOG%"
exit /b %ERRORLEVEL%
</file>

<file path="LumitraderBridge/start_mt5_reporter_hidden.vbs">
Set shell = CreateObject("WScript.Shell")
scriptPath = CreateObject("Scripting.FileSystemObject").GetParentFolderName(WScript.ScriptFullName) & "\run_mt5_reporter.bat"
shell.Run Chr(34) & scriptPath & Chr(34), 0, False
</file>

<file path="n8n/lumitrader-telegram-workflow.json">
{
  "name": "Lumitrader Telegram Reports",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "lumitrader-report",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-lumitrader-report",
      "name": "Lumitrader Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        260,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "const report = $json;\nconst isClosed = report.event === 'operation_closed';\nconst emoji = isClosed ? '📕' : '📘';\nconst direction = report.operation.side === 'buy' ? 'Compra' : 'Venda';\nconst pnl = Number(report.operation.profit_loss ?? 0);\nconst pnlSignal = pnl >= 0 ? '+' : '-';\nconst pnlAbs = Math.abs(pnl).toFixed(2);\nconst currency = report.account.currency_symbol ?? '$';\nconst telegramId = report.user.telegram_id;\nconst lines = [\n  `${emoji} Lumitrader | ${isClosed ? 'Operacao encerrada' : 'Operacao aberta'}`,\n  `Cliente: ${report.user.nome ?? report.user.email ?? 'Cliente'}`,\n  `Conta MT5: ${report.account.number}`,\n  `Plano: ${report.account.active_license.plan} (${report.account.active_license.status})`,\n  `Ativo: ${report.operation.symbol} ${report.operation.timeframe}`,\n  `Direcao: ${direction}`,\n  `Lote: ${report.operation.lot}`,\n  `Entrada: ${report.operation.entry_price}`,\n  report.operation.exit_price ? `Saida: ${report.operation.exit_price}` : null,\n  `Saldo: ${currency} ${Number(report.account.balance ?? 0).toFixed(2)}`,\n  `Equity: ${currency} ${Number(report.account.equity ?? 0).toFixed(2)}`,\n  `Resultado: ${pnlSignal}${currency} ${pnlAbs}`,\n  `Risco: ${currency} ${Number(report.risk.riskAmount ?? 0).toFixed(2)}`,\n  report.risk.rewardAmount != null ? `Alvo estimado: ${currency} ${Number(report.risk.rewardAmount).toFixed(2)}` : null,\n  report.ai?.summary ? `IA: ${report.ai.summary}` : null\n].filter(Boolean);\n\nreturn [{\n  json: {\n    ok: true,\n    telegram_id: telegramId,\n    text: lines.join('\\n')\n  }\n}];"
      },
      "id": "format-telegram-message",
      "name": "Formatar mensagem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        520,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "leftValue": "={{ $json.telegram_id }}",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "notEmpty",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "if-telegram-id",
      "name": "Tem Telegram ID?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        760,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{$env.TELEGRAM_BOT_TOKEN}}/sendMessage",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"chat_id\": $json.telegram_id,\n  \"text\": $json.text\n}",
        "options": {}
      },
      "id": "send-telegram-message",
      "name": "Enviar Telegram",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1020,
        220
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={\n  \"ok\": true,\n  \"delivered\": true,\n  \"telegram_id\": $json.telegram_id\n}",
        "options": {}
      },
      "id": "respond-success",
      "name": "Responder sucesso",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.3,
      "position": [
        1260,
        220
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={\n  \"ok\": true,\n  \"delivered\": false,\n  \"reason\": \"telegram_id_nao_configurado\"\n}",
        "options": {}
      },
      "id": "respond-no-telegram",
      "name": "Responder sem Telegram",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.3,
      "position": [
        1020,
        420
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "Lumitrader Webhook": {
      "main": [
        [
          {
            "node": "Formatar mensagem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Formatar mensagem": {
      "main": [
        [
          {
            "node": "Tem Telegram ID?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Tem Telegram ID?": {
      "main": [
        [
          {
            "node": "Enviar Telegram",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Responder sem Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enviar Telegram": {
      "main": [
        [
          {
            "node": "Responder sucesso",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "7d1a35f0-0f32-47b4-8d8b-784f9f2c0a0f",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "lumitrader"
  },
  "id": "lumitrader-telegram-reports",
  "tags": []
}
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
</file>

<file path="postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="proxy.ts">
import { type NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/proxy";

export async function proxy(request: NextRequest) {
  return updateSession(request);
}

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
};
</file>

<file path="public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="python/install_mt5_reporter_startup.bat">
@echo off
setlocal

set "ROOT=%~dp0"
set "STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup"
set "VBS_SOURCE=%ROOT%start_mt5_reporter_hidden.vbs"
set "VBS_TARGET=%STARTUP%\Lumitrader MT5 Reporter.vbs"

if not exist "%VBS_SOURCE%" (
  echo Arquivo nao encontrado: "%VBS_SOURCE%"
  exit /b 1
)

copy /Y "%VBS_SOURCE%" "%VBS_TARGET%" >nul
if errorlevel 1 (
  echo Falha ao copiar para a pasta Startup.
  exit /b 1
)

echo Inicializacao automatica instalada em:
echo %VBS_TARGET%
echo.
echo Reinicie a VPS ou faca logoff/logon para testar.
exit /b 0
</file>

<file path="python/install_mt5_reporter_task.ps1">
param(
  [string]$TaskName = "Lumitrader MT5 Reporter",
  [string]$ReporterScript = "$PSScriptRoot\run_mt5_reporter.ps1"
)

$ErrorActionPreference = "Stop"

if (-not (Test-Path $ReporterScript)) {
  throw "Script nao encontrado em $ReporterScript"
}

$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoProfile -ExecutionPolicy Bypass -File `"$ReporterScript`""
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -MultipleInstances IgnoreNew -RestartCount 999 -RestartInterval (New-TimeSpan -Minutes 1) -StartWhenAvailable
$principal = New-ScheduledTaskPrincipal -UserId $env:USERNAME -LogonType Interactive -RunLevel Highest

Register-ScheduledTask -TaskName $TaskName -Action $action -Trigger $trigger -Settings $settings -Principal $principal -Force | Out-Null
Start-ScheduledTask -TaskName $TaskName

Write-Host "Tarefa registrada e iniciada: $TaskName"
</file>

<file path="python/run_mt5_reporter.bat">
@echo off
setlocal

set "ROOT=%~dp0"
set "PYTHON_EXE=%ROOT%.venv\Scripts\python.exe"
set "SCRIPT_PATH=%ROOT%mt5_reporter.py"
set "LOG_DIR=%ROOT%logs"
set "STDOUT_LOG=%LOG_DIR%\mt5_reporter.out.log"
set "STDERR_LOG=%LOG_DIR%\mt5_reporter.err.log"

if not exist "%LOG_DIR%" mkdir "%LOG_DIR%"

if not exist "%PYTHON_EXE%" (
  echo Python nao encontrado em "%PYTHON_EXE%"
  exit /b 1
)

if not exist "%SCRIPT_PATH%" (
  echo Script nao encontrado em "%SCRIPT_PATH%"
  exit /b 1
)

cd /d "%ROOT%"
"%PYTHON_EXE%" "%SCRIPT_PATH%" 1>> "%STDOUT_LOG%" 2>> "%STDERR_LOG%"
exit /b %ERRORLEVEL%
</file>

<file path="python/run_mt5_reporter.ps1">
param(
  [string]$PythonExe = "$PSScriptRoot\.venv\Scripts\python.exe",
  [string]$ScriptPath = "$PSScriptRoot\mt5_reporter.py",
  [string]$LogDir = "$PSScriptRoot\logs"
)

$ErrorActionPreference = "Stop"

New-Item -ItemType Directory -Path $LogDir -Force | Out-Null

$stdoutLog = Join-Path $LogDir "mt5_reporter.out.log"
$stderrLog = Join-Path $LogDir "mt5_reporter.err.log"

if (-not (Test-Path $PythonExe)) {
  throw "Python nao encontrado em $PythonExe"
}

if (-not (Test-Path $ScriptPath)) {
  throw "Script nao encontrado em $ScriptPath"
}

$workingDir = Split-Path -Path $ScriptPath -Parent
Set-Location $workingDir

& $PythonExe $ScriptPath 1>> $stdoutLog 2>> $stderrLog
exit $LASTEXITCODE
</file>

<file path="python/start_mt5_reporter_hidden.vbs">
Set shell = CreateObject("WScript.Shell")
scriptPath = CreateObject("Scripting.FileSystemObject").GetParentFolderName(WScript.ScriptFullName) & "\run_mt5_reporter.bat"
shell.Run Chr(34) & scriptPath & Chr(34), 0, False
</file>

<file path="README.md">
# Lumitrader

Plataforma de trading algorítmico inteligente com foco inicial em XAUUSD e arquitetura preparada para múltiplos ativos.

## Stack inicial

- Next.js 16
- React 19
- TypeScript
- Tailwind CSS 4
- Supabase JS
- Base PWA

## Rodando localmente

```bash
npm install
npm run dev
```

Abra `http://localhost:3000`.

## Variáveis de ambiente

Use o arquivo [`.env.example`](C:\Lumitechia\Lumitrader-Plus\.env.example) como referência.

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

## Arquivos principais

- [`src/app/page.tsx`](C:\Lumitechia\Lumitrader-Plus\src\app\page.tsx): dashboard inicial do Lumitrader
- [`src/lib/supabase/client.ts`](C:\Lumitechia\Lumitrader-Plus\src\lib\supabase\client.ts): cliente base do Supabase
- [`supabase/schema.sql`](C:\Lumitechia\Lumitrader-Plus\supabase\schema.sql): SQL inicial para o Supabase

## Próximas etapas

1. Executar o SQL no Supabase.
2. Ligar autenticação com Supabase Auth.
3. Integrar o fluxo MT5 -> Python -> n8n -> Supabase.
4. Trocar os dados mockados do dashboard por dados reais.
5. Publicar no Netlify com as mesmas variáveis de ambiente.
</file>

<file path="src/app/api/backend/bridge/commands/route.ts">
import { NextResponse } from "next/server";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export const maxDuration = 60;

import { getIngestToken } from "@/lib/env";
import { createAdminClient } from "@/lib/supabase/admin";

function unauthorized() {
  return NextResponse.json({ ok: false, error: "Unauthorized" }, { status: 401 });
}

export async function GET(request: Request) {
  const authHeader = request.headers.get("authorization");
  const bearer = authHeader?.replace(/^Bearer\s+/i, "");

  if (!bearer || bearer !== getIngestToken()) {
    return unauthorized();
  }

  const url = new URL(request.url);
  const accountNumber = url.searchParams.get("account");
  const adminClient = createAdminClient();

  let query = adminClient
    .from("comandos_trading")
    .select(`
      id,
      ativo,
      timeframe,
      tipo,
      lote,
      stop_loss,
      take_profit,
      ticket_referencia,
      payload,
      conta_trading:conta_trading_id!inner(id, numero_conta)
    `)
    .eq("status", "pending")
    .order("solicitado_em", { ascending: true })
    .limit(20);

  if (accountNumber) {
    query = query.eq("conta_trading.numero_conta", accountNumber);
  }

  const { data, error } = await query;

  if (error) {
    return NextResponse.json({ ok: false, error: error.message }, { status: 500 });
  }

  const rows = data ?? [];
  const commandIds = rows.map((item) => item.id);

  if (commandIds.length > 0) {
    const { error: updateError } = await adminClient
      .from("comandos_trading")
      .update({
        status: "processing",
        processado_em: new Date().toISOString(),
      })
      .in("id", commandIds)
      .eq("status", "pending");

    if (updateError) {
      return NextResponse.json({ ok: false, error: updateError.message }, { status: 500 });
    }
  }

  const commands = rows.map((item) => {
    const tradingAccount = item.conta_trading as { numero_conta?: string } | Array<{ numero_conta?: string }> | null;

    return {
    id: item.id,
    accountNumber: Array.isArray(tradingAccount) ? tradingAccount[0]?.numero_conta : tradingAccount?.numero_conta,
    symbol: item.ativo,
    timeframe: item.timeframe,
    type: item.tipo,
    lot: item.lote != null ? Number(item.lote) : null,
    stopLoss: item.stop_loss != null ? Number(item.stop_loss) : null,
    takeProfit: item.take_profit != null ? Number(item.take_profit) : null,
    referenceTicket: item.ticket_referencia,
    payload: item.payload ?? {},
    };
  });

  return NextResponse.json({ ok: true, commands });
}

export async function POST(request: Request) {
  const authHeader = request.headers.get("authorization");
  const bearer = authHeader?.replace(/^Bearer\s+/i, "");

  if (!bearer || bearer !== getIngestToken()) {
    return unauthorized();
  }

  const body = await request.json();
  const adminClient = createAdminClient();

  const { error } = await adminClient
    .from("comandos_trading")
    .update({
      status: body.status,
      resultado: body.result ?? null,
      erro: body.error ?? null,
      processado_em: new Date().toISOString(),
    })
    .eq("id", body.commandId);

  if (error) {
    return NextResponse.json({ ok: false, error: error.message }, { status: 500 });
  }

  return NextResponse.json({ ok: true });
}
</file>

<file path="src/app/auth/confirm/route.ts">
import { type EmailOtpType } from "@supabase/supabase-js";
import { type NextRequest, NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url);
  const tokenHash = searchParams.get("token_hash");
  const type = searchParams.get("type") as EmailOtpType | null;

  const redirectTo = request.nextUrl.clone();
  redirectTo.pathname = "/dashboard";
  redirectTo.searchParams.delete("token_hash");
  redirectTo.searchParams.delete("type");

  if (tokenHash && type) {
    const supabase = await createClient();
    const { error } = await supabase.auth.verifyOtp({
      type,
      token_hash: tokenHash,
    });

    if (!error) {
      return NextResponse.redirect(redirectTo);
    }
  }

  redirectTo.pathname = "/login";
  redirectTo.search = "";
  redirectTo.searchParams.set("message", "Nao foi possivel confirmar o email.");
  redirectTo.searchParams.set("type", "error");
  return NextResponse.redirect(redirectTo);
}
</file>

<file path="src/app/error/page.tsx">
import Link from "next/link";

export default function ErrorPage() {
  return (
    <main className="min-h-screen px-4 py-6 text-white sm:px-6 lg:px-8">
      <div className="mx-auto flex min-h-[calc(100vh-3rem)] max-w-3xl items-center">
        <section className="glass-panel w-full rounded-[32px] p-8 text-center">
          <p className="font-mono text-xs uppercase tracking-[0.3em] text-red-200/80">
            Fluxo interrompido
          </p>
          <h1 className="mt-4 text-4xl font-semibold">Nao foi possivel concluir a autenticacao.</h1>
          <p className="mt-4 text-slate-300">
            Confira o email de confirmacao, tente novamente ou retorne para a tela de acesso.
          </p>
          <Link
            href="/login"
            className="mt-8 inline-flex rounded-[20px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 font-semibold text-slate-950"
          >
            Voltar para login
          </Link>
        </section>
      </div>
    </main>
  );
}
</file>

<file path="src/app/globals.css">
@import "tailwindcss";

:root {
  --background: #06111e;
  --background-soft: #0b1a2b;
  --panel: rgba(8, 18, 33, 0.72);
  --panel-strong: rgba(12, 25, 45, 0.94);
  --border: rgba(167, 191, 255, 0.16);
  --foreground: #f4f7fb;
  --muted: #95a9c8;
  --accent: #9de833;
  --accent-strong: #4db20c;
  --danger: #ff5f5f;
  --warning: #f6c453;
  --info: #38bdf8;
  --font-sora: "Segoe UI", "Trebuchet MS", system-ui, sans-serif;
  --font-space-grotesk: "Arial Narrow", "Aptos", "Segoe UI", sans-serif;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-sora);
  --font-mono: var(--font-space-grotesk);
}

* {
  box-sizing: border-box;
}

html {
  background:
    radial-gradient(circle at top left, rgba(56, 189, 248, 0.16), transparent 22%),
    radial-gradient(circle at top right, rgba(157, 232, 51, 0.12), transparent 24%),
    linear-gradient(180deg, #09101b 0%, #06111e 45%, #040b14 100%);
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: var(--font-sora), sans-serif;
}

a {
  color: inherit;
  text-decoration: none;
}

::selection {
  background: rgba(157, 232, 51, 0.24);
}

.grid-sheen {
  background-image:
    linear-gradient(rgba(167, 191, 255, 0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(167, 191, 255, 0.06) 1px, transparent 1px);
  background-size: 24px 24px;
}

.glass-panel {
  background: linear-gradient(180deg, rgba(14, 28, 47, 0.95), rgba(8, 18, 33, 0.86));
  border: 1px solid var(--border);
  box-shadow:
    0 24px 80px rgba(0, 0, 0, 0.32),
    inset 0 1px 0 rgba(255, 255, 255, 0.06);
}

.glow-ring {
  box-shadow:
    0 0 0 1px rgba(157, 232, 51, 0.2),
    0 0 30px rgba(157, 232, 51, 0.12);
}
</file>

<file path="src/app/icon.svg">
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <defs>
    <linearGradient id="bg" x1="0%" x2="100%" y1="0%" y2="100%">
      <stop offset="0%" stop-color="#11233a" />
      <stop offset="100%" stop-color="#07111d" />
    </linearGradient>
    <linearGradient id="accent" x1="0%" x2="100%" y1="0%" y2="100%">
      <stop offset="0%" stop-color="#b7ff46" />
      <stop offset="100%" stop-color="#4db20c" />
    </linearGradient>
  </defs>
  <rect width="128" height="128" rx="28" fill="url(#bg)" />
  <path
    d="M33 34h18l19 34 16-34h18L76 94H58L33 34Z"
    fill="url(#accent)"
  />
</svg>
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Lumitrader",
  description: "Plataforma de trading algorítmico inteligente com foco em XAUUSD.",
  applicationName: "Lumitrader",
  manifest: "/manifest.webmanifest",
  icons: {
    icon: "/icon.svg",
    apple: "/icon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className="h-full antialiased">
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
</file>

<file path="src/app/login/actions.ts">
"use server";

import { revalidatePath } from "next/cache";
import { headers } from "next/headers";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

function getBaseUrl(headerList: Headers) {
  const forwardedHost = headerList.get("x-forwarded-host");
  const host = forwardedHost ?? headerList.get("host");
  const protocol = headerList.get("x-forwarded-proto") ?? "http";

  if (process.env.NEXT_PUBLIC_SITE_URL) {
    return process.env.NEXT_PUBLIC_SITE_URL;
  }

  if (!host) {
    return "http://localhost:3000";
  }

  return `${protocol}://${host}`;
}

function toText(value: FormDataEntryValue | null) {
  return typeof value === "string" ? value.trim() : "";
}

export async function login(formData: FormData) {
  const supabase = await createClient();
  const email = toText(formData.get("email"));
  const password = toText(formData.get("password"));

  if (!email || !password) {
    redirect("/login?message=Informe+email+e+senha.&type=error");
  }

  const { error } = await supabase.auth.signInWithPassword({ email, password });

  if (error) {
    redirect(`/login?message=${encodeURIComponent(error.message)}&type=error`);
  }

  revalidatePath("/", "layout");
  redirect("/dashboard");
}

export async function signup(formData: FormData) {
  const supabase = await createClient();
  const headerList = await headers();

  const name = toText(formData.get("name"));
  const email = toText(formData.get("email"));
  const password = toText(formData.get("password"));

  if (!name || !email || !password) {
    redirect("/login?message=Preencha+nome%2C+email+e+senha.&type=error");
  }

  const redirectTo = `${getBaseUrl(headerList)}/auth/confirm`;

  const { error } = await supabase.auth.signUp({
    email,
    password,
    options: {
      emailRedirectTo: redirectTo,
      data: {
        nome: name,
      },
    },
  });

  if (error) {
    redirect(`/login?message=${encodeURIComponent(error.message)}&type=error`);
  }

  revalidatePath("/", "layout");
  redirect("/login?message=Conta+criada.+Confirme+seu+email+para+entrar.&type=success");
}

export async function signout() {
  const supabase = await createClient();

  await supabase.auth.signOut();

  revalidatePath("/", "layout");
  redirect("/login?message=Sessao+encerrada.&type=success");
}
</file>

<file path="src/app/login/page.tsx">
import Link from "next/link";
import { redirect } from "next/navigation";
import { login, signup } from "@/app/login/actions";
import { createClient } from "@/lib/supabase/server";

type LoginPageProps = {
  searchParams: Promise<{
    message?: string;
    type?: string;
  }>;
};

export default async function LoginPage({ searchParams }: LoginPageProps) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (user) {
    redirect("/dashboard");
  }

  const params = await searchParams;
  const message = params.message;
  const isSuccess = params.type === "success";

  return (
    <main className="min-h-screen overflow-hidden px-4 py-6 text-white sm:px-6 lg:px-8">
      <div className="mx-auto grid min-h-[calc(100vh-3rem)] w-full max-w-7xl gap-6 lg:grid-cols-[1.1fr_0.9fr]">
        <section className="glass-panel relative overflow-hidden rounded-[32px] p-6 sm:p-8">
          <div className="absolute inset-0 bg-[radial-gradient(circle_at_top_left,rgba(157,232,51,0.18),transparent_30%),radial-gradient(circle_at_bottom_right,rgba(34,211,238,0.16),transparent_28%)]" />
          <div className="relative">
            <div className="flex items-center gap-4">
              <div className="flex h-14 w-14 items-center justify-center rounded-[22px] bg-lime-400/12 text-2xl font-bold text-lime-300 glow-ring">
                L
              </div>
              <div>
                <p className="font-mono text-xs uppercase tracking-[0.32em] text-cyan-200/70">
                  Plataforma de Trading Algoritmico
                </p>
                <h1 className="font-mono text-3xl font-bold tracking-tight sm:text-4xl">
                  Lumitrader
                </h1>
              </div>
            </div>

            <div className="mt-10 max-w-2xl">
              <p className="font-mono text-xs uppercase tracking-[0.32em] text-lime-300/80">
                Acesso protegido
              </p>
              <h2 className="mt-3 text-4xl font-semibold leading-tight sm:text-5xl">
                Controle algoritmico com autenticacao real e painel em tempo real.
              </h2>
              <p className="mt-5 max-w-xl text-base leading-7 text-slate-300">
                Esta etapa ja deixa o Lumitrader pronto para proteger usuarios, persistir
                sessao no App Router e preparar o fluxo vivo de conta MT5, operacoes,
                saldo e equity.
              </p>
            </div>

            <div className="mt-10 grid gap-4 sm:grid-cols-3">
              <FeatureCard
                title="Auth SSR"
                description="Cookies seguros e paginas protegidas com Supabase no Next.js."
              />
              <FeatureCard
                title="Conta MT5"
                description="Painel preparado para numero da conta, cliente, saldo e equity."
              />
              <FeatureCard
                title="Realtime"
                description="Estrutura pronta para assinaturas ao vivo de conta e operacoes."
              />
            </div>

            <div className="mt-8 rounded-[28px] border border-white/8 bg-slate-950/35 p-5">
              <p className="font-mono text-xs uppercase tracking-[0.28em] text-cyan-200/70">
                Proxima camada
              </p>
              <ul className="mt-4 grid gap-3 text-sm text-slate-200">
                <li className="rounded-[20px] border border-white/8 bg-white/4 px-4 py-3">
                  Webhooks MT5 -&gt; Python -&gt; n8n -&gt; Supabase.
                </li>
                <li className="rounded-[20px] border border-white/8 bg-white/4 px-4 py-3">
                  Candles estilo MT5 com atualizacao em tempo real.
                </li>
                <li className="rounded-[20px] border border-white/8 bg-white/4 px-4 py-3">
                  Auditoria operacional com logs e validacao por IA sob demanda.
                </li>
              </ul>
            </div>
          </div>
        </section>

        <section className="glass-panel rounded-[32px] p-6 sm:p-8">
          <div className="flex items-center justify-between gap-4">
            <div>
              <p className="font-mono text-xs uppercase tracking-[0.28em] text-cyan-200/70">
                Entrar no sistema
              </p>
              <h2 className="mt-2 text-3xl font-semibold">Sua mesa operacional</h2>
            </div>
            <span className="rounded-full border border-lime-400/30 bg-lime-400/10 px-3 py-1 text-sm text-lime-300">
              Supabase Auth
            </span>
          </div>

          {message ? (
            <div
              className={`mt-6 rounded-[22px] border px-4 py-3 text-sm ${
                isSuccess
                  ? "border-lime-400/20 bg-lime-400/10 text-lime-200"
                  : "border-red-400/20 bg-red-400/10 text-red-200"
              }`}
            >
              {message}
            </div>
          ) : null}

          <div className="mt-6 grid gap-5">
            <form className="rounded-[28px] border border-white/8 bg-white/4 p-5">
              <p className="text-lg font-semibold">Entrar</p>
              <div className="mt-4 grid gap-4">
                <AuthField label="Email" name="email" type="email" placeholder="voce@lumitrader.com" />
                <AuthField label="Senha" name="password" type="password" placeholder="Sua senha" />
              </div>
              <button
                formAction={login}
                className="mt-5 flex w-full items-center justify-center rounded-[20px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-4 py-3 text-base font-semibold text-slate-950 transition-transform hover:-translate-y-0.5"
              >
                Acessar dashboard
              </button>
            </form>

            <form className="rounded-[28px] border border-white/8 bg-white/4 p-5">
              <p className="text-lg font-semibold">Criar conta</p>
              <div className="mt-4 grid gap-4">
                <AuthField label="Nome do cliente" name="name" type="text" placeholder="Seu nome" />
                <AuthField label="Email" name="email" type="email" placeholder="voce@lumitrader.com" />
                <AuthField label="Senha" name="password" type="password" placeholder="Crie uma senha" />
              </div>
              <button
                formAction={signup}
                className="mt-5 flex w-full items-center justify-center rounded-[20px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-base font-semibold text-cyan-100 transition-colors hover:bg-cyan-400/16"
              >
                Criar acesso seguro
              </button>
            </form>
          </div>

          <p className="mt-6 text-sm leading-6 text-slate-400">
            Quando o cadastro for confirmado por email, o dashboard passa a carregar os
            dados do usuario autenticado e a conta de trading vinculada.
          </p>
          <p className="mt-3 text-sm text-slate-500">
            Deploy previsto para Netlify. Depois dessa etapa, seguimos para os webhooks
            operacionais e realtime.
          </p>
          <div className="mt-6 text-sm text-slate-400">
            Se precisar voltar para o inicio, use <Link href="/" className="text-lime-300">/</Link>.
          </div>
        </section>
      </div>
    </main>
  );
}

function FeatureCard({ title, description }: { title: string; description: string }) {
  return (
    <div className="rounded-[24px] border border-white/8 bg-white/4 p-4">
      <p className="font-mono text-xs uppercase tracking-[0.24em] text-cyan-200/70">{title}</p>
      <p className="mt-3 text-sm leading-6 text-slate-300">{description}</p>
    </div>
  );
}

function AuthField({
  label,
  name,
  type,
  placeholder,
}: {
  label: string;
  name: string;
  type: string;
  placeholder: string;
}) {
  return (
    <label className="grid gap-2">
      <span className="text-sm text-slate-300">{label}</span>
      <input
        name={name}
        type={type}
        placeholder={placeholder}
        required
        className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none transition-colors placeholder:text-slate-500 focus:border-lime-400/40"
      />
    </label>
  );
}
</file>

<file path="src/app/manifest.ts">
import type { MetadataRoute } from "next";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "Lumitrader",
    short_name: "Lumitrader",
    description: "Plataforma de trading algorítmico inteligente com dashboard operacional.",
    start_url: "/",
    display: "standalone",
    background_color: "#06111e",
    theme_color: "#06111e",
    icons: [
      {
        src: "/icon.svg",
        sizes: "any",
        type: "image/svg+xml",
      },
    ],
  };
}
</file>

<file path="src/app/page.tsx">
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export default async function HomePage() {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (user) {
    redirect("/dashboard");
  }

  redirect("/login");
}
</file>

<file path="src/components/auth/signout-button.tsx">
"use client";

import { useState } from "react";

export function SignoutButton({
  action,
  label = "Encerrar sessao",
  className,
}: {
  action: (formData: FormData) => void | Promise<void>;
  label?: string;
  className?: string;
}) {
  const [open, setOpen] = useState(false);

  return (
    <>
      <button type="button" onClick={() => setOpen(true)} className={className}>
        {label}
      </button>

      {open ? (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/70 px-4">
          <div className="glass-panel w-full max-w-md rounded-[28px] p-6">
            <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Confirmacao</p>
            <h3 className="mt-3 text-2xl font-semibold">Deseja encerrar a sessao?</h3>
            <p className="mt-3 text-sm text-slate-300">Voce sera redirecionado para a tela de login.</p>
            <div className="mt-6 flex gap-3">
              <button
                type="button"
                onClick={() => setOpen(false)}
                className="flex-1 rounded-[18px] border border-white/10 bg-white/5 px-4 py-3 text-sm font-semibold text-slate-200"
              >
                Cancelar
              </button>
              <form action={action} className="flex-1">
                <button className="w-full rounded-[18px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-4 py-3 text-sm font-semibold text-slate-950">
                  Confirmar
                </button>
              </form>
            </div>
          </div>
        </div>
      ) : null}
    </>
  );
}
</file>

<file path="src/lib/auth.ts">
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export type AppUser = {
  id: string;
  nome: string | null;
  email: string | null;
  role: "admin" | "user";
  acesso_ativo: boolean;
  ativo_padrao: string;
  timeframe_padrao: string;
};

export async function requireAuthenticatedUser() {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    redirect("/login");
  }

  const { data: profile } = await supabase
    .from("usuarios")
    .select("id, nome, email, role, acesso_ativo, ativo_padrao, timeframe_padrao")
    .eq("id", user.id)
    .maybeSingle<AppUser>();

  const resolvedProfile: AppUser =
    profile ?? {
      id: user.id,
      nome: (user.user_metadata.nome as string | undefined) ?? null,
      email: user.email ?? null,
      role: "user",
      acesso_ativo: true,
      ativo_padrao: "XAUUSD",
      timeframe_padrao: "M5",
    };

  return { supabase, authUser: user, profile: resolvedProfile };
}

export async function requireAdminUser() {
  const context = await requireAuthenticatedUser();

  if (context.profile.role !== "admin") {
    redirect("/dashboard");
  }

  return context;
}
</file>

<file path="src/lib/backend/auto-trader.ts">
import type { LoadedContext } from "@/lib/backend/supabase";
import type { TradingEventPayload } from "@/lib/backend/types";

const BASE_AUTO_LOT = 0.1;

type AutoSignal = {
  type: "open_buy" | "open_sell";
  lot: number;
  stopLoss: number;
  takeProfit: number;
  rationale: string;
};

function roundPrice(value: number) {
  return Number(value.toFixed(2));
}

function computeRiskDistance(entry: number, fallback: number | null | undefined) {
  const structuralDistance = fallback != null ? Math.abs(entry - fallback) : 0;
  const minimumDistance = Math.max(entry * 0.001, 2);
  return Math.max(structuralDistance, minimumDistance);
}

export function evaluateAutoOpportunity(
  context: LoadedContext,
  payload: TradingEventPayload,
  operationsToday: number,
): AutoSignal | null {
  if (payload.event !== "account_sync" || !payload.market || !context.config?.sistema_ligado) {
    return null;
  }

  const market = payload.market;
  const trend = market.trend;
  const rsi = market.rsi ?? null;
  const movingAverage20 = market.moving_average_20 ?? null;
  const support = market.support ?? null;
  const resistance = market.resistance ?? null;
  const lastBid = market.last_bid ?? null;
  const lastAsk = market.last_ask ?? null;

  if (!trend || rsi == null || movingAverage20 == null || lastBid == null || lastAsk == null) {
    return null;
  }

  if (context.config.limite_operacoes_ativo && context.config.limite_operacoes_diaria != null && operationsToday >= context.config.limite_operacoes_diaria) {
    return null;
  }

  const floatingLoss = Math.max((payload.account.balance ?? 0) - (payload.account.equity ?? 0), 0);
  if (context.config.perda_maxima_diaria > 0 && floatingLoss >= context.config.perda_maxima_diaria) {
    return null;
  }

  if (trend === "uptrend" && rsi >= 55 && rsi <= 68 && lastAsk > movingAverage20) {
    const entry = lastAsk;
    const riskDistance = computeRiskDistance(entry, support);
    const stopLoss = roundPrice(support != null && support < entry ? support : entry - riskDistance);
    const takeProfit = roundPrice(resistance != null && resistance > entry ? resistance : entry + riskDistance * 2);

    if (stopLoss < entry && takeProfit > entry) {
      return {
        type: "open_buy",
        lot: BASE_AUTO_LOT,
        stopLoss,
        takeProfit,
        rationale: `Signal uptrend RSI ${rsi.toFixed(1)} above MA20`,
      };
    }
  }

  if (trend === "downtrend" && rsi >= 32 && rsi <= 45 && lastBid < movingAverage20) {
    const entry = lastBid;
    const riskDistance = computeRiskDistance(entry, resistance);
    const stopLoss = roundPrice(resistance != null && resistance > entry ? resistance : entry + riskDistance);
    const takeProfit = roundPrice(support != null && support < entry ? support : entry - riskDistance * 2);

    if (stopLoss > entry && takeProfit < entry) {
      return {
        type: "open_sell",
        lot: BASE_AUTO_LOT,
        stopLoss,
        takeProfit,
        rationale: `Signal downtrend RSI ${rsi.toFixed(1)} below MA20`,
      };
    }
  }

  return null;
}
</file>

<file path="src/lib/backend/formatters.ts">
import type { ReportPayload } from "@/lib/backend/types";

function escapeCsv(value: string | number | null | undefined) {
  const normalized = value == null ? "" : String(value);
  return `"${normalized.replaceAll('"', '""')}"`;
}

export function toCsv(report: Omit<ReportPayload, "formats">) {
  const rows = [
    ["event", report.event],
    ["generatedAt", report.generatedAt],
    ["user", report.user.nome ?? report.user.email ?? ""],
    ["telegram_id", report.user.telegram_id ?? ""],
    ["account_number", report.account.number],
    ["license_plan", report.account.active_license.plan],
    ["symbol", report.operation.symbol],
    ["side", report.operation.side],
    ["lot", report.operation.lot],
    ["entry_price", report.operation.entry_price],
    ["exit_price", report.operation.exit_price ?? ""],
    ["profit_loss", report.operation.profit_loss ?? ""],
    ["risk_amount", report.risk.riskAmount],
    ["reward_amount", report.risk.rewardAmount ?? ""],
    ["risk_reward_ratio", report.risk.riskRewardRatio ?? ""],
    ["ai_summary", report.ai.summary],
  ];

  return rows.map((row) => row.map((value) => escapeCsv(value)).join(",")).join("\n");
}

export function toHtml(report: Omit<ReportPayload, "formats">) {
  return `
    <html>
      <body style="font-family: Arial, sans-serif; background:#081221; color:#f8fafc; padding:24px;">
        <h1>Lumitrader Report</h1>
        <p><strong>Evento:</strong> ${report.event}</p>
        <p><strong>Cliente:</strong> ${report.user.nome ?? report.user.email ?? "-"}</p>
        <p><strong>Conta MT5:</strong> ${report.account.number}</p>
        <p><strong>Ativo:</strong> ${report.operation.symbol}</p>
        <p><strong>Lado:</strong> ${report.operation.side}</p>
        <p><strong>Lote:</strong> ${report.operation.lot}</p>
        <p><strong>Entrada:</strong> ${report.operation.entry_price}</p>
        <p><strong>Saida:</strong> ${report.operation.exit_price ?? "-"}</p>
        <p><strong>P/L:</strong> ${report.operation.profit_loss ?? "-"}</p>
        <p><strong>Resumo IA:</strong></p>
        <pre style="white-space:pre-wrap;background:#0f172a;padding:16px;border-radius:12px;">${report.ai.summary}</pre>
      </body>
    </html>
  `.trim();
}
</file>

<file path="src/lib/backend/n8n.ts">
import { getN8nReportWebhookUrl } from "@/lib/env";
import type { ReportPayload } from "@/lib/backend/types";

export async function sendReportToN8n(report: ReportPayload) {
  const response = await fetch(getN8nReportWebhookUrl(), {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify(report),
  });

  if (!response.ok) {
    throw new Error(`n8n webhook error: ${response.status}`);
  }
}
</file>

<file path="src/lib/backend/openai.ts">
import OpenAI from "openai";
import { getOpenAiApiKey } from "@/lib/env";
import type { ReportPayload } from "@/lib/backend/types";

const model = "gpt-5";

function buildPrompt(report: Omit<ReportPayload, "ai" | "formats">) {
  return [
    "Voce e o analista do Lumitrader.",
    "Resuma a operacao em portugues do Brasil, com foco objetivo e operacional.",
    "Inclua contexto de risco, situacao da conta e um comentario util para Telegram.",
    "Se o evento for de abertura, destaque o setup e o risco.",
    "Se o evento for de fechamento, destaque resultado, risco-retorno e disciplina operacional.",
    "Responda em no maximo 5 linhas curtas.",
    JSON.stringify(report),
  ].join("\n");
}

export async function generateAiSummary(report: Omit<ReportPayload, "ai" | "formats">) {
  const client = new OpenAI({ apiKey: getOpenAiApiKey() });
  const response = await client.responses.create({
    model,
    input: buildPrompt(report),
  });

  return {
    summary: response.output_text.trim(),
    request_id: response._request_id ?? null,
    model,
  };
}
</file>

<file path="src/lib/supabase/admin.ts">
import { createClient } from "@supabase/supabase-js";
import { getServiceRoleKey, getSupabaseEnv } from "@/lib/env";

export function createAdminClient() {
  const { url } = getSupabaseEnv();
  const serviceRoleKey = getServiceRoleKey();

  return createClient(url, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });
}
</file>

<file path="src/lib/supabase/client.ts">
import { createBrowserClient } from "@supabase/ssr";
import { getSupabaseEnv } from "@/lib/env";

export function createClient() {
  const { url, anonKey } = getSupabaseEnv();

  return createBrowserClient(url, anonKey);
}
</file>

<file path="src/lib/supabase/proxy.ts">
import { createServerClient } from "@supabase/ssr";
import { type NextRequest, NextResponse } from "next/server";
import { getSupabaseEnv } from "@/lib/env";

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({
    request,
  });

  const { url, anonKey } = getSupabaseEnv();

  const supabase = createServerClient(url, anonKey, {
    cookies: {
      getAll() {
        return request.cookies.getAll();
      },
      setAll(cookiesToSet) {
        cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value));

        response = NextResponse.next({
          request,
        });

        cookiesToSet.forEach(({ name, value, options }) => {
          response.cookies.set(name, value, options);
        });
      },
    },
  });

  await supabase.auth.getClaims();

  return response;
}
</file>

<file path="src/lib/supabase/server.ts">
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import { getSupabaseEnv } from "@/lib/env";

export async function createClient() {
  const cookieStore = await cookies();
  const { url, anonKey } = getSupabaseEnv();

  return createServerClient(url, anonKey, {
    cookies: {
      getAll() {
        return cookieStore.getAll();
      },
      setAll(cookiesToSet) {
        try {
          cookiesToSet.forEach(({ name, value, options }) => {
            cookieStore.set(name, value, options);
          });
        } catch {
          // Server Components cannot set cookies during rendering.
        }
      },
    },
  });
}
</file>

<file path="supabase/migrations/20260325_consolidate_mt5_server.sql">
update public.contas_trading
set servidor = coalesce(nullif(mt5_server, ''), servidor)
where coalesce(nullif(mt5_server, ''), '') <> '';

alter table public.contas_trading
  drop column if exists mt5_server;
</file>

<file path="supabase/migrations/20260325_trading_commands.sql">
create type public.tipo_comando_trading as enum ('open_buy', 'open_sell', 'close_position');
create type public.status_comando_trading as enum ('pending', 'processing', 'executed', 'failed', 'cancelled');

create table if not exists public.comandos_trading (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo text not null,
  timeframe text not null default 'M5',
  tipo public.tipo_comando_trading not null,
  status public.status_comando_trading not null default 'pending',
  lote numeric(10, 2),
  stop_loss numeric(12, 5),
  take_profit numeric(12, 5),
  ticket_referencia text,
  payload jsonb,
  resultado jsonb,
  erro text,
  solicitado_em timestamptz not null default timezone('utc', now()),
  processado_em timestamptz
);

alter table public.comandos_trading enable row level security;

drop policy if exists "comandos_trading_select_policy" on public.comandos_trading;
create policy "comandos_trading_select_policy"
on public.comandos_trading
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "comandos_trading_write_policy" on public.comandos_trading;
create policy "comandos_trading_write_policy"
on public.comandos_trading
for all
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
)
with check (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'comandos_trading'
  ) then
    alter publication supabase_realtime add table public.comandos_trading;
  end if;
end
$$;

create index if not exists idx_comandos_trading_conta_status_solicitado on public.comandos_trading (conta_trading_id, status, solicitado_em asc);
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path="package.json">
{
  "name": "lumitrader-plus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@supabase/ssr": "^0.9.0",
    "@supabase/supabase-js": "^2.100.0",
    "next": "16.2.1",
    "openai": "^6.32.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
</file>

<file path="python/requirements.txt">
metatrader5>=5.0.5640
requests==2.32.3
python-dotenv==1.0.1
</file>

<file path="src/lib/backend/risk.ts">
import type { RiskSnapshot, TradingEventPayload } from "@/lib/backend/types";

type SessionConfig = {
  daily_loss_limit?: number | null;
  operation_limit_enabled?: boolean | null;
  operation_limit?: number | null;
};

export function calculateRiskSnapshot(input: {
  payload: TradingEventPayload;
  balance: number;
  equity: number;
  operationsToday: number;
  sessionConfig?: SessionConfig | null;
}): RiskSnapshot {
  const { payload, balance, equity, operationsToday, sessionConfig } = input;
  const operation = payload.operation;

  if (!operation) {
    return {
      riskAmount: 0,
      rewardAmount: null,
      riskRewardRatio: null,
      floatingDifference: equity - balance,
      spreadCostEstimate: null,
      remainingDailyLoss: typeof sessionConfig?.daily_loss_limit === "number" ? sessionConfig.daily_loss_limit : null,
      remainingOperationsToday: sessionConfig?.operation_limit_enabled && typeof sessionConfig.operation_limit === "number"
        ? Math.max(sessionConfig.operation_limit - operationsToday, 0)
        : null,
    };
  }

  const stopDistance = operation.stop_loss ? Math.abs(operation.entry_price - operation.stop_loss) : null;
  const targetDistance = operation.take_profit ? Math.abs(operation.take_profit - operation.entry_price) : null;
  const riskAmount = stopDistance ? stopDistance * operation.lot * 100 : 0;
  const rewardAmount = targetDistance ? targetDistance * operation.lot * 100 : null;
  const riskRewardRatio = riskAmount > 0 && rewardAmount !== null ? rewardAmount / riskAmount : null;
  const floatingDifference = equity - balance;
  const spreadCostEstimate = operation.spread ? operation.spread * operation.lot * 10 : null;
  const remainingDailyLoss = typeof sessionConfig?.daily_loss_limit === "number"
    ? Math.max(sessionConfig.daily_loss_limit - Math.max(0, Math.abs(Math.min(operation.profit_loss ?? 0, 0))), 0)
    : null;
  const remainingOperationsToday = sessionConfig?.operation_limit_enabled && typeof sessionConfig.operation_limit === "number"
    ? Math.max(sessionConfig.operation_limit - operationsToday, 0)
    : null;

  return {
    riskAmount,
    rewardAmount,
    riskRewardRatio,
    floatingDifference,
    spreadCostEstimate,
    remainingDailyLoss,
    remainingOperationsToday,
  };
}
</file>

<file path="src/lib/env.ts">
const requiredPublicEnvVars = {
  NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
  NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
} as const;

export function hasSupabaseEnv() {
  return Object.values(requiredPublicEnvVars).every(Boolean);
}

export function getSupabaseEnv() {
  const missing = Object.entries(requiredPublicEnvVars)
    .filter(([, value]) => !value)
    .map(([key]) => key);

  if (missing.length > 0) {
    throw new Error(`Missing required environment variables: ${missing.join(", ")}`);
  }

  return {
    url: requiredPublicEnvVars.NEXT_PUBLIC_SUPABASE_URL as string,
    anonKey: requiredPublicEnvVars.NEXT_PUBLIC_SUPABASE_ANON_KEY as string,
  };
}

export function getSiteUrl() {
  return process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";
}

function getRequiredServerEnv(name: string) {
  const value = process.env[name];

  if (!value) {
    throw new Error(`Missing required environment variable: ${name}`);
  }

  return value;
}

export function getServiceRoleKey() {
  return getRequiredServerEnv("SUPABASE_SERVICE_ROLE_KEY");
}

export function getOpenAiApiKey() {
  return getRequiredServerEnv("OPENAI_API_KEY");
}

export function getN8nReportWebhookUrl() {
  return getRequiredServerEnv("N8N_REPORT_WEBHOOK_URL");
}

export function getIngestToken() {
  return getRequiredServerEnv("LUMITRADER_INGEST_TOKEN");
}
</file>

<file path="python/README.md">
# Lumitrader Python Layer

Arquivos:

- `mt5_reporter.py`: bridge multi-conta para VPS Windows com MT5 aberto.
- `send_test_event.py`: dispara um evento de teste manual no backend.
- `.env.example`: variaveis do bridge.
- `requirements.txt`: dependencias Python.

Uso rapido:

```bash
pip install -r requirements.txt
copy .env.example .env
python send_test_event.py
python mt5_reporter.py
```

O `mt5_reporter.py` nao depende mais de login, servidor e senha no `.env`.
Esses dados passam a vir do banco, por conta/licenca, sempre que a conta estiver em `Play`.

A `.env` da pasta `python` agora precisa ter apenas:

```txt
LUMITRADER_BACKEND_URL
LUMITRADER_INGEST_TOKEN
POLL_INTERVAL_SECONDS
```

Fluxo do bridge:

1. Busca no backend quais contas estao ativas e em `Play`.
2. Faz login sequencial nelas no MT5 da VPS.
3. Envia `account_sync` com saldo, equity, corretora, horario do servidor e candles.
4. Envia `operation_opened` e `operation_closed` quando detectar ordens.

## Rodar 24/7 no Windows (Hyonix)

Caminho recomendado: Agendador de Tarefas do Windows.

1. Confirme que o MT5 esta instalado, aberto e logado na VPS.
2. Na pasta `python`, instale as dependencias com `python -m pip install -r requirements.txt`.
3. Confirme que `python/.env` tem `LUMITRADER_BACKEND_URL`, `LUMITRADER_INGEST_TOKEN` e `POLL_INTERVAL_SECONDS`.
4. Se quiser testar manualmente, rode `powershell -ExecutionPolicy Bypass -File .\run_mt5_reporter.ps1`.
5. Para instalar a execucao automatica ao ligar a VPS, rode `powershell -ExecutionPolicy Bypass -File .\install_mt5_reporter_task.ps1`.
6. Logs ficam em `python\logs\mt5_reporter.out.log` e `python\logs\mt5_reporter.err.log`.

Comandos uteis:

```powershell
Get-ScheduledTask -TaskName "Lumitrader MT5 Reporter"
Start-ScheduledTask -TaskName "Lumitrader MT5 Reporter"
Stop-ScheduledTask -TaskName "Lumitrader MT5 Reporter"
Get-Content .\logs\mt5_reporter.out.log -Tail 50
Get-Content .\logs\mt5_reporter.err.log -Tail 50
```

Observacoes:

- O bridge depende do terminal MT5 estar aberto na sessao do Windows.
- Se a Hyonix reiniciar a maquina, a tarefa sobe novamente no boot.
- Se preferir rodar somente apos logon do usuario em vez de startup, o script de instalacao pode ser ajustado.
## Opcao sem PowerShell

Se a Hyonix bloquear PowerShell, use os arquivos abaixo pela interface normal do Windows ou pelo `cmd`:

- `run_mt5_reporter.bat`: roda o bridge e grava logs em `python\logs`.
- `start_mt5_reporter_hidden.vbs`: inicia o `.bat` sem abrir janela.
- `install_mt5_reporter_startup.bat`: copia o `.vbs` para a pasta Startup do usuario.

Fluxo recomendado:

1. Teste manual: clique duas vezes em `run_mt5_reporter.bat` ou rode `cmd /c run_mt5_reporter.bat`.
2. Se estiver tudo certo, instale o auto start: clique duas vezes em `install_mt5_reporter_startup.bat`.
3. Depois faca logoff/logon ou reinicie a VPS.
4. Confira os logs em `python\logs\mt5_reporter.out.log` e `python\logs\mt5_reporter.err.log`.

Observacao:

- Essa opcao inicia depois que o usuario entra na sessao do Windows. Como o MT5 precisa estar aberto na sessao grafica, isso costuma funcionar melhor do que startup de sistema em VPS Windows.
- Se voce realmente quiser um `.exe`, o caminho mais direto e empacotar esse launcher com PyInstaller em uma maquina onde possamos instalar a dependencia.
</file>

<file path="python/send_test_event.py">
import os
import sys
from datetime import datetime, timedelta, timezone
from pathlib import Path

import requests
from dotenv import dotenv_values

ENV_PATH = Path(__file__).with_name(".env")


def load_env_file() -> None:
    values = dotenv_values(ENV_PATH, encoding="utf-8-sig")
    for key, value in values.items():
        if key and value is not None:
            os.environ[key] = value


load_env_file()

BACKEND_URL = os.getenv("LUMITRADER_BACKEND_URL", "http://localhost:3000/api/backend/trading/events")
INGEST_TOKEN = os.getenv("LUMITRADER_INGEST_TOKEN", "")
ACCOUNT_NUMBER = os.getenv("TEST_ACCOUNT_NUMBER", "1512756960")


def safe_print(value: str) -> None:
    encoding = sys.stdout.encoding or "utf-8"
    print(value.encode(encoding, errors="replace").decode(encoding, errors="replace"))


def build_payload(event: str) -> dict:
    now = datetime.now(timezone.utc)
    opened_at = (now - timedelta(minutes=12)).isoformat().replace("+00:00", "Z")
    closed_at = now.isoformat().replace("+00:00", "Z")

    operation = {
        "ticket": "demo-ticket-001",
        "symbol": "XAUUSD",
        "timeframe": "M5",
        "side": "sell",
        "lot": 0.5,
        "entry_price": 3035.40,
        "stop_loss": 3041.20,
        "take_profit": 3028.10,
        "profit_loss": 137.50 if event == "operation_closed" else 0,
        "opened_at": opened_at,
    }

    if event == "operation_closed":
        operation["exit_price"] = 3032.65
        operation["closed_at"] = closed_at
        operation["close_reason"] = "tp"

    return {
        "event": event,
        "account": {
            "number": str(ACCOUNT_NUMBER),
            "broker": "FTMO",
            "server": "FTMO-Demo",
            "server_time": closed_at,
            "name": "Conta de teste",
            "currency_code": "USD",
            "currency_symbol": "$",
            "balance": 10000,
            "equity": 10137.5,
            "margin": 500,
            "free_margin": 9637.5,
            "margin_level": 2027.5,
            "leverage": 100,
        },
        "operation": operation,
        "session": {
            "mode": "agressivo",
            "breakeven_enabled": True,
            "trailing_stop_enabled": True,
            "profit_target": 1000,
            "daily_loss_limit": 500,
            "operation_limit_enabled": True,
            "operation_limit": 4,
        },
        "market": {
            "trend": "bearish",
            "rsi": 62,
            "moving_average_20": 3038.10,
            "support": 3029.30,
            "resistance": 3042.50,
            "notes": ["Teste manual do fluxo backend + Supabase + OpenAI + n8n"],
            "candles": [
                {"time": opened_at, "open": 3036.1, "high": 3037.2, "low": 3034.8, "close": 3035.4},
                {"time": closed_at, "open": 3035.4, "high": 3036.0, "low": 3032.4, "close": 3032.65}
            ]
        },
    }


def main() -> None:
    if not INGEST_TOKEN:
        raise RuntimeError("Missing LUMITRADER_INGEST_TOKEN")

    event = os.getenv("LUMITRADER_TEST_EVENT", "operation_closed")
    payload = build_payload(event)

    response = requests.post(
        BACKEND_URL,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {INGEST_TOKEN}",
        },
        json=payload,
        timeout=30,
    )
    print(response.status_code)
    safe_print(response.text)
    response.raise_for_status()


if __name__ == "__main__":
    main()
</file>

<file path="src/app/api/backend/bridge/accounts/route.ts">
import { NextResponse } from "next/server";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export const maxDuration = 60;
import { getIngestToken } from "@/lib/env";
import { createAdminClient } from "@/lib/supabase/admin";

function unauthorized() {
  return NextResponse.json({ ok: false, error: "Unauthorized" }, { status: 401 });
}

export async function GET(request: Request) {
  const authHeader = request.headers.get("authorization");
  const bearer = authHeader?.replace(/^Bearer\s+/i, "");

  if (!bearer || bearer !== getIngestToken()) {
    return unauthorized();
  }

  const adminClient = createAdminClient();
  const { data, error } = await adminClient
    .from("contas_trading")
    .select(`
      id,
      numero_conta,
      servidor,
      mt5_password,
      ativo,
      usuarios!inner(id, acesso_ativo),
      licencas!inner(status, data_expiracao),
      configuracoes_sessao!inner(id, ativo, timeframe, modo, sistema_ligado, breakeven_ativo, trailing_stop_ativo, meta_lucro_diaria, perda_maxima_diaria, limite_operacoes_ativo, limite_operacoes_diaria)
    `)
    .eq("usuarios.acesso_ativo", true)
    .eq("licencas.status", "ativa")
    .eq("configuracoes_sessao.sistema_ligado", true)
    .gte("licencas.data_expiracao", new Date().toISOString().slice(0, 10));

  if (error) {
    return NextResponse.json({ ok: false, error: error.message }, { status: 500 });
  }

  const accounts = (data ?? [])
    .filter((item) => item.servidor && item.mt5_password)
    .map((item) => ({
      id: item.id,
      number: item.numero_conta,
      server: item.servidor,
      password: item.mt5_password,
      config: Array.isArray(item.configuracoes_sessao) ? item.configuracoes_sessao[0] : item.configuracoes_sessao,
    }));

  return NextResponse.json({ ok: true, accounts });
}
</file>

<file path="src/app/api/backend/trading/events/route.ts">
import { NextResponse } from "next/server";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export const maxDuration = 60;
import { getIngestToken } from "@/lib/env";
import { processTradingEvent } from "@/lib/backend/reporting";
import type { TradingEventPayload } from "@/lib/backend/types";

function unauthorized() {
  return NextResponse.json({ ok: false, error: "Unauthorized" }, { status: 401 });
}

export async function POST(request: Request) {
  const authHeader = request.headers.get("authorization");
  const bearer = authHeader?.replace(/^Bearer\s+/i, "");

  if (!bearer || bearer !== getIngestToken()) {
    return unauthorized();
  }

  try {
    const payload = (await request.json()) as TradingEventPayload;

    if (!payload?.event || !payload?.account?.number) {
      return NextResponse.json({ ok: false, error: "Invalid payload" }, { status: 400 });
    }

    if (payload.event !== "account_sync" && !payload?.operation?.symbol) {
      return NextResponse.json({ ok: false, error: "Operation payload required" }, { status: 400 });
    }

    const report = await processTradingEvent(payload);
    return NextResponse.json({ ok: true, report });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Unknown error";
    return NextResponse.json({ ok: false, error: message }, { status: 500 });
  }
}
</file>

<file path="src/components/admin/admin-users-panel.tsx">
"use client";

import { useState } from "react";
import { renewLicense30Days, saveLicense, toggleUserAccess, updateLicenseStatus, updateManagedUser } from "@/app/admin/actions";

type ManagedUser = {
  id: string;
  nome: string | null;
  email: string | null;
  telegram_id: string | null;
  role: "admin" | "user";
  acesso_ativo: boolean;
};

type ManagedLicense = {
  id: string;
  user_id: string;
  nome_plano: string;
  status: string;
  valor: number;
  data_expiracao: string;
  conta_trading_id: string;
};

type ManagedAccount = {
  id: string;
  user_id: string;
  nome_cliente: string;
  numero_conta: string;
  servidor: string | null;
  mt5_password: string | null;
  alavancagem: number | null;
};

const PLAN_OPTIONS = ["Starter", "Pro", "Premium", "Enterprise"];
const LICENSE_STATUS_OPTIONS = [
  { value: "ativa", label: "Ativa" },
  { value: "pendente", label: "Pendente" },
  { value: "bloqueada", label: "Bloqueada" },
  { value: "cancelada", label: "Cancelada" },
  { value: "expirada", label: "Expirada" },
];

export function AdminUsersPanel({ users, licenses, accounts }: { users: ManagedUser[]; licenses: ManagedLicense[]; accounts: ManagedAccount[] }) {
  const [openUserId, setOpenUserId] = useState<string | null>(null);

  return (
    <div className="glass-panel rounded-[32px] p-5">
      <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Usuarios</p>
      <div className="mt-5 grid gap-4">
        {users.length > 0 ? users.map((user) => {
          const userLicenses = licenses.filter((license) => license.user_id === user.id);
          return (
            <div key={user.id} className="rounded-[24px] border border-white/8 bg-white/4 p-4">
              <form>
                <input type="hidden" name="user_id" value={user.id} />
                <div className="grid gap-4 lg:grid-cols-[1fr_1fr_0.8fr_auto_auto] lg:items-end">
                  <Field label="Nome" name="nome" defaultValue={user.nome ?? ""} />
                  <Field label="Email" name="email" type="email" defaultValue={user.email ?? ""} />
                  <Field label="Telegram ID" name="telegram_id" defaultValue={user.telegram_id ?? ""} required={false} />
                  <button formAction={updateManagedUser} className="rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Salvar</button>
                  <button formAction={toggleUserAccess} formNoValidate name="next_state" value={String(!user.acesso_ativo)} className={`rounded-[18px] px-4 py-3 text-sm font-semibold ${user.acesso_ativo ? "bg-red-400/12 text-red-200" : "bg-lime-400/12 text-lime-200"}`}>{user.acesso_ativo ? "Bloquear" : "Liberar"}</button>
                </div>
              </form>

              <div className="mt-4 flex flex-wrap items-center justify-between gap-3">
                <p className="text-xs uppercase tracking-[0.22em] text-slate-500">Role {user.role} · {userLicenses.length} licenca(s)</p>
                <button type="button" onClick={() => setOpenUserId(user.id)} className="rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Gerenciar licencas</button>
              </div>

              {openUserId === user.id ? (
                <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/75 px-4 py-8">
                  <div className="glass-panel max-h-[90vh] w-full max-w-5xl overflow-y-auto rounded-[32px] p-6">
                    <div className="flex items-start justify-between gap-4">
                      <div>
                        <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Licencas do usuario</p>
                        <h3 className="mt-2 text-3xl font-semibold">{user.nome ?? user.email ?? "Usuario"}</h3>
                        <p className="mt-2 text-slate-400">Edite, renove ou cadastre novas contas MT5 para este usuario.</p>
                      </div>
                      <button type="button" onClick={() => setOpenUserId(null)} className="rounded-[18px] border border-white/10 bg-white/5 px-4 py-3 text-sm font-semibold text-slate-200">Fechar</button>
                    </div>

                    <form className="mt-6 rounded-[24px] border border-white/8 bg-white/4 p-5">
                      <input type="hidden" name="user_id" value={user.id} />
                      <p className="text-xl font-semibold">Nova licenca</p>
                      <div className="mt-4 grid gap-4 md:grid-cols-2">
                        <SelectField label="Plano" name="nome_plano" options={PLAN_OPTIONS.map((plan) => ({ value: plan, label: plan }))} defaultValue="Premium" />
                        <Field label="Numero da conta MT5" name="numero_conta" placeholder="12345678" />
                        <Field label="Servidor MT5" name="servidor" placeholder="FTMO-Demo" required={false} />
                        <Field label="Senha MT5" name="mt5_password" type="password" placeholder="Senha da conta" required={false} />
                        <Field label="Alavancagem" name="alavancagem" type="number" placeholder="100" required={false} />
                        <Field label="Valor da licenca (R$)" name="valor" type="number" placeholder="297" required={false} />
                        <Field label="Data de expiracao" name="data_expiracao" type="date" />
                        <SelectField label="Status" name="status" options={LICENSE_STATUS_OPTIONS.filter((item) => item.value !== "expirada")} defaultValue="ativa" />
                      </div>
                      <button formAction={saveLicense} className="mt-5 rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-5 py-3 text-sm font-semibold text-cyan-100">Salvar licenca</button>
                    </form>

                    <div className="mt-6 grid gap-4">
                      {userLicenses.length > 0 ? userLicenses.map((license) => {
                        const account = accounts.find((item) => item.id === license.conta_trading_id);
                        return (
                          <form key={license.id} className="rounded-[24px] border border-white/8 bg-white/4 p-5">
                            <input type="hidden" name="license_id" value={license.id} />
                            <input type="hidden" name="user_id" value={license.user_id} />
                            <div className="grid gap-4 md:grid-cols-2">
                              <SelectField label="Plano" name="nome_plano" options={PLAN_OPTIONS.map((plan) => ({ value: plan, label: plan }))} defaultValue={license.nome_plano} />
                              <Field label="Numero da conta MT5" name="numero_conta" defaultValue={account?.numero_conta ?? ""} />
                              <Field label="Servidor MT5" name="servidor" defaultValue={account?.servidor ?? ""} required={false} />
                              <Field label="Senha MT5" name="mt5_password" type="password" defaultValue={account?.mt5_password ?? ""} required={false} />
                              <Field label="Alavancagem" name="alavancagem" type="number" defaultValue={account?.alavancagem != null ? String(account.alavancagem) : ""} required={false} />
                              <Field label="Valor da licenca (R$)" name="valor" type="number" defaultValue={String(license.valor)} />
                              <Field label="Data de expiracao" name="data_expiracao" type="date" defaultValue={license.data_expiracao} />
                              <SelectField label="Status" name="status" options={LICENSE_STATUS_OPTIONS} defaultValue={license.status} />
                            </div>
                            <div className="mt-4 flex flex-wrap gap-3">
                              <button formAction={saveLicense} className="rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Salvar licenca</button>
                              <button formAction={renewLicense30Days} formNoValidate name="current_expiration" value={license.data_expiracao} className="rounded-[18px] border border-lime-400/30 bg-lime-400/10 px-4 py-3 text-sm font-semibold text-lime-100">Renovar +30 dias</button>
                              <button formAction={updateLicenseStatus} formNoValidate name="status" value={license.status === "bloqueada" ? "ativa" : "bloqueada"} className={`rounded-[18px] px-4 py-3 text-sm font-semibold ${license.status === "bloqueada" ? "bg-lime-400/12 text-lime-200" : "bg-red-400/12 text-red-200"}`}>{license.status === "bloqueada" ? "Reativar" : "Bloquear"}</button>
                            </div>
                          </form>
                        );
                      }) : <EmptyState text="Este usuario ainda nao possui licencas." />}
                    </div>
                  </div>
                </div>
              ) : null}
            </div>
          );
        }) : <EmptyState text="Nenhum usuario encontrado para os filtros atuais." />}
      </div>
    </div>
  );
}

function Field({ label, name, placeholder, type = "text", defaultValue, required = true }: { label: string; name: string; placeholder?: string; type?: string; defaultValue?: string; required?: boolean }) {
  return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><input name={name} type={type} placeholder={placeholder} defaultValue={defaultValue} required={required} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none placeholder:text-slate-500" /></label>;
}

function SelectField({ label, name, options, defaultValue, required = true }: { label: string; name: string; options: Array<{ value: string; label: string }>; defaultValue?: string; required?: boolean }) {
  return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><select name={name} defaultValue={defaultValue} required={required} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">{required ? <option value="">Selecione</option> : null}{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>;
}

function EmptyState({ text }: { text: string }) {
  return <div className="rounded-[24px] border border-white/8 bg-white/4 p-6 text-slate-400">{text}</div>;
}
</file>

<file path="src/components/dashboard/tradingview-chart.tsx">
"use client";

import { useEffect, useId, useRef, useState } from "react";

declare global {
  interface Window {
    TradingView?: {
      widget: new (config: Record<string, unknown>) => unknown;
    };
  }
}

const INTERVAL_MAP: Record<string, string> = {
  M1: "1",
  M5: "5",
  M15: "15",
  M30: "30",
  H1: "60",
  H4: "240",
  D1: "1D",
};

function resolveTradingViewSymbol(symbol: string) {
  const normalized = symbol.trim().toUpperCase();

  if (normalized.includes(":")) {
    return normalized;
  }

  if (["XAUUSD", "EURUSD", "GBPUSD", "USDJPY", "AUDUSD", "NZDUSD", "USDCAD", "USDCHF"].includes(normalized)) {
    return `OANDA:${normalized}`;
  }

  if (normalized.endsWith("USDT")) {
    return `BINANCE:${normalized}`;
  }

  return `OANDA:${normalized}`;
}

export function TradingViewChart({ initialSymbol = "XAUUSD", initialTimeframe = "M5" }: { initialSymbol?: string; initialTimeframe?: string }) {
  const id = useId().replace(/:/g, "");
  const containerId = `tv-chart-${id}`;
  const containerRef = useRef<HTMLDivElement | null>(null);
  const [isCompact, setIsCompact] = useState(false);

  useEffect(() => {
    const mediaQuery = window.matchMedia("(max-width: 767px)");
    const syncViewport = () => setIsCompact(mediaQuery.matches);

    syncViewport();
    mediaQuery.addEventListener("change", syncViewport);

    return () => {
      mediaQuery.removeEventListener("change", syncViewport);
    };
  }, []);

  useEffect(() => {
    const container = containerRef.current;

    const renderWidget = () => {
      if (!window.TradingView || !container) {
        return;
      }

      container.innerHTML = `<div id="${containerId}" style="height:100%;width:100%"></div>`;

      new window.TradingView.widget({
        autosize: true,
        symbol: resolveTradingViewSymbol(initialSymbol),
        interval: INTERVAL_MAP[initialTimeframe] ?? "5",
        timezone: "America/Sao_Paulo",
        theme: "dark",
        style: "1",
        locale: "br",
        enable_publishing: false,
        allow_symbol_change: !isCompact,
        withdateranges: !isCompact,
        hide_side_toolbar: isCompact,
        details: !isCompact,
        hotlist: false,
        calendar: false,
        save_image: false,
        studies: ["Volume@tv-basicstudies"],
        container_id: containerId,
      });
    };

    const existingScript = document.querySelector<HTMLScriptElement>("script[data-tradingview-widget=\"tvjs\"]");
    if (existingScript) {
      if ((window.TradingView as { widget?: unknown } | undefined)?.widget) {
        renderWidget();
      } else {
        existingScript.addEventListener("load", renderWidget, { once: true });
      }
      return;
    }

    const script = document.createElement("script");
    script.src = "https://s3.tradingview.com/tv.js";
    script.async = true;
    script.dataset.tradingviewWidget = "tvjs";
    script.onload = renderWidget;
    document.body.appendChild(script);

    return () => {
      if (container) {
        container.innerHTML = "";
      }
    };
  }, [containerId, initialSymbol, initialTimeframe, isCompact]);

  return <div ref={containerRef} className="mt-5 h-[360px] min-w-0 overflow-hidden rounded-[28px] border border-white/8 bg-slate-950/60 sm:h-[420px] xl:h-[520px]" />;
}
</file>

<file path="supabase/migrations/20260325_runtime_and_mt5_credentials.sql">
alter table public.contas_trading
  drop column if exists mt5_login,
  add column if not exists mt5_server text,
  add column if not exists mt5_password text,
  add column if not exists server_time timestamptz,
  add column if not exists mercado_snapshot jsonb,
  add column if not exists insight_atual text,
  add column if not exists ultima_sincronizacao timestamptz;

alter table public.configuracoes_sessao
  add column if not exists timeframe text not null default 'M5';

update public.configuracoes_sessao
set timeframe = 'M5'
where timeframe is null;


create index if not exists idx_contas_trading_numero_conta on public.contas_trading (numero_conta);
create index if not exists idx_licencas_conta_status_expiracao on public.licencas (conta_trading_id, status, data_expiracao);
create index if not exists idx_configuracoes_sessao_conta_ligado on public.configuracoes_sessao (conta_trading_id, sistema_ligado, atualizado_em desc);
create index if not exists idx_operacoes_conta_aberta_em on public.operacoes (conta_trading_id, aberta_em desc);
create index if not exists idx_operacoes_conta_status on public.operacoes (conta_trading_id, status);
</file>

<file path="src/components/dashboard/dashboard-realtime-fixed.tsx">
"use client";

import { useEffect, useEffectEvent, useState, useTransition } from "react";
import type { AppUser } from "@/lib/auth";
import {
  saveTradingSettings,
  submitBuyCommand,
  submitCloseCommand,
  submitPartialCloseCommand,
  submitSellCommand,
  toggleSystemState,
} from "@/app/dashboard/actions";
import { createClient } from "@/lib/supabase/client";
import { TradingViewChart } from "@/components/dashboard/tradingview-chart";
import type {
  DashboardAccount,
  DashboardCommandStatus,
  DashboardConfig,
  DashboardHistoryFilters,
  DashboardHistoryRow,
  DashboardInsightBundle,
  DashboardLicense,
  DashboardOpenOperation,
  DashboardStats,
} from "@/app/dashboard/page";

type SelectableAccount = DashboardAccount & { license: DashboardLicense };

type DashboardRealtimeProps = {
  profile: AppUser;
  accounts: SelectableAccount[];
  selectedAccountId: string;
  selectedLicense: DashboardLicense;
  account: DashboardAccount;
  config: DashboardConfig;
  stats: DashboardStats;
  history: DashboardHistoryRow[];
  historyFilters: DashboardHistoryFilters;
  insightBundle: DashboardInsightBundle;
  openOperation: DashboardOpenOperation | null;
  commandStatuses: DashboardCommandStatus[];
};

function formatAccountCurrency(value: number, account: DashboardAccount) {
  const currencyCode = account.moeda_codigo?.toUpperCase?.() || "USD";
  const locale = currencyCode === "BRL" ? "pt-BR" : currencyCode === "EUR" ? "de-DE" : "en-US";

  try {
    return new Intl.NumberFormat(locale, {
      style: "currency",
      currency: currencyCode,
      maximumFractionDigits: 2,
    }).format(value);
  } catch {
    return `${account.moeda_simbolo} ${value.toFixed(2)}`;
  }
}

function formatLicenseCurrency(value: number) {
  return new Intl.NumberFormat("pt-BR", {
    style: "currency",
    currency: "BRL",
    maximumFractionDigits: 2,
  }).format(value);
}

function formatDate(date: string) {
  return new Intl.DateTimeFormat("pt-BR").format(new Date(`${date}T00:00:00`));
}

function formatDateTime(date: string | null) {
  if (!date) return "Aguardando primeira leitura";

  return new Intl.DateTimeFormat("pt-BR", {
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: false,
  }).format(new Date(date));
}

function formatTimeRange(start: string, end: string) {
  return `${start.slice(0, 5)}-${end.slice(0, 5)}`;
}

function commandLabel(command: DashboardCommandStatus | null) {
  if (!command) return "Sem comandos recentes";
  if (command.status === "pending") return "Na fila";
  if (command.status === "processing") return "Executando na VPS";
  if (command.status === "executed") return "Executado";
  if (command.status === "failed") return "Falhou";
  return "Cancelado";
}

function commandTypeLabel(command: DashboardCommandStatus | null) {
  if (!command) return null;
  if (command.tipo === "open_buy") return "Compra";
  if (command.tipo === "open_sell") return "Venda";
  if (command.tipo === "partial_close_position") return "Parcial";
  return "Fechamento";
}

function commandTone(command: DashboardCommandStatus | null) {
  if (!command) return "text-slate-300";
  if (command.status === "executed") return "text-lime-300";
  if (command.status === "failed") return "text-red-300";
  return "text-amber-200";
}

export function DashboardRealtimeFixed({
  profile,
  accounts,
  selectedAccountId,
  selectedLicense,
  account,
  config,
  stats,
  history,
  historyFilters,
  insightBundle,
  openOperation,
  commandStatuses,
}: DashboardRealtimeProps) {
  const supabase = createClient();
  const [, startTransition] = useTransition();
  const [liveAccount, setLiveAccount] = useState(account);
  const [liveConfig, setLiveConfig] = useState(config);
  const [liveStats, setLiveStats] = useState(stats);
  const [liveHistory, setLiveHistory] = useState(history);
  const [liveNow, setLiveNow] = useState<Date | null>(null);
  const [hasMounted, setHasMounted] = useState(false);
  const [liveInsightBundle, setLiveInsightBundle] = useState(insightBundle);
  const [liveOpenOperation, setLiveOpenOperation] = useState(openOperation);
  const [liveCommandStatuses, setLiveCommandStatuses] = useState(commandStatuses);

  useEffect(() => {
    setLiveAccount(account);
    setLiveConfig(config);
    setLiveStats(stats);
    setLiveHistory(history);
    setLiveInsightBundle(insightBundle);
    setLiveOpenOperation(openOperation);
    setLiveCommandStatuses(commandStatuses);
  }, [account, commandStatuses, config, history, insightBundle, openOperation, stats]);

  const refreshSnapshot = useEffectEvent(async () => {
    const [
      { data: nextAccount },
      { data: nextConfig },
      { data: nextStats },
      { data: nextOperations },
      { data: nextCommands },
    ] = await Promise.all([
      supabase.from("contas_trading").select("id, user_id, nome_cliente, numero_conta, corretora, moeda_codigo, moeda_simbolo, saldo_atual, equity, margem_livre, nivel_margem, ativo, atualizado_em, server_time, mercado_snapshot, insight_atual, ultima_sincronizacao").eq("id", selectedAccountId).maybeSingle<DashboardAccount>(),
      supabase.from("configuracoes_sessao").select("id, conta_trading_id, ativo, timeframe, sistema_ligado, modo, breakeven_ativo, trailing_stop_ativo, horario_inicio, horario_fim, meta_lucro_diaria, perda_maxima_diaria, limite_operacoes_ativo, limite_operacoes_diaria").eq("conta_trading_id", selectedAccountId).order("atualizado_em", { ascending: false }).limit(1).maybeSingle<DashboardConfig>(),
      supabase.from("estatisticas").select("conta_trading_id, operacoes_total, vitorias, derrotas, win_rate, lucro_total, prejuizo_total, drawdown, melhor_operacao, pior_operacao").eq("conta_trading_id", selectedAccountId).order("periodo", { ascending: false }).limit(1).maybeSingle<DashboardStats>(),
      supabase.from("operacoes").select("id, direcao, status, lote, preco_entrada, preco_saida, stop_loss, take_profit, lucro_prejuizo, aberta_em, fechada_em, timeframe, ativo, validacao_ia").eq("conta_trading_id", selectedAccountId).order("aberta_em", { ascending: false }).limit(50),
      supabase.from("comandos_trading").select("id, tipo, status, erro, solicitado_em, processado_em").eq("conta_trading_id", selectedAccountId).order("solicitado_em", { ascending: false }).limit(5),
    ]);

    startTransition(() => {
      if (nextAccount) {
        setLiveAccount(nextAccount);
        const snapshotNotes = Array.isArray(nextAccount.mercado_snapshot?.notes) ? nextAccount.mercado_snapshot.notes : [];
        const snapshotCandles = Array.isArray(nextAccount.mercado_snapshot?.candles) ? nextAccount.mercado_snapshot.candles : [];
        setLiveInsightBundle((current) => ({
          summary: nextAccount.insight_atual ?? current.summary,
          notes: snapshotNotes.length > 0 ? snapshotNotes : current.notes,
          candles: snapshotCandles.length > 0 ? snapshotCandles : current.candles,
        }));
      }

      if (nextConfig) setLiveConfig(nextConfig);
      if (nextStats) setLiveStats(nextStats);

      if (nextOperations) {
        const nextOpenOperation = nextOperations.find((operation) => operation.status === "aberta");
        setLiveOpenOperation(
          nextOpenOperation
            ? {
                id: nextOpenOperation.id,
                ticket: typeof nextOpenOperation.validacao_ia?.ticket === "string" ? nextOpenOperation.validacao_ia.ticket : null,
                direction: nextOpenOperation.direcao === "compra" ? "buy" : "sell",
                lot: Number(nextOpenOperation.lote),
                entryPrice: Number(nextOpenOperation.preco_entrada),
                stopLoss: nextOpenOperation.stop_loss != null ? Number(nextOpenOperation.stop_loss) : null,
                takeProfit: nextOpenOperation.take_profit != null ? Number(nextOpenOperation.take_profit) : null,
                openedAt: nextOpenOperation.aberta_em,
                timeframe: nextOpenOperation.timeframe,
                symbol: nextOpenOperation.ativo,
                profitLoss: Number(nextOpenOperation.lucro_prejuizo ?? 0),
              }
            : null,
        );

        const nextRows: Array<DashboardHistoryRow | null> = nextOperations.map((operation) => {
          if (operation.status === "aberta") return null;
          const resultValue = Number(operation.lucro_prejuizo ?? 0);
          return {
            id: operation.id,
            time: new Intl.DateTimeFormat("pt-BR", { hour: "2-digit", minute: "2-digit", hour12: false }).format(new Date(operation.fechada_em ?? operation.aberta_em)),
            type: operation.direcao === "compra" ? "Compra" : "Venda",
            lot: Number(operation.lote).toFixed(2),
            entry: Number(operation.preco_entrada).toFixed(2),
            result: `${resultValue >= 0 ? "+" : "-"}${Math.abs(resultValue).toFixed(2)}`,
            resultTone: resultValue >= 0 ? "text-lime-400" : "text-red-400",
          };
        });

        setLiveHistory(nextRows.filter((row): row is DashboardHistoryRow => row !== null));

        const latestTelemetry = nextOperations.find((operation) => operation.validacao_ia);
        if (latestTelemetry?.validacao_ia) {
          setLiveInsightBundle((current) => ({
            summary: typeof latestTelemetry.validacao_ia.ai?.summary === "string" ? latestTelemetry.validacao_ia.ai.summary : current.summary,
            notes: Array.isArray(latestTelemetry.validacao_ia.market?.notes) ? latestTelemetry.validacao_ia.market.notes : current.notes,
            candles: Array.isArray(latestTelemetry.validacao_ia.market?.candles) ? latestTelemetry.validacao_ia.market.candles : current.candles,
          }));
        }
      }

      if (nextCommands) setLiveCommandStatuses(nextCommands as DashboardCommandStatus[]);
    });
  });

  useEffect(() => {
    setHasMounted(true);
    setLiveNow(new Date());

    const interval = window.setInterval(() => setLiveNow(new Date()), 1000);
    return () => window.clearInterval(interval);
  }, []);

  useEffect(() => {
    const channel = supabase
      .channel(`lumitrader-account-${selectedAccountId}`)
      .on("postgres_changes", { event: "*", schema: "public", table: "contas_trading", filter: `id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .on("postgres_changes", { event: "*", schema: "public", table: "configuracoes_sessao", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .on("postgres_changes", { event: "*", schema: "public", table: "estatisticas", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .on("postgres_changes", { event: "*", schema: "public", table: "operacoes", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .on("postgres_changes", { event: "*", schema: "public", table: "licencas", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .on("postgres_changes", { event: "*", schema: "public", table: "comandos_trading", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => { void refreshSnapshot(); })
      .subscribe();

    return () => {
      void supabase.removeChannel(channel);
    };
  }, [refreshSnapshot, selectedAccountId, supabase]);

  const floatingProfit = (liveAccount.equity ?? 0) - (liveAccount.saldo_atual ?? 0);
  const isSystemOnline = liveConfig.sistema_ligado && Boolean(liveAccount.ativo);
  const latestCommand = liveCommandStatuses[0] ?? null;
  const latestCommandType = commandTypeLabel(latestCommand);
  const latestCommandStatus = commandLabel(latestCommand);
  const latestCommandTone = commandTone(latestCommand);
  const brasiliaClockLabel = hasMounted && liveNow
    ? new Intl.DateTimeFormat("pt-BR", {
        day: "2-digit",
        month: "2-digit",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
        second: "2-digit",
        hour12: false,
        timeZone: "America/Sao_Paulo",
      }).format(liveNow)
    : "Sincronizando relogio...";

  const metrics = [
    { label: "Lucro do Dia", value: formatAccountCurrency(Math.max(liveStats.lucro_total, 0), liveAccount), tone: "text-lime-400" },
    { label: "Perda do Dia", value: `-${formatAccountCurrency(Math.abs(liveStats.prejuizo_total), liveAccount)}`, tone: "text-red-400" },
    { label: "Win Rate", value: `${liveStats.win_rate}%`, tone: "text-white" },
    { label: "Drawdown", value: `${liveStats.drawdown}%`, tone: "text-white" },
  ];

  const marketSnapshot = (liveAccount.mercado_snapshot ?? {}) as {
    notes?: string[];
    candles?: DashboardInsightBundle["candles"];
    trend?: string | null;
    rsi?: number | null;
    moving_average_20?: number | null;
    support?: number | null;
    resistance?: number | null;
  };

  return (
    <div className="mt-5 grid gap-5">
      <div className="grid gap-3 xl:grid-cols-[1.1fr_1fr_1fr_1fr_0.9fr]">
        <HeaderPill label="Ativo" value={liveConfig.ativo} />
        <HeaderPill label="Modo" value={liveConfig.modo === "agressivo" ? "Agressivo" : "Conservador"} />
        <HeaderPill label="Conta" value={liveAccount.numero_conta} />
        <HeaderPill label="Licenca" value={selectedLicense.nome_plano} />
        <HeaderPill label="Status" value={isSystemOnline ? "ONLINE" : "PAUSADO"} online={isSystemOnline} />
      </div>

      <section className="rounded-[28px] border border-white/8 bg-white/4 p-4 sm:p-5">
        <div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between">
          <div className="min-w-0">
            <p className="font-mono text-xs uppercase tracking-[0.26em] text-cyan-200/70">Seletor de licenca</p>
            <p className="mt-2 max-w-4xl text-sm text-slate-300">Cada conta MT5 opera de forma independente com limites, saldo, equity, execucao manual e automacao dedicados.</p>
          </div>
          <form action="/dashboard" className="min-w-0 xl:w-[34rem]">
            <select
              name="account"
              defaultValue={selectedAccountId}
              onChange={(event) => event.currentTarget.form?.requestSubmit()}
              className="w-full rounded-[18px] border border-white/10 bg-slate-950/60 px-4 py-3 text-white outline-none"
            >
              {accounts.map((item) => (
                <option key={item.id} value={item.id}>
                  {`${item.numero_conta} - ${item.nome_cliente} - expira ${formatDate(item.license.data_expiracao)}`}
                </option>
              ))}
            </select>
          </form>
        </div>
      </section>

      <div className="grid items-start gap-5 2xl:grid-cols-[280px_minmax(0,1fr)_320px]">
        <section className="glass-panel rounded-[28px] p-4 sm:p-5">
          <PanelEyebrow>Controle Central</PanelEyebrow>
          <h2 className="mt-2 text-[1.8rem] font-semibold leading-tight">{isSystemOnline ? "Sistema armado" : "Sistema em pausa"}</h2>
          <form key={`toggle-${selectedAccountId}`} className="mt-4">
            <input type="hidden" name="config_id" value={liveConfig.id ?? ""} />
            <input type="hidden" name="conta_trading_id" value={selectedAccountId} />
            <input type="hidden" name="current_state" value={String(liveConfig.sistema_ligado)} />
            <input type="hidden" name="ativo" value={liveConfig.ativo} />
            <input type="hidden" name="timeframe" value={liveConfig.timeframe} />
            <button formAction={toggleSystemState} className="flex w-full items-center justify-center gap-3 rounded-[18px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 text-lg font-semibold text-slate-950 shadow-[0_20px_50px_rgba(157,232,51,0.22)] transition-transform duration-200 hover:-translate-y-0.5">
              <span>{liveConfig.sistema_ligado ? "||" : ">"}</span>
              {liveConfig.sistema_ligado ? "Pausar automacao" : "Armar automacao"}
            </button>
          </form>

          <div className="mt-4 rounded-[20px] border border-lime-400/20 bg-lime-400/8 p-4 text-sm text-lime-200/90">
            <div className="grid grid-cols-2 gap-3">
              <div>
                <p className="text-[11px] uppercase tracking-[0.22em] text-lime-200/70">Servidor</p>
                <p className="mt-2 break-words">{formatDateTime(liveAccount.server_time)}</p>
              </div>
              <div>
                <p className="text-[11px] uppercase tracking-[0.22em] text-lime-200/70">Brasilia</p>
                <p className="mt-2 break-words">{brasiliaClockLabel}</p>
              </div>
            </div>
          </div>

          <div className="mt-4 grid gap-3 sm:grid-cols-2">
            <ToggleCard label="Breakeven" value={liveConfig.breakeven_ativo ? "ON" : "OFF"} />
            <ToggleCard label="Trailing Stop" value={liveConfig.trailing_stop_ativo ? "ON" : "OFF"} />
          </div>

          <div className="mt-4 rounded-[22px] border border-white/8 bg-slate-950/35 p-4">
            <p className="text-sm text-slate-300">Canal operacional MT5</p>
            <p className={`mt-2 text-lg font-semibold ${latestCommandTone}`}>{latestCommandStatus}</p>
            <p className="mt-2 text-sm text-slate-400">Ultima leitura: {formatDateTime(liveAccount.ultima_sincronizacao)}</p>
            {latestCommand && latestCommandType ? <p className="mt-2 text-sm text-slate-300">Ultimo comando: {latestCommandType} em {formatDateTime(latestCommand.solicitado_em)}</p> : null}
            {latestCommand?.erro ? <p className="mt-3 rounded-[16px] border border-red-400/20 bg-red-400/10 px-3 py-2 text-sm text-red-200">{latestCommand.erro}</p> : null}
          </div>
        </section>

        <section className="glass-panel overflow-hidden rounded-[28px] p-4 sm:p-5">
          <div className="flex flex-wrap items-center justify-between gap-3">
            <div className="min-w-0">
              <PanelEyebrow>Grafico em Tempo Real</PanelEyebrow>
              <h2 className="mt-2 break-words text-3xl font-semibold">Painel tecnico {liveConfig.ativo}</h2>
            </div>
            <div className="flex items-center gap-2">
              <span className="rounded-full border border-cyan-400/20 bg-cyan-400/10 px-3 py-1 text-xs uppercase tracking-[0.22em] text-cyan-200">{liveConfig.timeframe}</span>
              <span className="rounded-full bg-white/6 px-3 py-1 text-sm text-slate-200">{liveAccount.numero_conta}</span>
            </div>
          </div>
          <TradingViewChart initialSymbol={liveConfig.ativo} initialTimeframe={liveConfig.timeframe} />
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Conta de Trading</PanelEyebrow>
          <div className="mt-3 flex flex-wrap items-start justify-between gap-3">
            <h2 className="min-w-0 flex-1 break-words text-[1.9rem] font-semibold leading-tight">{liveAccount.corretora ?? "Corretora nao informada"}</h2>
            <span className="rounded-full bg-cyan-400/10 px-3 py-1 text-sm text-cyan-100">{liveAccount.numero_conta}</span>
          </div>
          <div className="mt-4 space-y-3 rounded-[22px] border border-white/8 bg-slate-950/40 p-4">
            <DataRow label="Cliente" value={liveAccount.nome_cliente} />
            <DataRow label="Licenca" value={`${selectedLicense.nome_plano} ate ${formatDate(selectedLicense.data_expiracao)}`} />
            <DataRow label="Moeda" value={`${liveAccount.moeda_simbolo} / ${liveAccount.moeda_codigo}`} />
            <DataRow label="Saldo atual" value={formatAccountCurrency(liveAccount.saldo_atual ?? 0, liveAccount)} />
            <DataRow label="Equity" value={formatAccountCurrency(liveAccount.equity ?? 0, liveAccount)} />
            <DataRow label="Margem livre" value={formatAccountCurrency(liveAccount.margem_livre ?? 0, liveAccount)} />
          </div>
          <div className="mt-4 rounded-[22px] border border-lime-400/20 bg-lime-400/8 p-4">
            <p className="text-sm text-lime-200/80">Resultado flutuante</p>
            <p className={`mt-3 break-words text-4xl font-bold ${floatingProfit >= 0 ? "text-lime-300" : "text-red-300"}`}>{floatingProfit >= 0 ? "+" : "-"}{formatAccountCurrency(Math.abs(floatingProfit), liveAccount)}</p>
            <p className="mt-2 text-sm text-slate-300">Nivel de margem: {liveAccount.nivel_margem ?? 0}%.</p>
          </div>
        </section>
      </div>

      <div className="grid gap-5 2xl:grid-cols-[minmax(0,1.05fr)_420px]">
        <section className="glass-panel rounded-[28px] p-5">
          <div className="flex flex-wrap items-start justify-between gap-3">
            <div className="min-w-0">
              <PanelEyebrow>Operacao Atual</PanelEyebrow>
              <h2 className="mt-2 break-words text-2xl font-semibold leading-tight">{liveOpenOperation ? `${liveOpenOperation.direction === "buy" ? "Compra" : "Venda"} ${liveOpenOperation.symbol}` : "Nenhuma operacao aberta"}</h2>
            </div>
            {liveOpenOperation ? (
              <span className={`rounded-full px-3 py-1 text-sm font-semibold ${floatingProfit >= 0 ? "bg-lime-400/12 text-lime-300" : "bg-red-400/12 text-red-300"}`}>{floatingProfit >= 0 ? "No lucro" : "No prejuizo"}</span>
            ) : null}
          </div>

          {liveOpenOperation ? (
            <>
              <div className="mt-4 grid gap-3 xl:grid-cols-3">
                <CompactMetric label="Direcao" value={liveOpenOperation.direction === "buy" ? "Compra" : "Venda"} tone={liveOpenOperation.direction === "buy" ? "text-lime-300" : "text-red-300"} />
                <CompactMetric label="Entrada" value={formatAccountCurrency(liveOpenOperation.entryPrice, liveAccount)} />
                <CompactMetric label="Lote" value={liveOpenOperation.lot.toFixed(2)} />
                <CompactMetric label="P/L em tempo real" value={`${floatingProfit >= 0 ? "+" : "-"}${formatAccountCurrency(Math.abs(floatingProfit), liveAccount)}`} tone={floatingProfit >= 0 ? "text-lime-400" : "text-red-400"} />
                <CompactMetric label="Stop Loss" value={liveOpenOperation.stopLoss != null ? formatAccountCurrency(liveOpenOperation.stopLoss, liveAccount) : "--"} tone="text-red-300" />
                <CompactMetric label="Take Profit" value={liveOpenOperation.takeProfit != null ? formatAccountCurrency(liveOpenOperation.takeProfit, liveAccount) : "--"} tone="text-lime-300" />
              </div>
              <div className="mt-4 rounded-[18px] border border-white/8 bg-white/4 px-4 py-3 text-sm text-slate-300">{liveInsightBundle.notes[0] ?? `Sync do mercado para ${liveConfig.ativo} em ${liveConfig.timeframe}.`}</div>
            </>
          ) : (
            <div className="mt-5 rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-400">Assim que houver uma posicao aberta nesta conta, o painel mostra direcao, lote, entrada, P/L e PnL em tempo real.</div>
          )}
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Execucao Manual</PanelEyebrow>
          <form key={`manual-${selectedAccountId}-${liveOpenOperation?.id ?? "idle"}`} className="mt-4 grid gap-4">
            <input type="hidden" name="conta_trading_id" value={selectedAccountId} />
            <input type="hidden" name="ativo" value={liveConfig.ativo} />
            <input type="hidden" name="timeframe" value={liveConfig.timeframe} />
            <input type="hidden" name="ticket_referencia" value={liveOpenOperation?.ticket ?? ""} />
            <div className="grid gap-3 md:grid-cols-3">
              <SettingsField label="Lote" name="lote" type="text" inputMode="decimal" defaultValue={liveOpenOperation ? liveOpenOperation.lot.toFixed(2).replace(".", ",") : "0,10"} />
              <SettingsField label="Stop Loss" name="stop_loss" type="text" inputMode="decimal" defaultValue={liveOpenOperation?.stopLoss != null ? String(liveOpenOperation.stopLoss).replace(".", ",") : ""} />
              <SettingsField label="Take Profit" name="take_profit" type="text" inputMode="decimal" defaultValue={liveOpenOperation?.takeProfit != null ? String(liveOpenOperation.takeProfit).replace(".", ",") : ""} />
            </div>
            <div className="grid gap-3 sm:grid-cols-2">
              <button formAction={submitBuyCommand} className="w-full rounded-[16px] bg-lime-500/80 px-4 py-3 text-sm font-semibold text-white transition-colors hover:bg-lime-500">Comprar</button>
              <button formAction={submitSellCommand} className="w-full rounded-[16px] bg-red-500/80 px-4 py-3 text-sm font-semibold text-white transition-colors hover:bg-red-500">Vender</button>
            </div>
            <div className="grid gap-3 sm:grid-cols-2">
              <button formAction={submitPartialCloseCommand} disabled={!liveOpenOperation} className="w-full rounded-[16px] border border-white/10 bg-white/6 px-4 py-3 text-sm font-semibold text-slate-100 transition-colors hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-50">Parcial 50%</button>
              <button formAction={submitCloseCommand} disabled={!liveOpenOperation} className="w-full rounded-[16px] border border-white/10 bg-white/6 px-4 py-3 text-sm font-semibold text-slate-100 transition-colors hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-50">Fechar posicao</button>
            </div>
          </form>
          <div className="mt-4 rounded-[18px] border border-white/8 bg-white/4 px-4 py-3 text-sm text-slate-300">O envio manual aceita lote com virgula ou ponto. Os botoes de parcial e fechamento atuam sobre a posicao aberta da conta selecionada.</div>
        </section>
      </div>

      <div className="grid gap-5 2xl:grid-cols-[0.95fr_0.95fr_1.15fr_320px]">
        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Estatisticas da Conta</PanelEyebrow>
          <div className="mt-5 space-y-3">
            <DataRow label="Operacoes" value={String(liveStats.operacoes_total)} />
            <DataRow label="Vitorias" value={String(liveStats.vitorias)} />
            <DataRow label="Derrotas" value={String(liveStats.derrotas)} />
            <DataRow label="Maior ganho" value={formatAccountCurrency(liveStats.melhor_operacao, liveAccount)} />
            <DataRow label="Maior perda" value={formatAccountCurrency(liveStats.pior_operacao, liveAccount)} />
          </div>
          <div className="mt-5 grid gap-3 sm:grid-cols-2">
            {metrics.map((item) => <MetricCard key={item.label} label={item.label} value={item.value} tone={item.tone} />)}
          </div>
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Monitor da Conta</PanelEyebrow>
          <div className="mt-5 space-y-3 rounded-[24px] border border-white/8 bg-slate-950/35 p-4">
            <DataRow label="Ativo" value={liveConfig.ativo} />
            <DataRow label="Timeframe" value={liveConfig.timeframe} />
            <DataRow label="Modo" value={liveConfig.modo === "agressivo" ? "Agressivo" : "Conservador"} />
            <DataRow label="Janela operacional" value={formatTimeRange(liveConfig.horario_inicio, liveConfig.horario_fim)} />
            <DataRow label="Meta diaria" value={formatAccountCurrency(liveConfig.meta_lucro_diaria, liveAccount)} />
            <DataRow label="Perda maxima" value={formatAccountCurrency(liveConfig.perda_maxima_diaria, liveAccount)} />
            <DataRow label="Lim. operacoes" value={liveConfig.limite_operacoes_ativo ? String(liveConfig.limite_operacoes_diaria ?? 0) : "Desativado"} />
          </div>
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Parametros desta Conta</PanelEyebrow>
          <form key={`settings-${selectedAccountId}`} className="mt-4 grid gap-4 md:grid-cols-2">
            <input type="hidden" name="config_id" value={liveConfig.id ?? ""} />
            <input type="hidden" name="conta_trading_id" value={selectedAccountId} />
            <input type="hidden" name="current_system_state" value={String(liveConfig.sistema_ligado)} />
            <SettingsField label="Ativo" name="ativo" defaultValue={liveConfig.ativo} />
            <label className="grid gap-2">
              <span className="text-sm text-slate-300">Timeframe</span>
              <select name="timeframe" defaultValue={liveConfig.timeframe} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">
                {"M1,M5,M15,M30,H1".split(",").map((timeframe) => <option key={timeframe} value={timeframe}>{timeframe}</option>)}
              </select>
            </label>
            <label className="grid gap-2">
              <span className="text-sm text-slate-300">Modo</span>
              <select name="modo" defaultValue={liveConfig.modo} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">
                <option value="agressivo">Agressivo</option>
                <option value="conservador">Conservador</option>
              </select>
            </label>
            <SettingsField label="Inicio" name="horario_inicio" type="time" defaultValue={liveConfig.horario_inicio.slice(0, 5)} />
            <SettingsField label="Fim" name="horario_fim" type="time" defaultValue={liveConfig.horario_fim.slice(0, 5)} />
            <SettingsField label="Meta diaria" name="meta_lucro_diaria" type="text" inputMode="decimal" defaultValue={String(liveConfig.meta_lucro_diaria)} />
            <SettingsField label="Perda maxima diaria" name="perda_maxima_diaria" type="text" inputMode="decimal" defaultValue={String(liveConfig.perda_maxima_diaria)} />
            <SettingsField label="Limite de operacoes" name="limite_operacoes_diaria" type="text" inputMode="numeric" defaultValue={String(liveConfig.limite_operacoes_diaria ?? "")} />
            <div className="grid gap-3 rounded-[18px] border border-white/8 bg-slate-950/35 p-4 md:col-span-2">
              <CheckBox label="Breakeven ativo" name="breakeven_ativo" defaultChecked={liveConfig.breakeven_ativo} />
              <CheckBox label="Trailing stop ativo" name="trailing_stop_ativo" defaultChecked={liveConfig.trailing_stop_ativo} />
              <CheckBox label="Limite de operacoes ativo" name="limite_operacoes_ativo" defaultChecked={liveConfig.limite_operacoes_ativo} />
            </div>
            <div className="flex md:col-span-2 md:justify-end">
              <button formAction={saveTradingSettings} className="w-full rounded-[18px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 text-sm font-semibold text-slate-950 md:w-auto">Salvar parametros</button>
            </div>
          </form>
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Timeline Operacional</PanelEyebrow>
          <div className="mt-3 text-sm text-slate-400">Historico recente com filtros da conta selecionada.</div>
          <form action="/dashboard" className="mt-5 grid gap-3 rounded-[24px] border border-white/8 bg-white/4 p-4">
            <input type="hidden" name="account" value={selectedAccountId} />
            <SettingsField label="De" name="from" type="date" defaultValue={historyFilters.from} />
            <SettingsField label="Ate" name="to" type="date" defaultValue={historyFilters.to} />
            <label className="grid gap-2">
              <span className="text-sm text-slate-300">Tipo</span>
              <select name="type" defaultValue={historyFilters.type} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">
                <option value="all">Todos</option>
                <option value="compra">Compra</option>
                <option value="venda">Venda</option>
              </select>
            </label>
            <label className="grid gap-2">
              <span className="text-sm text-slate-300">Resultado</span>
              <select name="result" defaultValue={historyFilters.result} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">
                <option value="all">Todos</option>
                <option value="gain">Ganho</option>
                <option value="loss">Perda</option>
              </select>
            </label>
            <button className="w-full rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Filtrar</button>
          </form>
          <div className="mt-5 overflow-hidden rounded-[24px] border border-white/8 bg-slate-950/30">
            <div className="grid grid-cols-[72px_72px_72px_1fr] gap-3 border-b border-white/8 px-4 py-4 text-xs uppercase tracking-[0.22em] text-slate-400">
              <span>Hora</span>
              <span>Tipo</span>
              <span>Lote</span>
              <span>Resultado</span>
            </div>
            <div className="max-h-[24rem] overflow-y-auto">
              {liveHistory.length > 0 ? liveHistory.map((row) => (
                <div key={row.id} className="grid grid-cols-[72px_72px_72px_1fr] gap-3 border-b border-white/6 px-4 py-4 text-sm last:border-b-0 even:bg-white/3">
                  <span className="text-slate-300">{row.time}</span>
                  <span>{row.type}</span>
                  <span>{row.lot}</span>
                  <span className={`font-semibold ${row.resultTone}`}>{liveAccount.moeda_simbolo} {row.result}</span>
                </div>
              )) : <div className="px-4 py-10 text-center text-slate-400">Nenhuma operacao encontrada para os filtros selecionados.</div>}
            </div>
          </div>
        </section>
      </div>

      <div className="grid gap-5 2xl:grid-cols-[1.35fr_1fr]">
        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Insights Operacionais</PanelEyebrow>
          <div className="mt-5 grid gap-3">
            <div className="rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-100">{liveInsightBundle.summary ?? "O monitoramento de mercado ja esta sincronizando candles e notas operacionais. O resumo com IA so aparece depois de uma abertura ou fechamento real registrado no backend."}</div>
            {liveInsightBundle.notes.length > 0 ? (
              <div className="grid gap-3 xl:grid-cols-3">
                {liveInsightBundle.notes.slice(0, 3).map((insight) => (
                  <div key={insight} className="rounded-[20px] border border-white/8 bg-white/4 p-4 text-sm text-slate-200">{insight}</div>
                ))}
              </div>
            ) : (
              <div className="rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-400">Aguardando novas observacoes do monitoramento desta conta.</div>
            )}
          </div>
        </section>

        <section className="glass-panel rounded-[28px] p-5">
          <PanelEyebrow>Snapshot Tecnico</PanelEyebrow>
          <div className="mt-5 grid gap-3 sm:grid-cols-2">
            <MetricCard label="Tendencia" value={String(marketSnapshot.trend ?? "range")} tone="text-white" />
            <MetricCard label="RSI 14" value={marketSnapshot.rsi != null ? String(marketSnapshot.rsi) : "--"} tone="text-cyan-200" />
            <MetricCard label="MM20" value={marketSnapshot.moving_average_20 != null ? String(marketSnapshot.moving_average_20) : "--"} tone="text-white" />
            <MetricCard label="Suporte / Resistencia" value={`${marketSnapshot.support ?? "--"} / ${marketSnapshot.resistance ?? "--"}`} tone="text-white" />
          </div>
        </section>
      </div>
    </div>
  );
}

function PanelEyebrow({ children }: { children: string }) {
  return <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">{children}</p>;
}

function HeaderPill({ label, value, online = false }: { label: string; value: string; online?: boolean }) {
  return <div className="rounded-[24px] border border-white/8 bg-white/4 px-4 py-3"><p className="text-xs uppercase tracking-[0.22em] text-slate-400">{label}</p><div className="mt-2 flex items-center gap-2 text-lg font-semibold">{online ? <span className="h-2.5 w-2.5 rounded-full bg-lime-400 shadow-[0_0_18px_rgba(157,232,51,0.9)]" /> : null}<span className="break-words">{value}</span></div></div>;
}

function InfoCard({ eyebrow, title, detail }: { eyebrow: string; title: string; detail: string }) {
  return <div className="rounded-[24px] border border-white/8 bg-white/4 p-4"><p className="font-mono text-xs uppercase tracking-[0.26em] text-cyan-200/70">{eyebrow}</p><p className="mt-3 break-words text-xl font-semibold">{title}</p><p className="mt-2 break-words text-sm text-slate-400">{detail}</p></div>;
}

function ToggleCard({ label, value }: { label: string; value: string }) {
  return <div className="flex items-center justify-between gap-3 rounded-[24px] border border-white/8 bg-slate-950/40 px-4 py-3"><span className="text-base sm:text-lg">{label}</span><span className="rounded-full bg-lime-400/12 px-3 py-1 text-sm font-semibold text-lime-300">{value}</span></div>;
}

function QuickFact({ label, value }: { label: string; value: string }) {
  return <div className="min-w-0"><p className="text-xs uppercase tracking-[0.2em] text-slate-500">{label}</p><p className="mt-2 break-words text-lg font-semibold">{value}</p></div>;
}

function DataRow({ label, value }: { label: string; value: string }) {
  return <div className="flex items-center justify-between gap-4 border-b border-white/6 pb-3 last:border-b-0 last:pb-0"><span className="text-slate-400">{label}</span><span className="max-w-[60%] break-words text-right font-semibold">{value}</span></div>;
}

function StatRow({ label, value, valueTone = "text-white" }: { label: string; value: string; valueTone?: string }) {
  return <div className="flex items-center justify-between gap-4 rounded-[24px] border border-white/8 bg-white/4 px-4 py-4"><span className="text-slate-300">{label}</span><span className={`text-right text-xl font-semibold ${valueTone}`}>{value}</span></div>;
}

function CompactMetric({ label, value, tone = "text-white" }: { label: string; value: string; tone?: string }) {
  return <div className="rounded-[20px] border border-white/8 bg-white/4 px-4 py-4"><p className="text-sm text-slate-400">{label}</p><p className={`mt-3 break-words text-2xl font-semibold ${tone}`}>{value}</p></div>;
}

function MetricCard({ label, value, tone }: { label: string; value: string; tone: string }) {
  return <div className="glass-panel rounded-[24px] px-5 py-4"><p className="text-sm text-slate-300">{label}</p><p className={`mt-4 break-words text-3xl font-bold ${tone}`}>{value}</p></div>;
}

function SettingsField({ label, name, defaultValue, type = "text", inputMode }: { label: string; name: string; defaultValue: string; type?: string; inputMode?: "none" | "text" | "tel" | "url" | "email" | "numeric" | "decimal" | "search" }) {
  return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><input name={name} type={type} inputMode={inputMode} defaultValue={defaultValue} className="min-w-0 rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none" /></label>;
}

function CheckBox({ label, name, defaultChecked }: { label: string; name: string; defaultChecked: boolean }) {
  return <label className="flex items-center gap-3 text-sm text-slate-200"><input type="checkbox" name={name} defaultChecked={defaultChecked} className="h-4 w-4 rounded border-white/20 bg-slate-950/60" />{label}</label>;
}
</file>

<file path="src/lib/backend/types.ts">
export type MarketCandle = {
  time?: string;
  open: number;
  close: number;
  high: number;
  low: number;
};

export type TradingEventPayload = {
  event: "account_sync" | "operation_opened" | "operation_closed";
  account: {
    number: string;
    broker?: string;
    server?: string;
    server_time?: string;
    name?: string;
    currency_code?: string;
    currency_symbol?: string;
    balance?: number;
    equity?: number;
    margin?: number;
    free_margin?: number;
    margin_level?: number;
    leverage?: number;
  };
  operation?: {
    ticket?: string;
    symbol: string;
    timeframe: string;
    side: "buy" | "sell";
    lot: number;
    entry_price: number;
    exit_price?: number;
    stop_loss?: number;
    take_profit?: number;
    spread?: number;
    volume?: number;
    volatility?: number;
    profit_loss?: number;
    opened_at: string;
    closed_at?: string;
    close_reason?: string;
  };
  session?: {
    mode?: "agressivo" | "conservador";
    breakeven_enabled?: boolean;
    trailing_stop_enabled?: boolean;
    profit_target?: number;
    daily_loss_limit?: number;
    operation_limit_enabled?: boolean;
    operation_limit?: number;
  };
  market?: {
    trend?: string;
    rsi?: number;
    moving_average_20?: number;
    support?: number;
    resistance?: number;
    last_bid?: number;
    last_ask?: number;
    notes?: string[];
    candles?: MarketCandle[];
  };
};

export type RiskSnapshot = {
  riskAmount: number;
  rewardAmount: number | null;
  riskRewardRatio: number | null;
  floatingDifference: number;
  spreadCostEstimate: number | null;
  remainingDailyLoss: number | null;
  remainingOperationsToday: number | null;
};

export type ReportPayload = {
  event: Exclude<TradingEventPayload["event"], "account_sync">;
  generatedAt: string;
  user: {
    id: string;
    nome: string | null;
    email: string | null;
    telegram_id: string | null;
  };
  account: {
    id: string;
    number: string;
    broker: string | null;
    currency_code: string;
    currency_symbol: string;
    balance: number;
    equity: number;
    active_license: {
      id: string;
      plan: string;
      expires_at: string;
      status: string;
      value: number;
    };
  };
  operation: NonNullable<TradingEventPayload["operation"]>;
  risk: RiskSnapshot;
  ai: {
    summary: string;
    request_id: string | null;
    model: string;
  };
  formats: {
    json: string;
    csv: string;
    html: string;
  };
};
</file>

<file path="python/mt5_reporter.py">
import json
import os
import time
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, List

import MetaTrader5 as mt5
import requests
from dotenv import dotenv_values

ENV_PATH = Path(__file__).with_name(".env")


def load_env_file() -> None:
    values = dotenv_values(ENV_PATH, encoding="utf-8-sig")
    for key, value in values.items():
        if key and value is not None:
            os.environ[key] = value


load_env_file()

EVENTS_URL = os.getenv("LUMITRADER_BACKEND_URL", "http://localhost:3000/api/backend/trading/events")
BRIDGE_ACCOUNTS_URL = EVENTS_URL.replace("/trading/events", "/bridge/accounts")
BRIDGE_COMMANDS_URL = EVENTS_URL.replace("/trading/events", "/bridge/commands")
INGEST_TOKEN = os.getenv("LUMITRADER_INGEST_TOKEN", "")
POLL_INTERVAL_SECONDS = int(os.getenv("POLL_INTERVAL_SECONDS", "3"))
STATE_PATH = Path(__file__).with_name("state.json")

TIMEFRAME_MAP = {
    "M1": mt5.TIMEFRAME_M1,
    "M5": mt5.TIMEFRAME_M5,
    "M15": mt5.TIMEFRAME_M15,
    "M30": mt5.TIMEFRAME_M30,
    "H1": mt5.TIMEFRAME_H1,
}


@dataclass
class PositionSnapshot:
    ticket: int
    symbol: str
    side: str
    lot: float
    entry_price: float
    stop_loss: float
    take_profit: float
    profit_loss: float
    opened_at: str


def ensure_symbol(symbol: str) -> None:
    if not mt5.symbol_select(symbol, True):
        raise RuntimeError(f"Nao foi possivel selecionar o simbolo {symbol}: {mt5.last_error()}")


def resolve_filling_mode(symbol: str) -> int:
    info = mt5.symbol_info(symbol)
    if info is None:
        return mt5.ORDER_FILLING_IOC

    filling_mode = getattr(info, "filling_mode", None)
    allowed = {
        mt5.ORDER_FILLING_FOK,
        mt5.ORDER_FILLING_IOC,
        getattr(mt5, "ORDER_FILLING_RETURN", mt5.ORDER_FILLING_IOC),
    }
    if filling_mode in allowed:
        return filling_mode
    return mt5.ORDER_FILLING_IOC


def utc_iso(timestamp: int) -> str:
    return datetime.fromtimestamp(timestamp, tz=timezone.utc).isoformat().replace("+00:00", "Z")


def load_state() -> Dict[str, Dict[str, Any]]:
    if not STATE_PATH.exists():
        return {"accounts": {}}
    return json.loads(STATE_PATH.read_text(encoding="utf-8"))


def save_state(state: Dict[str, Dict[str, Any]]) -> None:
    STATE_PATH.write_text(json.dumps(state, indent=2), encoding="utf-8")


def fetch_active_accounts() -> List[Dict[str, Any]]:
    response = requests.get(
        BRIDGE_ACCOUNTS_URL,
        headers={"Authorization": f"Bearer {INGEST_TOKEN}"},
        timeout=30,
    )
    response.raise_for_status()
    data = response.json()
    return data.get("accounts", [])




def fetch_pending_commands(account_number: str) -> List[Dict[str, Any]]:
    response = requests.get(
        BRIDGE_COMMANDS_URL,
        headers={"Authorization": f"Bearer {INGEST_TOKEN}"},
        params={"account": account_number},
        timeout=30,
    )
    response.raise_for_status()
    data = response.json()
    return data.get("commands", [])


def acknowledge_command(command_id: str, status: str, result: Dict[str, Any] | None = None, error: str | None = None) -> None:
    response = requests.post(
        BRIDGE_COMMANDS_URL,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {INGEST_TOKEN}",
        },
        json={
            "commandId": command_id,
            "status": status,
            "result": result or {},
            "error": error,
        },
        timeout=30,
    )
    response.raise_for_status()


def execute_command(command: Dict[str, Any]) -> None:
    symbol = command["symbol"]
    command_type = command["type"]
    ensure_symbol(symbol)
    filling_mode = resolve_filling_mode(symbol)

    if command_type in {"open_buy", "open_sell"}:
        tick = mt5.symbol_info_tick(symbol)
        if not tick:
            raise RuntimeError(f"Tick indisponivel para {symbol}")

        order_type = mt5.ORDER_TYPE_BUY if command_type == "open_buy" else mt5.ORDER_TYPE_SELL
        price = tick.ask if command_type == "open_buy" else tick.bid
        request = {
            "action": mt5.TRADE_ACTION_DEAL,
            "symbol": symbol,
            "volume": float(command.get("lot") or 0),
            "type": order_type,
            "price": price,
            "sl": float(command.get("stopLoss") or 0),
            "tp": float(command.get("takeProfit") or 0),
            "deviation": 20,
            "comment": "Lumitrader command",
            "type_time": mt5.ORDER_TIME_GTC,
            "type_filling": filling_mode,
        }
        result = mt5.order_send(request)
        if result is None or result.retcode != mt5.TRADE_RETCODE_DONE:
            raise RuntimeError(f"order_send falhou: {getattr(result, 'retcode', mt5.last_error())}")

        acknowledge_command(command["id"], "executed", {"retcode": result.retcode, "order": result.order, "deal": result.deal})
        return

    if command_type == "close_position":
        positions = mt5.positions_get(symbol=symbol) or []
        reference_ticket = str(command.get("referenceTicket") or "").strip()
        if reference_ticket:
            positions = [position for position in positions if str(position.ticket) == reference_ticket]
        if not positions:
            acknowledge_command(command["id"], "executed", {"message": "Nenhuma posicao aberta para fechar."})
            return

        position = positions[0]
        tick = mt5.symbol_info_tick(symbol)
        if not tick:
            raise RuntimeError(f"Tick indisponivel para {symbol}")

        close_type = mt5.ORDER_TYPE_SELL if position.type == mt5.POSITION_TYPE_BUY else mt5.ORDER_TYPE_BUY
        price = tick.bid if position.type == mt5.POSITION_TYPE_BUY else tick.ask
        request = {
            "action": mt5.TRADE_ACTION_DEAL,
            "symbol": symbol,
            "volume": float(position.volume),
            "type": close_type,
            "position": position.ticket,
            "price": price,
            "deviation": 20,
            "comment": "Lumitrader close",
            "type_time": mt5.ORDER_TIME_GTC,
            "type_filling": filling_mode,
        }
        result = mt5.order_send(request)
        if result is None or result.retcode != mt5.TRADE_RETCODE_DONE:
            raise RuntimeError(f"close order_send falhou: {getattr(result, 'retcode', mt5.last_error())}")

        acknowledge_command(command["id"], "executed", {"retcode": result.retcode, "order": result.order, "deal": result.deal, "closed_position": position.ticket})
        return

    raise RuntimeError(f"Tipo de comando nao suportado: {command_type}")


def process_commands(account_number: str) -> None:
    commands = fetch_pending_commands(account_number)
    for command in commands:
        try:
            execute_command(command)
        except Exception as exc:
            acknowledge_command(command["id"], "failed", error=str(exc))

def ensure_terminal() -> None:
    if not mt5.initialize():
        raise RuntimeError(f"MT5 initialize failed: {mt5.last_error()}")


def login_account(account: Dict[str, Any]) -> None:
    ensure_terminal()
    login = int(account["number"])
    password = account["password"]
    server = account["server"]
    if not mt5.login(login=login, password=password, server=server):
        raise RuntimeError(f"MT5 login failed for {login}: {mt5.last_error()}")


def account_payload(symbol: str | None = None) -> Dict[str, Any]:
    account = mt5.account_info()
    if account is None:
        raise RuntimeError("MT5 account_info returned None")

    currency_code = account.currency or "USD"
    currency_symbol = "$" if currency_code == "USD" else "R$" if currency_code == "BRL" else currency_code
    server_time = None
    if symbol:
        tick = mt5.symbol_info_tick(symbol)
        if tick:
            server_time = utc_iso(tick.time)

    return {
        "number": str(account.login),
        "broker": getattr(account, "company", None),
        "server": account.server,
        "server_time": server_time,
        "name": account.name,
        "currency_code": currency_code,
        "currency_symbol": currency_symbol,
        "balance": float(account.balance),
        "equity": float(account.equity),
        "margin": float(account.margin),
        "free_margin": float(account.margin_free),
        "margin_level": float(account.margin_level),
        "leverage": int(account.leverage),
    }


def build_position_snapshot(position) -> PositionSnapshot:
    return PositionSnapshot(
        ticket=int(position.ticket),
        symbol=position.symbol,
        side="buy" if position.type == mt5.POSITION_TYPE_BUY else "sell",
        lot=float(position.volume),
        entry_price=float(position.price_open),
        stop_loss=float(position.sl or 0),
        take_profit=float(position.tp or 0),
        profit_loss=float(position.profit),
        opened_at=utc_iso(position.time),
    )


def calculate_rsi(closes: List[float], period: int = 14) -> float | None:
    if len(closes) <= period:
        return None

    deltas = [closes[index] - closes[index - 1] for index in range(1, len(closes))]
    recent = deltas[-period:]
    gains = [max(delta, 0.0) for delta in recent]
    losses = [max(-delta, 0.0) for delta in recent]
    average_gain = sum(gains) / period
    average_loss = sum(losses) / period

    if average_loss == 0:
        return 100.0

    rs = average_gain / average_loss
    return 100.0 - (100.0 / (1.0 + rs))



def build_market_payload(symbol: str, timeframe_name: str) -> Dict[str, Any]:
    timeframe = TIMEFRAME_MAP.get(timeframe_name, mt5.TIMEFRAME_M5)
    rates = mt5.copy_rates_from_pos(symbol, timeframe, 0, 40)
    if rates is None:
        rates = []

    candles = [
        {
            "time": utc_iso(int(rate["time"])),
            "open": float(rate["open"]),
            "high": float(rate["high"]),
            "low": float(rate["low"]),
            "close": float(rate["close"]),
        }
        for rate in rates
    ]

    closes = [candle["close"] for candle in candles]
    highs = [candle["high"] for candle in candles]
    lows = [candle["low"] for candle in candles]

    moving_average_20 = sum(closes[-20:]) / 20 if len(closes) >= 20 else None
    rsi = calculate_rsi(closes, 14)
    support = min(lows[-20:]) if len(lows) >= 20 else None
    resistance = max(highs[-20:]) if len(highs) >= 20 else None

    trend = None
    if moving_average_20 is not None and len(closes) >= 2:
        last_close = closes[-1]
        previous_close = closes[-2]
        if last_close > moving_average_20 and last_close >= previous_close:
            trend = "uptrend"
        elif last_close < moving_average_20 and last_close <= previous_close:
            trend = "downtrend"
        else:
            trend = "range"

    tick = mt5.symbol_info_tick(symbol)
    symbol_info = mt5.symbol_info(symbol)
    notes = [f"Sync do mercado para {symbol} em {timeframe_name}."]
    if trend:
        notes.append(f"Tendencia detectada: {trend}.")
    if rsi is not None:
        notes.append(f"RSI 14: {rsi:.2f}.")
    if moving_average_20 is not None:
        notes.append(f"Media movel 20: {moving_average_20:.2f}.")

    return {
        "trend": trend,
        "rsi": round(rsi, 2) if rsi is not None else None,
        "moving_average_20": round(moving_average_20, 2) if moving_average_20 is not None else None,
        "support": round(support, 2) if support is not None else None,
        "resistance": round(resistance, 2) if resistance is not None else None,
        "notes": notes,
        "candles": candles,
        "spread": float(symbol_info.spread) if symbol_info else None,
        "last_bid": float(tick.bid) if tick else None,
        "last_ask": float(tick.ask) if tick else None,
    }



def send_event(payload: Dict[str, Any]) -> None:
    response = requests.post(
        EVENTS_URL,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {INGEST_TOKEN}",
        },
        json=payload,
        timeout=30,
    )
    response.raise_for_status()
    operation = payload.get("operation") or {}
    symbol = operation.get("symbol", "sync")
    print(f"Event sent: {payload['event']} -> {symbol} -> {response.status_code}")


def sync_account(account_config: Dict[str, Any]) -> Dict[str, Any]:
    config = account_config.get("config") or {}
    symbol = config.get("ativo") or "XAUUSD"
    timeframe = config.get("timeframe") or "M5"
    market = build_market_payload(symbol, timeframe)
    payload = {
        "event": "account_sync",
        "account": account_payload(symbol),
        "session": {
            "mode": config.get("modo"),
            "breakeven_enabled": config.get("breakeven_ativo"),
            "trailing_stop_enabled": config.get("trailing_stop_ativo"),
            "profit_target": config.get("meta_lucro_diaria"),
            "daily_loss_limit": config.get("perda_maxima_diaria"),
            "operation_limit_enabled": config.get("limite_operacoes_ativo"),
            "operation_limit": config.get("limite_operacoes_diaria"),
        },
        "market": market,
    }
    send_event(payload)
    return payload


def handle_open_positions(account_config: Dict[str, Any], account_state: Dict[str, Any]) -> None:
    config = account_config.get("config") or {}
    symbol_filter = config.get("ativo")
    current_positions = mt5.positions_get(symbol=symbol_filter) if symbol_filter else mt5.positions_get()
    current_positions = current_positions or []
    current_map = {str(position.ticket): build_position_snapshot(position) for position in current_positions}
    account = account_payload(symbol_filter)
    timeframe = config.get("timeframe") or "M5"
    market = build_market_payload(symbol_filter or current_positions[0].symbol if current_positions else "XAUUSD", timeframe)

    for ticket, snapshot in current_map.items():
        if ticket not in account_state["open_positions"]:
            payload = {
                "event": "operation_opened",
                "account": account,
                "session": {
                    "mode": config.get("modo"),
                    "breakeven_enabled": config.get("breakeven_ativo"),
                    "trailing_stop_enabled": config.get("trailing_stop_ativo"),
                    "profit_target": config.get("meta_lucro_diaria"),
                    "daily_loss_limit": config.get("perda_maxima_diaria"),
                    "operation_limit_enabled": config.get("limite_operacoes_ativo"),
                    "operation_limit": config.get("limite_operacoes_diaria"),
                },
                "market": market,
                "operation": {
                    "ticket": ticket,
                    "symbol": snapshot.symbol,
                    "timeframe": timeframe,
                    "side": snapshot.side,
                    "lot": snapshot.lot,
                    "entry_price": snapshot.entry_price,
                    "stop_loss": snapshot.stop_loss,
                    "take_profit": snapshot.take_profit,
                    "profit_loss": snapshot.profit_loss,
                    "opened_at": snapshot.opened_at,
                },
            }
            send_event(payload)

    account_state["open_positions"] = {ticket: snapshot.__dict__ for ticket, snapshot in current_map.items()}


def handle_closed_positions(account_config: Dict[str, Any], account_state: Dict[str, Any]) -> None:
    config = account_config.get("config") or {}
    symbol = config.get("ativo") or "XAUUSD"
    timeframe = config.get("timeframe") or "M5"
    start_of_day = datetime.now(timezone.utc).replace(hour=0, minute=0, second=0, microsecond=0)
    history = mt5.history_deals_get(start_of_day, datetime.now(timezone.utc))
    if history is None:
        history = []
    account = account_payload(symbol)
    market = build_market_payload(symbol, timeframe)
    recent_history_ids = set(account_state.get("recent_history_ids", []))
    open_positions = account_state.get("open_positions", {})

    for deal in history:
        if deal.entry != mt5.DEAL_ENTRY_OUT:
            continue
        history_id = str(deal.ticket)
        if history_id in recent_history_ids:
            continue
        position_id = str(deal.position_id)
        cached_position = open_positions.get(position_id)
        payload = {
            "event": "operation_closed",
            "account": account,
            "session": {
                "mode": config.get("modo"),
                "breakeven_enabled": config.get("breakeven_ativo"),
                "trailing_stop_enabled": config.get("trailing_stop_ativo"),
                "profit_target": config.get("meta_lucro_diaria"),
                "daily_loss_limit": config.get("perda_maxima_diaria"),
                "operation_limit_enabled": config.get("limite_operacoes_ativo"),
                "operation_limit": config.get("limite_operacoes_diaria"),
            },
            "market": market,
            "operation": {
                "ticket": position_id,
                "symbol": deal.symbol,
                "timeframe": timeframe,
                "side": cached_position["side"] if cached_position else "buy",
                "lot": float(deal.volume),
                "entry_price": float(cached_position["entry_price"]) if cached_position else float(deal.price),
                "exit_price": float(deal.price),
                "stop_loss": float(cached_position["stop_loss"]) if cached_position else 0,
                "take_profit": float(cached_position["take_profit"]) if cached_position else 0,
                "profit_loss": float(deal.profit),
                "opened_at": cached_position["opened_at"] if cached_position else utc_iso(deal.time),
                "closed_at": utc_iso(deal.time),
                "close_reason": str(getattr(deal, "reason", "mt5_close")),
            },
        }
        send_event(payload)
        recent_history_ids.add(history_id)
        if position_id in open_positions:
            del open_positions[position_id]

    account_state["open_positions"] = open_positions
    account_state["recent_history_ids"] = list(recent_history_ids)[-200:]


def main() -> None:
    if not INGEST_TOKEN:
        raise RuntimeError("Missing LUMITRADER_INGEST_TOKEN")

    state = load_state()
    state.setdefault("accounts", {})
    print("Lumitrader MT5 reporter started")

    while True:
        try:
            accounts = fetch_active_accounts()
            active_numbers = set()
            for account in accounts:
                number = str(account["number"])
                active_numbers.add(number)
                state["accounts"].setdefault(number, {"open_positions": {}, "recent_history_ids": []})
                account_state = state["accounts"][number]

                try:
                    login_account(account)
                    process_commands(number)
                    sync_account(account)
                    handle_open_positions(account, account_state)
                    handle_closed_positions(account, account_state)
                except Exception as account_exc:
                    print(f"Account {number} error: {account_exc}")

            stale = [number for number in state["accounts"].keys() if number not in active_numbers]
            for number in stale:
                del state["accounts"][number]

            save_state(state)
        except Exception as exc:
            print(f"Loop error: {exc}")

        time.sleep(POLL_INTERVAL_SECONDS)


if __name__ == "__main__":
    main()
</file>

<file path="src/lib/backend/reporting.ts">
import { toCsv, toHtml } from "@/lib/backend/formatters";
import { sendReportToN8n } from "@/lib/backend/n8n";
import { generateAiSummary } from "@/lib/backend/openai";
import { evaluateAutoOpportunity } from "@/lib/backend/auto-trader";
import { calculateRiskSnapshot } from "@/lib/backend/risk";
import {
  attachOperationTelemetry,
  countOperationsToday,
  enqueueAutoTradeCommand,
  loadAccountExecutionState,
  loadTradingContext,
  recordTradingEvent,
  refreshDailyStats,
  updateAccountSnapshot,
} from "@/lib/backend/supabase";
import type { ReportPayload, TradingEventPayload } from "@/lib/backend/types";

export async function processTradingEvent(payload: TradingEventPayload) {
  const context = await loadTradingContext(payload.account.number);

  if (!context.user.acesso_ativo) {
    throw new Error("Usuario bloqueado pelo SaaS.");
  }

  const licenseActive = context.license.status === "ativa" && context.license.data_expiracao >= new Date().toISOString().slice(0, 10);
  if (!licenseActive) {
    throw new Error("Licenca inativa, bloqueada ou expirada para esta conta MT5.");
  }

  await updateAccountSnapshot(context.account.id, payload);

  if (payload.event === "account_sync") {
    const [operationsToday, executionState] = await Promise.all([
      countOperationsToday(context.account.id),
      loadAccountExecutionState(context.account.id),
    ]);

    const signal = evaluateAutoOpportunity(context, payload, operationsToday);

    if (signal && !executionState.hasOpenPosition && !executionState.hasPendingCommand) {
      await enqueueAutoTradeCommand(context, payload, signal);
      return { synced: true, account: payload.account.number, mode: "fast_sync", autoCommand: signal.type };
    }

    return { synced: true, account: payload.account.number, mode: "fast_sync" };
  }

  if (!payload.operation) {
    throw new Error("Operacao obrigatoria para este evento.");
  }

  const operationPayload = payload.operation;
  const operationId = await recordTradingEvent(context, payload);
  await refreshDailyStats(context, payload);
  const operationsToday = await countOperationsToday(context.account.id);

  const risk = calculateRiskSnapshot({
    payload,
    balance: payload.account.balance ?? context.account.saldo_atual,
    equity: payload.account.equity ?? context.account.equity,
    operationsToday,
    sessionConfig: {
      daily_loss_limit: payload.session?.daily_loss_limit ?? context.config?.perda_maxima_diaria ?? null,
      operation_limit_enabled: payload.session?.operation_limit_enabled ?? context.config?.limite_operacoes_ativo ?? null,
      operation_limit: payload.session?.operation_limit ?? context.config?.limite_operacoes_diaria ?? null,
    },
  });

  const baseReport: Omit<ReportPayload, "ai" | "formats"> = {
    event: payload.event,
    generatedAt: new Date().toISOString(),
    user: {
      id: context.user.id,
      nome: context.user.nome,
      email: context.user.email,
      telegram_id: context.user.telegram_id,
    },
    account: {
      id: context.account.id,
      number: context.account.numero_conta,
      broker: payload.account.broker ?? context.account.corretora,
      currency_code: payload.account.currency_code ?? context.account.moeda_codigo,
      currency_symbol: payload.account.currency_symbol ?? context.account.moeda_simbolo,
      balance: payload.account.balance ?? context.account.saldo_atual,
      equity: payload.account.equity ?? context.account.equity,
      active_license: {
        id: context.license.id,
        plan: context.license.nome_plano,
        expires_at: context.license.data_expiracao,
        status: context.license.status,
        value: context.license.valor,
      },
    },
    operation: operationPayload,
    risk,
  };

  const ai = await generateAiSummary(baseReport);
  const reportWithoutFormats: Omit<ReportPayload, "formats"> = {
    ...baseReport,
    ai,
  };

  await attachOperationTelemetry(operationId, payload, reportWithoutFormats);
  await updateAccountSnapshot(context.account.id, payload, ai.summary);

  const report: ReportPayload = {
    ...reportWithoutFormats,
    formats: {
      json: JSON.stringify(reportWithoutFormats, null, 2),
      csv: toCsv(reportWithoutFormats),
      html: toHtml(reportWithoutFormats),
    },
  };

  await sendReportToN8n(report);

  return report;
}
</file>

<file path="src/lib/backend/supabase.ts">
import { createAdminClient } from "@/lib/supabase/admin";
import type { ReportPayload, TradingEventPayload } from "@/lib/backend/types";

export type LoadedContext = {
  user: {
    id: string;
    nome: string | null;
    email: string | null;
    telegram_id: string | null;
    acesso_ativo: boolean;
  };
  account: {
    id: string;
    user_id: string;
    nome_cliente: string;
    numero_conta: string;
    corretora: string | null;
    servidor: string | null;
    moeda_codigo: string;
    moeda_simbolo: string;
    saldo_atual: number;
    equity: number;
    margem: number | null;
    margem_livre: number | null;
    nivel_margem: number | null;
    alavancagem: number | null;
    ativo: boolean;
  };
  license: {
    id: string;
    nome_plano: string;
    status: string;
    valor: number;
    data_expiracao: string;
  };
  config: {
    id: string;
    sistema_ligado: boolean;
    modo: "agressivo" | "conservador";
    timeframe: string;
    breakeven_ativo: boolean;
    trailing_stop_ativo: boolean;
    meta_lucro_diaria: number;
    perda_maxima_diaria: number;
    limite_operacoes_ativo: boolean;
    limite_operacoes_diaria: number | null;
    ativo: string;
  } | null;
};

export async function loadSyncContext(accountNumber: string): Promise<Pick<LoadedContext, "user" | "account" | "license">> {
  const adminClient = createAdminClient();
  const { data, error } = await adminClient
    .from("contas_trading")
    .select(`
      id,
      user_id,
      nome_cliente,
      numero_conta,
      corretora,
      servidor,
      moeda_codigo,
      moeda_simbolo,
      saldo_atual,
      equity,
      margem,
      margem_livre,
      nivel_margem,
      alavancagem,
      ativo,
      usuarios!inner(id, nome, email, telegram_id, acesso_ativo),
      licencas!inner(id, nome_plano, status, valor, data_expiracao)
    `)
    .eq("numero_conta", accountNumber)
    .eq("licencas.status", "ativa")
    .gte("licencas.data_expiracao", new Date().toISOString().slice(0, 10))
    .maybeSingle();

  if (error || !data) {
    throw new Error("Conta MT5 nao encontrada ou sem licenca ativa.");
  }

  const user = Array.isArray(data.usuarios) ? data.usuarios[0] : data.usuarios;
  const license = Array.isArray(data.licencas) ? data.licencas[0] : data.licencas;

  if (!user || !license) {
    throw new Error("Conta MT5 sem usuario/licenca valida.");
  }

  return {
    user,
    license,
    account: {
      id: data.id,
      user_id: data.user_id,
      nome_cliente: data.nome_cliente,
      numero_conta: data.numero_conta,
      corretora: data.corretora,
      servidor: data.servidor,
      moeda_codigo: data.moeda_codigo,
      moeda_simbolo: data.moeda_simbolo,
      saldo_atual: data.saldo_atual,
      equity: data.equity,
      margem: data.margem,
      margem_livre: data.margem_livre,
      nivel_margem: data.nivel_margem,
      alavancagem: data.alavancagem,
      ativo: data.ativo,
    },
  };
}

export async function loadTradingContext(accountNumber: string): Promise<LoadedContext> {
  const adminClient = createAdminClient();
  const { data: account, error: accountError } = await adminClient
    .from("contas_trading")
    .select("id, user_id, nome_cliente, numero_conta, corretora, servidor, moeda_codigo, moeda_simbolo, saldo_atual, equity, margem, margem_livre, nivel_margem, alavancagem, ativo")
    .eq("numero_conta", accountNumber)
    .maybeSingle<LoadedContext["account"]>();

  if (accountError || !account) {
    throw new Error("Conta MT5 nao encontrada.");
  }

  const [{ data: user }, { data: license }, { data: config }] = await Promise.all([
    adminClient
      .from("usuarios")
      .select("id, nome, email, telegram_id, acesso_ativo")
      .eq("id", account.user_id)
      .single<LoadedContext["user"]>(),
    adminClient
      .from("licencas")
      .select("id, nome_plano, status, valor, data_expiracao")
      .eq("conta_trading_id", account.id)
      .maybeSingle<LoadedContext["license"]>(),
    adminClient
      .from("configuracoes_sessao")
      .select("id, sistema_ligado, modo, timeframe, breakeven_ativo, trailing_stop_ativo, meta_lucro_diaria, perda_maxima_diaria, limite_operacoes_ativo, limite_operacoes_diaria, ativo")
      .eq("conta_trading_id", account.id)
      .order("atualizado_em", { ascending: false })
      .limit(1)
      .maybeSingle<LoadedContext["config"]>(),
  ]);

  if (!user) {
    throw new Error("Usuario da conta nao encontrado.");
  }

  if (!license) {
    throw new Error("Nenhuma licenca vinculada a esta conta.");
  }

  return { user, account, license, config };
}

export async function updateAccountSnapshot(accountId: string, payload: TradingEventPayload, insightSummary?: string | null) {
  const adminClient = createAdminClient();
  await adminClient
    .from("contas_trading")
    .update({
      nome_cliente: payload.account.name ?? undefined,
      corretora: payload.account.broker ?? undefined,
      servidor: payload.account.server ?? undefined,
      moeda_codigo: payload.account.currency_code ?? undefined,
      moeda_simbolo: payload.account.currency_symbol ?? undefined,
      saldo_atual: payload.account.balance ?? undefined,
      equity: payload.account.equity ?? undefined,
      margem: payload.account.margin ?? undefined,
      margem_livre: payload.account.free_margin ?? undefined,
      nivel_margem: payload.account.margin_level ?? undefined,
      alavancagem: payload.account.leverage ?? undefined,
      server_time: payload.account.server_time ?? null,
      mercado_snapshot: payload.market ?? null,
      insight_atual: insightSummary ?? undefined,
      ultima_sincronizacao: new Date().toISOString(),
      ativo: true,
    })
    .eq("id", accountId);
}

export async function recordTradingEvent(context: LoadedContext, payload: TradingEventPayload) {
  const adminClient = createAdminClient();

  if (!payload.operation) {
    return null;
  }

  if (payload.event === "operation_opened") {
    const { data: inserted } = await adminClient
      .from("operacoes")
      .insert({
        user_id: context.user.id,
        conta_trading_id: context.account.id,
        ativo: payload.operation.symbol,
        timeframe: payload.operation.timeframe,
        direcao: payload.operation.side === "buy" ? "compra" : "venda",
        status: "aberta",
        lote: payload.operation.lot,
        preco_entrada: payload.operation.entry_price,
        stop_loss: payload.operation.stop_loss ?? null,
        take_profit: payload.operation.take_profit ?? null,
        spread: payload.operation.spread ?? null,
        volume: payload.operation.volume ?? null,
        volatilidade: payload.operation.volatility ?? null,
        be_ativo: payload.session?.breakeven_enabled ?? context.config?.breakeven_ativo ?? false,
        ts_ativo: payload.session?.trailing_stop_enabled ?? context.config?.trailing_stop_ativo ?? false,
        validacao_ia: {
          ticket: payload.operation.ticket ?? null,
          market: payload.market ?? null,
        },
        aberta_em: payload.operation.opened_at,
      })
      .select("id")
      .single<{ id: string }>();

    return inserted?.id ?? null;
  }

  let latestOpen: { id: string; validacao_ia?: Record<string, unknown> | null } | null = null;

  if (payload.operation.ticket) {
    const { data: ticketMatchedOpen } = await adminClient
      .from("operacoes")
      .select("id, validacao_ia")
      .eq("conta_trading_id", context.account.id)
      .eq("status", "aberta")
      .contains("validacao_ia", { ticket: payload.operation.ticket })
      .order("aberta_em", { ascending: false })
      .limit(1)
      .maybeSingle<{ id: string; validacao_ia?: Record<string, unknown> | null }>();

    latestOpen = ticketMatchedOpen ?? null;
  }

  if (!latestOpen) {
    const { data: symbolMatchedOpen } = await adminClient
      .from("operacoes")
      .select("id, validacao_ia")
      .eq("conta_trading_id", context.account.id)
      .eq("status", "aberta")
      .eq("ativo", payload.operation.symbol)
      .order("aberta_em", { ascending: false })
      .limit(1)
      .maybeSingle<{ id: string; validacao_ia?: Record<string, unknown> | null }>();

    latestOpen = symbolMatchedOpen ?? null;
  }

  if (latestOpen?.id) {
    const currentTelemetry = latestOpen.validacao_ia && typeof latestOpen.validacao_ia === "object"
      ? latestOpen.validacao_ia
      : {};

    await adminClient
      .from("operacoes")
      .update({
        status: "fechada",
        preco_saida: payload.operation.exit_price ?? null,
        lucro_prejuizo: payload.operation.profit_loss ?? 0,
        motivo_fechamento: payload.operation.close_reason ?? null,
        fechada_em: payload.operation.closed_at ?? new Date().toISOString(),
        spread: payload.operation.spread ?? null,
        volume: payload.operation.volume ?? null,
        volatilidade: payload.operation.volatility ?? null,
        validacao_ia: {
          ...currentTelemetry,
          ticket: payload.operation.ticket ?? currentTelemetry.ticket ?? null,
          market: payload.market ?? currentTelemetry.market ?? null,
        },
      })
      .eq("id", latestOpen.id);

    return latestOpen.id;
  }
  const { data: inserted } = await adminClient
    .from("operacoes")
    .insert({
      user_id: context.user.id,
      conta_trading_id: context.account.id,
      ativo: payload.operation.symbol,
      timeframe: payload.operation.timeframe,
      direcao: payload.operation.side === "buy" ? "compra" : "venda",
      status: "fechada",
      lote: payload.operation.lot,
      preco_entrada: payload.operation.entry_price,
      preco_saida: payload.operation.exit_price ?? null,
      stop_loss: payload.operation.stop_loss ?? null,
      take_profit: payload.operation.take_profit ?? null,
      lucro_prejuizo: payload.operation.profit_loss ?? 0,
      spread: payload.operation.spread ?? null,
      volume: payload.operation.volume ?? null,
      volatilidade: payload.operation.volatility ?? null,
      be_ativo: payload.session?.breakeven_enabled ?? context.config?.breakeven_ativo ?? false,
      ts_ativo: payload.session?.trailing_stop_enabled ?? context.config?.trailing_stop_ativo ?? false,
      validacao_ia: {
        ticket: payload.operation.ticket ?? null,
        market: payload.market ?? null,
      },
      motivo_fechamento: payload.operation.close_reason ?? null,
      aberta_em: payload.operation.opened_at,
      fechada_em: payload.operation.closed_at ?? new Date().toISOString(),
    })
    .select("id")
    .single<{ id: string }>();

    return inserted?.id ?? null;
}

export async function attachOperationTelemetry(operationId: string | null, payload: TradingEventPayload, report: Omit<ReportPayload, "formats">) {
  if (!operationId || !payload.operation) {
    return;
  }

  const adminClient = createAdminClient();
  await adminClient
    .from("operacoes")
    .update({
      validacao_ia: {
        ticket: payload.operation.ticket ?? null,
        market: payload.market ?? null,
        ai: report.ai,
        report_generated_at: report.generatedAt,
      },
    })
    .eq("id", operationId);
}

export async function countOperationsToday(accountId: string) {
  const adminClient = createAdminClient();
  const start = new Date();
  start.setHours(0, 0, 0, 0);

  const { count } = await adminClient
    .from("operacoes")
    .select("id", { count: "exact", head: true })
    .eq("conta_trading_id", accountId)
    .gte("aberta_em", start.toISOString());

  return count ?? 0;
}

export async function refreshDailyStats(context: LoadedContext, payload: TradingEventPayload) {
  if (!payload.operation) {
    return;
  }

  const adminClient = createAdminClient();
  const today = new Date().toISOString().slice(0, 10);
  const { data: operations } = await adminClient
    .from("operacoes")
    .select("lucro_prejuizo")
    .eq("conta_trading_id", context.account.id)
    .gte("aberta_em", `${today}T00:00:00.000Z`)
    .lte("aberta_em", `${today}T23:59:59.999Z`);

  const normalized = operations ?? [];
  const profits = normalized.map((item) => Number(item.lucro_prejuizo ?? 0));
  const wins = profits.filter((value) => value > 0).length;
  const losses = profits.filter((value) => value < 0).length;
  const best = profits.length ? Math.max(...profits) : 0;
  const worst = profits.length ? Math.min(...profits) : 0;
  const profitTotal = profits.filter((value) => value > 0).reduce((sum, value) => sum + value, 0);
  const lossTotal = profits.filter((value) => value < 0).reduce((sum, value) => sum + Math.abs(value), 0);
  const drawdown = context.account.saldo_atual ? Number(((lossTotal / Math.max(context.account.saldo_atual, 1)) * 100).toFixed(2)) : 0;

  await adminClient.from("estatisticas").upsert({
    user_id: context.user.id,
    conta_trading_id: context.account.id,
    ativo: payload.operation.symbol,
    periodo: today,
    operacoes_total: normalized.length,
    vitorias: wins,
    derrotas: losses,
    win_rate: normalized.length ? Number(((wins / normalized.length) * 100).toFixed(2)) : 0,
    lucro_total: profitTotal,
    prejuizo_total: lossTotal,
    drawdown,
    melhor_operacao: best,
    pior_operacao: worst,
  }, {
    onConflict: "user_id,conta_trading_id,ativo,periodo",
  });
}



export async function loadAccountExecutionState(accountId: string) {
  const adminClient = createAdminClient();
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const [{ count: openPositions }, { count: pendingCommands }] = await Promise.all([
    adminClient
      .from("operacoes")
      .select("id", { count: "exact", head: true })
      .eq("conta_trading_id", accountId)
      .eq("status", "aberta"),
    adminClient
      .from("comandos_trading")
      .select("id", { count: "exact", head: true })
      .eq("conta_trading_id", accountId)
      .in("status", ["pending", "processing"])
      .gte("solicitado_em", today.toISOString()),
  ]);

  return {
    hasOpenPosition: (openPositions ?? 0) > 0,
    hasPendingCommand: (pendingCommands ?? 0) > 0,
  };
}

export async function enqueueAutoTradeCommand(
  context: LoadedContext,
  payload: TradingEventPayload,
  signal: {
    type: "open_buy" | "open_sell";
    lot: number;
    stopLoss: number;
    takeProfit: number;
    rationale: string;
  },
) {
  const adminClient = createAdminClient();
  const { error } = await adminClient.from("comandos_trading").insert({
    user_id: context.user.id,
    conta_trading_id: context.account.id,
    ativo: payload.operation?.symbol ?? context.config?.ativo ?? "XAUUSD",
    timeframe: payload.operation?.timeframe ?? context.config?.timeframe ?? "M5",
    tipo: signal.type,
    lote: signal.lot,
    stop_loss: signal.stopLoss,
    take_profit: signal.takeProfit,
    payload: {
      origem: "auto_signal",
      rationale: signal.rationale,
      market: payload.market ?? null,
    },
  });

  if (error) {
    throw new Error(error.message);
  }
}
</file>

<file path="supabase/schema.sql">
create extension if not exists pgcrypto;

create type public.user_role as enum ('admin', 'user');
create type public.modo_operacao as enum ('agressivo', 'conservador');
create type public.direcao_operacao as enum ('compra', 'venda');
create type public.status_operacao as enum ('aberta', 'fechada', 'cancelada');
create type public.status_licenca as enum ('ativa', 'expirada', 'bloqueada', 'cancelada', 'pendente');
create type public.tipo_comando_trading as enum ('open_buy', 'open_sell', 'close_position');
create type public.status_comando_trading as enum ('pending', 'processing', 'executed', 'failed', 'cancelled');

create table if not exists public.usuarios (
  id uuid primary key references auth.users (id) on delete cascade,
  nome text,
  email text unique,
  telegram_id text,
  role public.user_role not null default 'user',
  acesso_ativo boolean not null default true,
  ativo_padrao text not null default 'XAUUSD',
  timeframe_padrao text not null default 'M5',
  criado_em timestamptz not null default timezone('utc', now()),
  atualizado_em timestamptz not null default timezone('utc', now())
);

create table if not exists public.contas_trading (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  nome_cliente text not null,
  numero_conta text not null,
  corretora text,
  servidor text,
  moeda_codigo text not null default 'USD',
  moeda_simbolo text not null default '$',
  saldo_atual numeric(14, 2) not null default 0,
  equity numeric(14, 2) not null default 0,
  margem numeric(14, 2) not null default 0,
  margem_livre numeric(14, 2) not null default 0,
  nivel_margem numeric(10, 2) not null default 0,
  alavancagem integer,
  ativo boolean not null default false,
  atualizado_em timestamptz not null default timezone('utc', now()),
  criado_em timestamptz not null default timezone('utc', now()),
  mt5_password text,
  server_time timestamptz,
  mercado_snapshot jsonb,
  insight_atual text,
  ultima_sincronizacao timestamptz,
  unique (user_id, numero_conta)
);

create table if not exists public.licencas (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  nome_plano text not null default 'Licenca Padrao',
  status public.status_licenca not null default 'pendente',
  valor numeric(12, 2) not null default 0,
  data_inicio date not null default current_date,
  data_expiracao date not null,
  bloqueio_automatico boolean not null default true,
  observacoes text,
  criado_em timestamptz not null default timezone('utc', now()),
  atualizado_em timestamptz not null default timezone('utc', now()),
  unique (conta_trading_id)
);

create table if not exists public.ativos_config (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo text not null,
  timeframe text not null default 'M5',
  risco_por_operacao numeric(8, 4) not null default 0.01,
  ativo_principal boolean not null default false,
  criado_em timestamptz not null default timezone('utc', now()),
  atualizado_em timestamptz not null default timezone('utc', now())
);

create table if not exists public.configuracoes_sessao (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo text not null,
  sistema_ligado boolean not null default false,
  modo public.modo_operacao not null default 'conservador',
  breakeven_ativo boolean not null default true,
  trailing_stop_ativo boolean not null default true,
  horario_inicio time not null,
  horario_fim time not null,
  meta_lucro_diaria numeric(12, 2) not null default 0,
  perda_maxima_diaria numeric(12, 2) not null default 0,
  limite_operacoes_ativo boolean not null default false,
  limite_operacoes_diaria integer,
  observacoes text,
  atualizado_em timestamptz not null default timezone('utc', now()),
  timeframe text not null default 'M5'
);

create table if not exists public.operacoes (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo_config_id uuid references public.ativos_config (id) on delete set null,
  ativo text not null,
  timeframe text not null,
  direcao public.direcao_operacao not null,
  status public.status_operacao not null default 'aberta',
  lote numeric(10, 2) not null,
  preco_entrada numeric(12, 5) not null,
  preco_saida numeric(12, 5),
  stop_loss numeric(12, 5),
  take_profit numeric(12, 5),
  lucro_prejuizo numeric(12, 2) not null default 0,
  spread numeric(10, 3),
  volume numeric(14, 2),
  volatilidade numeric(14, 4),
  be_ativo boolean not null default false,
  ts_ativo boolean not null default false,
  validacao_ia jsonb,
  motivo_fechamento text,
  aberta_em timestamptz not null default timezone('utc', now()),
  fechada_em timestamptz
);

create table if not exists public.estatisticas (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo text not null,
  periodo date not null,
  operacoes_total integer not null default 0,
  vitorias integer not null default 0,
  derrotas integer not null default 0,
  win_rate numeric(6, 2) not null default 0,
  lucro_total numeric(12, 2) not null default 0,
  prejuizo_total numeric(12, 2) not null default 0,
  drawdown numeric(10, 2) not null default 0,
  melhor_operacao numeric(12, 2) not null default 0,
  pior_operacao numeric(12, 2) not null default 0,
  criado_em timestamptz not null default timezone('utc', now()),
  unique (user_id, conta_trading_id, ativo, periodo)
);

create table if not exists public.comandos_trading (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.usuarios (id) on delete cascade,
  conta_trading_id uuid not null references public.contas_trading (id) on delete cascade,
  ativo text not null,
  timeframe text not null default 'M5',
  tipo public.tipo_comando_trading not null,
  status public.status_comando_trading not null default 'pending',
  lote numeric(10, 2),
  stop_loss numeric(12, 5),
  take_profit numeric(12, 5),
  ticket_referencia text,
  payload jsonb,
  resultado jsonb,
  erro text,
  solicitado_em timestamptz not null default timezone('utc', now()),
  processado_em timestamptz
);

create or replace function public.set_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.atualizado_em = timezone('utc', now());
  return new;
end;
$$;

create or replace function public.is_admin(user_uuid uuid)
returns boolean
language sql
stable
security definer
set search_path = public
as $$
  select exists (
    select 1
    from public.usuarios
    where id = user_uuid
      and role = 'admin'
      and acesso_ativo = true
  );
$$;

create or replace function public.licenca_ativa(user_uuid uuid, trading_account_uuid uuid)
returns boolean
language sql
stable
security definer
set search_path = public
as $$
  select exists (
    select 1
    from public.licencas l
    join public.usuarios u on u.id = l.user_id
    where l.user_id = user_uuid
      and l.conta_trading_id = trading_account_uuid
      and u.acesso_ativo = true
      and l.status = 'ativa'
      and l.data_inicio <= current_date
      and l.data_expiracao >= current_date
  );
$$;

create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  insert into public.usuarios (id, nome, email)
  values (
    new.id,
    coalesce(new.raw_user_meta_data ->> 'nome', split_part(new.email, '@', 1)),
    new.email
  )
  on conflict (id) do update
  set
    nome = excluded.nome,
    email = excluded.email,
    atualizado_em = timezone('utc', now());

  return new;
end;
$$;

drop trigger if exists trg_usuarios_updated_at on public.usuarios;
create trigger trg_usuarios_updated_at
before update on public.usuarios
for each row
execute function public.set_updated_at();

drop trigger if exists trg_contas_trading_updated_at on public.contas_trading;
create trigger trg_contas_trading_updated_at
before update on public.contas_trading
for each row
execute function public.set_updated_at();

drop trigger if exists trg_licencas_updated_at on public.licencas;
create trigger trg_licencas_updated_at
before update on public.licencas
for each row
execute function public.set_updated_at();

drop trigger if exists trg_ativos_config_updated_at on public.ativos_config;
create trigger trg_ativos_config_updated_at
before update on public.ativos_config
for each row
execute function public.set_updated_at();

drop trigger if exists trg_configuracoes_sessao_updated_at on public.configuracoes_sessao;
create trigger trg_configuracoes_sessao_updated_at
before update on public.configuracoes_sessao
for each row
execute function public.set_updated_at();

drop trigger if exists on_auth_user_created on auth.users;
create trigger on_auth_user_created
after insert on auth.users
for each row
execute function public.handle_new_user();

alter table public.usuarios enable row level security;
alter table public.contas_trading enable row level security;
alter table public.licencas enable row level security;
alter table public.ativos_config enable row level security;
alter table public.configuracoes_sessao enable row level security;
alter table public.operacoes enable row level security;
alter table public.estatisticas enable row level security;
alter table public.comandos_trading enable row level security;

drop policy if exists "usuarios_select_policy" on public.usuarios;
create policy "usuarios_select_policy"
on public.usuarios
for select
using (auth.uid() = id or public.is_admin(auth.uid()));

drop policy if exists "usuarios_update_policy" on public.usuarios;
create policy "usuarios_update_policy"
on public.usuarios
for update
using (auth.uid() = id or public.is_admin(auth.uid()))
with check (auth.uid() = id or public.is_admin(auth.uid()));

drop policy if exists "contas_trading_select_policy" on public.contas_trading;
create policy "contas_trading_select_policy"
on public.contas_trading
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), id)
  )
);

drop policy if exists "contas_trading_admin_write_policy" on public.contas_trading;
create policy "contas_trading_admin_write_policy"
on public.contas_trading
for all
using (public.is_admin(auth.uid()))
with check (public.is_admin(auth.uid()));

drop policy if exists "licencas_select_policy" on public.licencas;
create policy "licencas_select_policy"
on public.licencas
for select
using (public.is_admin(auth.uid()) or auth.uid() = user_id);

drop policy if exists "licencas_admin_write_policy" on public.licencas;
create policy "licencas_admin_write_policy"
on public.licencas
for all
using (public.is_admin(auth.uid()))
with check (public.is_admin(auth.uid()));

drop policy if exists "ativos_config_select_policy" on public.ativos_config;
create policy "ativos_config_select_policy"
on public.ativos_config
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "ativos_config_write_policy" on public.ativos_config;
create policy "ativos_config_write_policy"
on public.ativos_config
for all
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
)
with check (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "configuracoes_sessao_select_policy" on public.configuracoes_sessao;
create policy "configuracoes_sessao_select_policy"
on public.configuracoes_sessao
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "configuracoes_sessao_write_policy" on public.configuracoes_sessao;
create policy "configuracoes_sessao_write_policy"
on public.configuracoes_sessao
for all
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
)
with check (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "operacoes_select_policy" on public.operacoes;
create policy "operacoes_select_policy"
on public.operacoes
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "operacoes_admin_write_policy" on public.operacoes;
create policy "operacoes_admin_write_policy"
on public.operacoes
for all
using (public.is_admin(auth.uid()))
with check (public.is_admin(auth.uid()));

drop policy if exists "estatisticas_select_policy" on public.estatisticas;
create policy "estatisticas_select_policy"
on public.estatisticas
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "estatisticas_admin_write_policy" on public.estatisticas;
create policy "estatisticas_admin_write_policy"
on public.estatisticas
for all
using (public.is_admin(auth.uid()))
with check (public.is_admin(auth.uid()));

drop policy if exists "comandos_trading_select_policy" on public.comandos_trading;
create policy "comandos_trading_select_policy"
on public.comandos_trading
for select
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

drop policy if exists "comandos_trading_write_policy" on public.comandos_trading;
create policy "comandos_trading_write_policy"
on public.comandos_trading
for all
using (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
)
with check (
  public.is_admin(auth.uid())
  or (
    auth.uid() = user_id
    and public.licenca_ativa(auth.uid(), conta_trading_id)
  )
);

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'contas_trading'
  ) then
    alter publication supabase_realtime add table public.contas_trading;
  end if;
end
$$;

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'licencas'
  ) then
    alter publication supabase_realtime add table public.licencas;
  end if;
end
$$;

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'configuracoes_sessao'
  ) then
    alter publication supabase_realtime add table public.configuracoes_sessao;
  end if;
end
$$;

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'operacoes'
  ) then
    alter publication supabase_realtime add table public.operacoes;
  end if;
end
$$;

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'estatisticas'
  ) then
    alter publication supabase_realtime add table public.estatisticas;
  end if;
end
$$;

create index if not exists idx_contas_trading_numero_conta on public.contas_trading (numero_conta);
create index if not exists idx_licencas_conta_status_expiracao on public.licencas (conta_trading_id, status, data_expiracao);
create index if not exists idx_configuracoes_sessao_conta_ligado on public.configuracoes_sessao (conta_trading_id, sistema_ligado, atualizado_em desc);
create index if not exists idx_operacoes_conta_aberta_em on public.operacoes (conta_trading_id, aberta_em desc);
create index if not exists idx_operacoes_conta_status on public.operacoes (conta_trading_id, status);

do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime' and schemaname = 'public' and tablename = 'comandos_trading'
  ) then
    alter publication supabase_realtime add table public.comandos_trading;
  end if;
end
$$;

create index if not exists idx_comandos_trading_conta_status_solicitado on public.comandos_trading (conta_trading_id, status, solicitado_em asc);
</file>

<file path="src/app/admin/actions.ts">
"use server";

import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { requireAdminUser } from "@/lib/auth";
import { createAdminClient } from "@/lib/supabase/admin";

function textValue(formData: FormData, key: string) {
  const value = formData.get(key);
  return typeof value === "string" ? value.trim() : "";
}

function numberValue(formData: FormData, key: string) {
  const value = Number(textValue(formData, key));
  return Number.isFinite(value) ? value : 0;
}

function optionalIntegerValue(formData: FormData, key: string) {
  const raw = textValue(formData, key);
  if (!raw) {
    return null;
  }

  const value = Number(raw);
  return Number.isFinite(value) ? Math.trunc(value) : null;
}

function normalizeDateValue(value: string) {
  if (!value) {
    return "";
  }

  if (/^\d{4}-\d{2}-\d{2}$/.test(value)) {
    return value;
  }

  const match = value.match(/^(\d{2})\/(\d{2})\/(\d{4})$/);
  if (match) {
    const [, day, month, year] = match;
    return `${year}-${month}-${day}`;
  }

  return value;
}

const PLAN_DEFAULTS: Record<string, number> = {
  Starter: 97,
  Pro: 197,
  Premium: 297,
  Enterprise: 497,
};

function resolveLicenseStatus(status: string, expirationDate: string) {
  const today = new Date().toISOString().slice(0, 10);
  if (status === "ativa" && expirationDate < today) {
    return "expirada";
  }
  return status;
}

function nextExpirationDate(currentDate: string) {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const parsedCurrent = currentDate ? new Date(`${currentDate}T00:00:00`) : null;
  const base = parsedCurrent && parsedCurrent > today ? parsedCurrent : today;
  base.setDate(base.getDate() + 30);
  return base.toISOString().slice(0, 10);
}

export async function createManagedUser(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const nome = textValue(formData, "nome");
  const email = textValue(formData, "email");
  const password = textValue(formData, "password");
  const telegramId = textValue(formData, "telegram_id");

  if (!nome || !email || !password) {
    redirect("/admin?message=Preencha+nome%2C+email+e+senha.&type=error");
  }

  const { data, error } = await adminClient.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
    user_metadata: {
      nome,
    },
  });

  if (error || !data.user) {
    redirect(`/admin?message=${encodeURIComponent(error?.message ?? "Nao foi possivel criar o usuario.")}&type=error`);
  }

  await adminClient
    .from("usuarios")
    .update({
      nome,
      email,
      telegram_id: telegramId || null,
      role: "user",
      acesso_ativo: true,
    })
    .eq("id", data.user.id);

  revalidatePath("/admin");
  redirect("/admin?message=Usuario+criado+com+sucesso.&type=success");
}

export async function updateManagedUser(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const userId = textValue(formData, "user_id");
  const nome = textValue(formData, "nome");
  const email = textValue(formData, "email");
  const telegramId = textValue(formData, "telegram_id");

  await adminClient.from("usuarios").update({ nome, email, telegram_id: telegramId || null }).eq("id", userId);

  revalidatePath("/admin");
  redirect("/admin?message=Dados+do+usuario+atualizados.&type=success");
}

export async function saveLicense(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const licenseId = textValue(formData, "license_id");
  const userId = textValue(formData, "user_id");
  const numeroConta = textValue(formData, "numero_conta");
  const servidor = textValue(formData, "servidor");
  const mt5Password = textValue(formData, "mt5_password");
  const valorInput = textValue(formData, "valor");
  const dataExpiracao = normalizeDateValue(textValue(formData, "data_expiracao"));
  const alavancagem = optionalIntegerValue(formData, "alavancagem");
  const requestedStatus = textValue(formData, "status") || "ativa";
  const nomePlano = textValue(formData, "nome_plano") || "Premium";
  const valor = valorInput ? numberValue(formData, "valor") : (PLAN_DEFAULTS[nomePlano] ?? 297);
  const status = resolveLicenseStatus(requestedStatus, dataExpiracao);

  if (!userId || !numeroConta || !dataExpiracao) {
    redirect("/admin?message=Preencha+usuario%2C+numero+da+conta+e+expiracao.&type=error");
  }

  const { data: existingUser } = await adminClient
    .from("usuarios")
    .select("nome")
    .eq("id", userId)
    .maybeSingle<{ nome: string | null }>();

  const { data: existingAccount } = await adminClient
    .from("contas_trading")
    .select("id")
    .eq("user_id", userId)
    .eq("numero_conta", numeroConta)
    .maybeSingle<{ id: string }>();

  let accountId = existingAccount?.id;

  const accountPayload = {
    numero_conta: numeroConta,
    servidor: servidor || null,
    alavancagem,
  } as const;

  if (!accountId && licenseId) {
    const { data: licenseAccount } = await adminClient
      .from("licencas")
      .select("conta_trading_id")
      .eq("id", licenseId)
      .maybeSingle<{ conta_trading_id: string }>();

    if (licenseAccount?.conta_trading_id) {
      accountId = licenseAccount.conta_trading_id;
      const accountUpdatePayload = mt5Password
        ? { ...accountPayload, mt5_password: mt5Password }
        : accountPayload;

      await adminClient
        .from("contas_trading")
        .update(accountUpdatePayload)
        .eq("id", accountId);
    }
  }

  if (accountId) {
    const accountUpdatePayload = mt5Password
      ? { ...accountPayload, mt5_password: mt5Password }
      : accountPayload;

    const { error: updateAccountError } = await adminClient
      .from("contas_trading")
      .update(accountUpdatePayload)
      .eq("id", accountId);

    if (updateAccountError) {
      redirect(`/admin?message=${encodeURIComponent(updateAccountError.message)}&type=error`);
    }
  } else {
    const { data: createdAccount, error: accountError } = await adminClient
      .from("contas_trading")
      .insert({
        user_id: userId,
        nome_cliente: existingUser?.nome ?? "Cliente Lumitrader",
        corretora: null,
        moeda_codigo: "USD",
        moeda_simbolo: "$",
        mt5_password: mt5Password || null,
        ...accountPayload,
      })
      .select("id")
      .single<{ id: string }>();

    if (accountError || !createdAccount) {
      redirect(`/admin?message=${encodeURIComponent(accountError?.message ?? "Nao foi possivel criar a conta MT5.")}&type=error`);
    }

    accountId = createdAccount.id;
  }

  if (licenseId) {
    const { error: licenseUpdateError } = await adminClient
      .from("licencas")
      .update({
        nome_plano: nomePlano,
        status,
        valor,
        data_expiracao: dataExpiracao,
      })
      .eq("id", licenseId);

    if (licenseUpdateError) {
      redirect(`/admin?message=${encodeURIComponent(licenseUpdateError.message)}&type=error`);
    }
  } else {
    const { error: licenseInsertError } = await adminClient.from("licencas").insert({
      user_id: userId,
      conta_trading_id: accountId,
      nome_plano: nomePlano,
      status,
      valor,
      data_expiracao: dataExpiracao,
    });

    if (licenseInsertError) {
      redirect(`/admin?message=${encodeURIComponent(licenseInsertError.message)}&type=error`);
    }
  }

  revalidatePath("/admin");
  revalidatePath("/dashboard");
  redirect("/admin?message=Licenca+salva+com+sucesso.&type=success");
}

export async function renewLicense30Days(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const licenseId = textValue(formData, "license_id");
  const currentDate = normalizeDateValue(textValue(formData, "current_expiration"));
  await adminClient
    .from("licencas")
    .update({
      data_expiracao: nextExpirationDate(currentDate),
      status: "ativa",
    })
    .eq("id", licenseId);

  revalidatePath("/admin");
  revalidatePath("/dashboard");
  redirect("/admin?message=Licenca+renovada+por+30+dias.&type=success");
}

export async function toggleUserAccess(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const userId = textValue(formData, "user_id");
  const nextState = textValue(formData, "next_state") === "true";

  await adminClient.from("usuarios").update({ acesso_ativo: nextState }).eq("id", userId);

  revalidatePath("/admin");
  revalidatePath("/dashboard");
  redirect("/admin?message=Acesso+do+usuario+atualizado.&type=success");
}

export async function updateLicenseStatus(formData: FormData) {
  await requireAdminUser();
  const adminClient = createAdminClient();

  const licenseId = textValue(formData, "license_id");
  const requestedStatus = textValue(formData, "status");
  const expirationDate = normalizeDateValue(textValue(formData, "data_expiracao"));
  const status = resolveLicenseStatus(requestedStatus, expirationDate);

  await adminClient.from("licencas").update({ status }).eq("id", licenseId);

  revalidatePath("/admin");
  revalidatePath("/dashboard");
  redirect("/admin?message=Status+da+licenca+atualizado.&type=success");
}
</file>

<file path="src/app/admin/page.tsx">
import Link from "next/link";
import { createManagedUser } from "@/app/admin/actions";
import { AdminUsersPanel } from "@/components/admin/admin-users-panel";
import { requireAdminUser } from "@/lib/auth";
import { createAdminClient } from "@/lib/supabase/admin";

const PLAN_OPTIONS = ["Starter", "Pro", "Premium", "Enterprise"];
const LICENSE_STATUS_OPTIONS = [
  { value: "ativa", label: "Ativa" },
  { value: "pendente", label: "Pendente" },
  { value: "bloqueada", label: "Bloqueada" },
  { value: "cancelada", label: "Cancelada" },
  { value: "expirada", label: "Expirada" },
];

type AdminPageProps = {
  searchParams: Promise<{
    message?: string;
    type?: string;
    query?: string;
    plan?: string;
    status?: string;
  }>;
};

type ManagedUser = {
  id: string;
  nome: string | null;
  email: string | null;
  telegram_id: string | null;
  role: "admin" | "user";
  acesso_ativo: boolean;
};

type ManagedLicense = {
  id: string;
  user_id: string;
  nome_plano: string;
  status: string;
  valor: number;
  data_expiracao: string;
  conta_trading_id: string;
};

type ManagedAccount = {
  id: string;
  user_id: string;
  nome_cliente: string;
  numero_conta: string;
  servidor: string | null;
  mt5_password: string | null;
  alavancagem: number | null;
};

function normalizedLicenseStatus(status: string, expirationDate: string) {
  const today = new Date().toISOString().slice(0, 10);
  if (status === "ativa" && expirationDate < today) {
    return "expirada";
  }
  return status;
}

export default async function AdminPage({ searchParams }: AdminPageProps) {
  const { supabase, profile } = await requireAdminUser();
  const params = await searchParams;
  const message = params.message;
  const isSuccess = params.type === "success";
  const query = (params.query ?? "").toLowerCase();
  const planFilter = params.plan ?? "all";
  const statusFilter = params.status ?? "all";

  const [{ data: users }, { data: licenses }, { data: accounts }] = await Promise.all([
    supabase.from("usuarios").select("id, nome, email, telegram_id, role, acesso_ativo").order("criado_em", { ascending: false }),
    supabase.from("licencas").select("id, user_id, nome_plano, status, valor, data_expiracao, conta_trading_id").order("data_expiracao", { ascending: true }),
    supabase.from("contas_trading").select("id, user_id, nome_cliente, numero_conta, servidor, mt5_password, alavancagem").order("criado_em", { ascending: false }),
  ]);

  const expiredIds = (licenses ?? [])
    .filter((license) => normalizedLicenseStatus(license.status, license.data_expiracao) === "expirada" && license.status !== "expirada")
    .map((license) => license.id);

  if (expiredIds.length > 0) {
    const adminClient = createAdminClient();
    await adminClient.from("licencas").update({ status: "expirada" }).in("id", expiredIds);
  }

  const managedUsers = (users ?? []) as ManagedUser[];
  const managedLicenses = ((licenses ?? []) as ManagedLicense[]).map((license) => ({ ...license, status: normalizedLicenseStatus(license.status, license.data_expiracao) }));
  const managedAccounts = (accounts ?? []) as ManagedAccount[];

  const filteredUserIds = new Set(
    managedUsers
      .filter((user) => !query || [user.nome ?? "", user.email ?? "", user.telegram_id ?? ""].some((value) => value.toLowerCase().includes(query)))
      .map((user) => user.id),
  );

  const filteredLicenses = managedLicenses.filter((license) => {
    const account = managedAccounts.find((item) => item.id === license.conta_trading_id);
    const user = managedUsers.find((item) => item.id === license.user_id);
    const matchesQuery = !query || [user?.nome ?? "", user?.email ?? "", account?.numero_conta ?? "", account?.servidor ?? ""].some((value) => value.toLowerCase().includes(query));
    const matchesPlan = planFilter === "all" || license.nome_plano === planFilter;
    const matchesStatus = statusFilter === "all" || license.status === statusFilter;
    return matchesQuery && matchesPlan && matchesStatus;
  });

  const filteredUsers = managedUsers.filter((user) => {
    if (filteredUserIds.has(user.id)) {
      return true;
    }
    return filteredLicenses.some((license) => license.user_id === user.id);
  });

  return (
    <main className="min-h-screen overflow-x-hidden px-4 py-6 text-white sm:px-6 lg:px-8">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-6">
        <section className="glass-panel rounded-[32px] p-6">
          <div className="flex flex-col gap-4 border-b border-white/8 pb-5 lg:flex-row lg:items-center lg:justify-between">
            <div>
              <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Gestao SaaS</p>
              <h1 className="mt-2 text-3xl font-semibold">Painel administrativo do Lumitrader</h1>
              <p className="mt-3 max-w-3xl text-slate-300">Cadastro de usuarios, licencas por conta MT5, filtros reais e edicao centralizada por usuario.</p>
            </div>
            <div className="flex flex-wrap gap-3">
              <div className="rounded-[24px] border border-white/8 bg-white/4 px-4 py-3">
                <p className="text-xs uppercase tracking-[0.22em] text-slate-400">Admin</p>
                <p className="mt-2 text-lg font-semibold">{profile.nome ?? profile.email ?? "Administrador"}</p>
              </div>
              <Link href="/dashboard" className="rounded-[22px] border border-white/10 bg-white/5 px-4 py-3 text-sm font-semibold text-slate-200 transition-colors hover:bg-white/10">Voltar ao dashboard</Link>
            </div>
          </div>

          {message ? <div className={`mt-5 rounded-[20px] border px-4 py-3 text-sm ${isSuccess ? "border-lime-400/20 bg-lime-400/10 text-lime-200" : "border-red-400/20 bg-red-400/10 text-red-200"}`}>{message}</div> : null}

          <form method="get" action="/admin" className="mt-5 grid gap-4 rounded-[24px] border border-white/8 bg-white/4 p-4 md:grid-cols-[1.2fr_0.8fr_0.8fr_auto]">
            <Field label="Buscar" name="query" placeholder="Nome, email, telegram, conta ou servidor" defaultValue={params.query ?? ""} required={false} />
            <SelectField label="Plano" name="plan" options={[{ value: "all", label: "Todos" }, ...PLAN_OPTIONS.map((plan) => ({ value: plan, label: plan }))]} defaultValue={planFilter} required={false} />
            <SelectField label="Status" name="status" options={[{ value: "all", label: "Todos" }, ...LICENSE_STATUS_OPTIONS]} defaultValue={statusFilter} required={false} />
            <div className="flex items-end gap-3"><button className="rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Filtrar</button></div>
          </form>

          <form className="mt-6 rounded-[28px] border border-white/8 bg-white/4 p-5">
            <p className="text-xl font-semibold">Cadastrar usuario</p>
            <div className="mt-4 grid gap-4 md:grid-cols-2">
              <Field label="Nome" name="nome" placeholder="Nome do cliente" />
              <Field label="Email" name="email" type="email" placeholder="cliente@exemplo.com" />
              <Field label="Telegram ID" name="telegram_id" placeholder="123456789" required={false} />
              <Field label="Senha inicial" name="password" type="password" placeholder="Senha provisoria" />
            </div>
            <button formAction={createManagedUser} className="mt-5 rounded-[20px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 font-semibold text-slate-950">Criar usuario SaaS</button>
          </form>
        </section>

        <AdminUsersPanel users={filteredUsers} licenses={filteredLicenses} accounts={managedAccounts} />
      </div>
    </main>
  );
}

function Field({ label, name, placeholder, type = "text", defaultValue, required = true }: { label: string; name: string; placeholder?: string; type?: string; defaultValue?: string; required?: boolean }) {
  return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><input name={name} type={type} placeholder={placeholder} defaultValue={defaultValue} required={required} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none placeholder:text-slate-500" /></label>;
}

function SelectField({ label, name, options, defaultValue, required = true }: { label: string; name: string; options: Array<{ value: string; label: string }>; defaultValue?: string; required?: boolean }) {
  return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><select name={name} defaultValue={defaultValue} required={required} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">{required ? <option value="">Selecione</option> : null}{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>;
}
</file>

<file path="src/app/dashboard/actions.ts">
"use server";

import { revalidatePath } from "next/cache";
import { requireAuthenticatedUser } from "@/lib/auth";
import { createAdminClient } from "@/lib/supabase/admin";

function textValue(formData: FormData, key: string) {
  const value = formData.get(key);
  return typeof value === "string" ? value.trim() : "";
}

function numberValue(formData: FormData, key: string) {
  const raw = textValue(formData, key);
  if (!raw) {
    return null;
  }
  const normalized = raw
    .replace(/\s+/g, "")
    .replace(/\.(?=\d{3}(?:\D|$))/g, "")
    .replace(",", ".");
  const value = Number(normalized);
  return Number.isFinite(value) ? value : null;
}

async function upsertConfig(profileId: string, contaTradingId: string, configId: string, payload: Record<string, unknown>) {
  const adminClient = createAdminClient();

  if (configId) {
    await adminClient.from("configuracoes_sessao").update(payload).eq("id", configId);
    return;
  }

  await adminClient.from("configuracoes_sessao").insert({
    user_id: profileId,
    conta_trading_id: contaTradingId,
    ...payload,
  });
}

export async function saveTradingSettings(formData: FormData) {
  const { profile } = await requireAuthenticatedUser();

  const configId = textValue(formData, "config_id");
  const contaTradingId = textValue(formData, "conta_trading_id");
  const ativo = textValue(formData, "ativo") || profile.ativo_padrao;
  const timeframe = textValue(formData, "timeframe") || profile.timeframe_padrao;
  const modo = textValue(formData, "modo") || "conservador";
  const horarioInicio = textValue(formData, "horario_inicio") || "09:00";
  const horarioFim = textValue(formData, "horario_fim") || "17:00";
  const metaLucro = numberValue(formData, "meta_lucro_diaria") ?? 0;
  const perdaMaxima = numberValue(formData, "perda_maxima_diaria") ?? 0;
  const sistemaLigado = textValue(formData, "current_system_state") === "true";
  const breakevenAtivo = textValue(formData, "breakeven_ativo") === "on";
  const trailingAtivo = textValue(formData, "trailing_stop_ativo") === "on";
  const limiteOperacoesAtivo = textValue(formData, "limite_operacoes_ativo") === "on";
  const limiteOperacoes = limiteOperacoesAtivo ? numberValue(formData, "limite_operacoes_diaria") : null;

  await upsertConfig(profile.id, contaTradingId, configId, {
    ativo,
    timeframe,
    sistema_ligado: sistemaLigado,
    modo,
    breakeven_ativo: breakevenAtivo,
    trailing_stop_ativo: trailingAtivo,
    horario_inicio: `${horarioInicio}:00`,
    horario_fim: `${horarioFim}:00`,
    meta_lucro_diaria: metaLucro,
    perda_maxima_diaria: perdaMaxima,
    limite_operacoes_ativo: limiteOperacoesAtivo,
    limite_operacoes_diaria: limiteOperacoes,
  });

  revalidatePath("/dashboard");
  return;
}

export async function toggleSystemState(formData: FormData) {
  const { profile } = await requireAuthenticatedUser();

  const configId = textValue(formData, "config_id");
  const contaTradingId = textValue(formData, "conta_trading_id");
  const currentState = textValue(formData, "current_state") === "true";
  const ativo = textValue(formData, "ativo") || profile.ativo_padrao;
  const timeframe = textValue(formData, "timeframe") || profile.timeframe_padrao;

  await upsertConfig(profile.id, contaTradingId, configId, {
    ativo,
    timeframe,
    sistema_ligado: !currentState,
  });

  revalidatePath("/dashboard");
  return;
}

async function submitTradeCommandWithAction(
  action: "buy" | "sell" | "close" | "partial",
  formData: FormData,
) {
  const { profile } = await requireAuthenticatedUser();
  const adminClient = createAdminClient();

  const contaTradingId = textValue(formData, "conta_trading_id");
  const ativo = textValue(formData, "ativo") || profile.ativo_padrao;
  const timeframe = textValue(formData, "timeframe") || profile.timeframe_padrao;
  const lote = numberValue(formData, "lote");
  const stopLoss = numberValue(formData, "stop_loss");
  const takeProfit = numberValue(formData, "take_profit");
  const ticketReferencia = textValue(formData, "ticket_referencia");

  const tipo =
    action === "buy"
      ? "open_buy"
      : action === "sell"
        ? "open_sell"
        : action === "partial"
          ? "partial_close_position"
          : "close_position";

  if (!contaTradingId || !tipo) {
    return;
  }

  const payload = {
    origem: "dashboard_manual",
    closeFraction: tipo === "partial_close_position" ? 0.5 : undefined,
  };

  const { error } = await adminClient.from("comandos_trading").insert({
    user_id: profile.id,
    conta_trading_id: contaTradingId,
    ativo,
    timeframe,
    tipo,
    lote: tipo === "close_position" || tipo === "partial_close_position" ? null : lote,
    stop_loss: tipo === "close_position" || tipo === "partial_close_position" ? null : stopLoss,
    take_profit: tipo === "close_position" || tipo === "partial_close_position" ? null : takeProfit,
    ticket_referencia: ticketReferencia || null,
    payload,
  });

  if (error) {
    throw new Error(error.message);
  }

  revalidatePath("/dashboard");
  return;
}

export async function submitBuyCommand(formData: FormData) {
  return submitTradeCommandWithAction("buy", formData);
}

export async function submitSellCommand(formData: FormData) {
  return submitTradeCommandWithAction("sell", formData);
}

export async function submitCloseCommand(formData: FormData) {
  return submitTradeCommandWithAction("close", formData);
}

export async function submitPartialCloseCommand(formData: FormData) {
  return submitTradeCommandWithAction("partial", formData);
}

export async function submitTradeCommand(formData: FormData) {
  const action = textValue(formData, "trade_action");
  const normalizedAction = action === "sell" ? "sell" : action === "close" ? "close" : "buy";
  return submitTradeCommandWithAction(normalizedAction, formData);
}
</file>

<file path="src/components/dashboard/dashboard-realtime.tsx">
"use client";  import { useEffect, useEffectEvent, useState, useTransition } from "react"; import type { AppUser } from "@/lib/auth"; import { saveTradingSettings, submitTradeCommand, toggleSystemState } from "@/app/dashboard/actions"; import { createClient } from "@/lib/supabase/client"; import { TradingViewChart } from "@/components/dashboard/tradingview-chart"; import type {   DashboardAccount,   DashboardConfig,   DashboardHistoryFilters,   DashboardHistoryRow,   DashboardInsightBundle,   DashboardLicense,   DashboardOpenOperation,   DashboardStats,   DashboardCommandStatus, } from "@/app/dashboard/page";  type SelectableAccount = DashboardAccount & {   license: DashboardLicense; };  type DashboardRealtimeProps = {   profile: AppUser;   accounts: SelectableAccount[];   selectedAccountId: string;   selectedLicense: DashboardLicense;   account: DashboardAccount;   config: DashboardConfig;   stats: DashboardStats;   history: DashboardHistoryRow[];   historyFilters: DashboardHistoryFilters;   insightBundle: DashboardInsightBundle;   openOperation: DashboardOpenOperation | null;   commandStatuses: DashboardCommandStatus[]; };  function formatAccountCurrency(value: number, account: DashboardAccount) {   const currencyCode = account.moeda_codigo?.toUpperCase?.() || "USD";   const locale = currencyCode === "BRL" ? "pt-BR" : currencyCode === "EUR" ? "de-DE" : "en-US";    try {     return new Intl.NumberFormat(locale, {       style: "currency",       currency: currencyCode,       maximumFractionDigits: 2,     }).format(value);   } catch {     return `${account.moeda_simbolo} ${value.toFixed(2)}`;   } }  function formatLicenseCurrency(value: number) {   return new Intl.NumberFormat("pt-BR", {     style: "currency",     currency: "BRL",     maximumFractionDigits: 2,   }).format(value); }  function formatTimeRange(start: string, end: string) {   return `${start.slice(0, 5)}-${end.slice(0, 5)}`; }  function formatDate(date: string) {   return new Intl.DateTimeFormat("pt-BR").format(new Date(`${date}T00:00:00`)); }  export function DashboardRealtime({   profile,   accounts,   selectedAccountId,   selectedLicense,   account,   config,   stats,   history,   historyFilters,   insightBundle,   openOperation,   commandStatuses, }: DashboardRealtimeProps) {   const supabase = createClient();   const [, startTransition] = useTransition();   const [liveAccount, setLiveAccount] = useState(account);   const [liveConfig, setLiveConfig] = useState(config);   const [liveStats, setLiveStats] = useState(stats);   const [liveHistory, setLiveHistory] = useState(history);   const [liveNow, setLiveNow] = useState(() => new Date());   const [liveInsightBundle, setLiveInsightBundle] = useState(insightBundle);   const [liveOpenOperation, setLiveOpenOperation] = useState(openOperation);   const [liveCommandStatuses, setLiveCommandStatuses] = useState(commandStatuses);    const metrics = [     { label: "Lucro do Dia", value: formatAccountCurrency(Math.max(liveStats.lucro_total, 0), liveAccount), tone: "text-lime-400" },     { label: "Perda do Dia", value: `-${formatAccountCurrency(Math.abs(liveStats.prejuizo_total), liveAccount)}`, tone: "text-red-400" },     { label: "Win Rate", value: `${liveStats.win_rate}%`, tone: "text-white" },     { label: "Drawdown", value: `${liveStats.drawdown}%`, tone: "text-white" },   ];    const refreshSnapshot = useEffectEvent(async () => {     const [{ data: nextAccount }, { data: nextConfig }, { data: nextStats }, { data: nextOperations }, { data: nextCommands }] = await Promise.all([       supabase         .from("contas_trading")         .select("id, user_id, nome_cliente, numero_conta, corretora, moeda_codigo, moeda_simbolo, saldo_atual, equity, margem_livre, nivel_margem, ativo, atualizado_em, server_time, mercado_snapshot, insight_atual, ultima_sincronizacao")         .eq("id", selectedAccountId)         .maybeSingle<DashboardAccount>(),       supabase         .from("configuracoes_sessao")         .select("id, conta_trading_id, ativo, timeframe, sistema_ligado, modo, breakeven_ativo, trailing_stop_ativo, horario_inicio, horario_fim, meta_lucro_diaria, perda_maxima_diaria, limite_operacoes_ativo, limite_operacoes_diaria")         .eq("conta_trading_id", selectedAccountId)         .order("atualizado_em", { ascending: false })         .limit(1)         .maybeSingle<DashboardConfig>(),       supabase         .from("estatisticas")         .select("conta_trading_id, operacoes_total, vitorias, derrotas, win_rate, lucro_total, prejuizo_total, drawdown, melhor_operacao, pior_operacao")         .eq("conta_trading_id", selectedAccountId)         .order("periodo", { ascending: false })         .limit(1)         .maybeSingle<DashboardStats>(),       supabase         .from("operacoes")         .select("id, direcao, status, lote, preco_entrada, preco_saida, stop_loss, take_profit, lucro_prejuizo, aberta_em, fechada_em, timeframe, ativo, validacao_ia")         .eq("conta_trading_id", selectedAccountId)         .order("aberta_em", { ascending: false })         .limit(50),       supabase         .from("comandos_trading")         .select("id, tipo, status, erro, solicitado_em, processado_em")         .eq("conta_trading_id", selectedAccountId)         .order("solicitado_em", { ascending: false })         .limit(5),     ]);      startTransition(() => {       if (nextAccount) {         setLiveAccount(nextAccount);         const snapshotNotes = Array.isArray(nextAccount.mercado_snapshot?.notes) ? nextAccount.mercado_snapshot.notes : [];         const snapshotCandles = Array.isArray(nextAccount.mercado_snapshot?.candles) ? nextAccount.mercado_snapshot.candles : [];         setLiveInsightBundle((current) => ({           summary: nextAccount.insight_atual ?? current.summary,           notes: snapshotNotes.length > 0 ? snapshotNotes : current.notes,           candles: snapshotCandles.length > 0 ? snapshotCandles : current.candles,         }));       }       if (nextConfig) {         setLiveConfig(nextConfig);       }       if (nextStats) {         setLiveStats(nextStats);       }       if (nextOperations) {         const nextOpenOperation = nextOperations.find((operation) => operation.status === "aberta");         setLiveOpenOperation(nextOpenOperation ? {           id: nextOpenOperation.id,           direction: nextOpenOperation.direcao === "compra" ? "buy" : "sell",           lot: Number(nextOpenOperation.lote),           entryPrice: Number(nextOpenOperation.preco_entrada),           stopLoss: nextOpenOperation.stop_loss != null ? Number(nextOpenOperation.stop_loss) : null,           takeProfit: nextOpenOperation.take_profit != null ? Number(nextOpenOperation.take_profit) : null,           openedAt: nextOpenOperation.aberta_em,           timeframe: nextOpenOperation.timeframe,           symbol: nextOpenOperation.ativo,           profitLoss: Number(nextOpenOperation.lucro_prejuizo ?? 0),         } : null);         const nextRows: Array<DashboardHistoryRow | null> = nextOperations.map((operation) => {           const resultValue = Number(operation.lucro_prejuizo ?? 0);           if (operation.status === "aberta") {             return null;           }            return {             id: operation.id,             time: new Intl.DateTimeFormat("pt-BR", {               hour: "2-digit",               minute: "2-digit",               hour12: false,             }).format(new Date(operation.fechada_em ?? operation.aberta_em)),             type: operation.direcao === "compra" ? "Compra" : "Venda",             lot: Number(operation.lote).toFixed(2),             entry: Number(operation.preco_entrada).toFixed(2),             result: `${resultValue >= 0 ? "+" : "-"}${Math.abs(resultValue).toFixed(2)}`,             resultTone: resultValue >= 0 ? "text-lime-400" : "text-red-400",           };         });         setLiveHistory(nextRows.filter((row): row is DashboardHistoryRow => row !== null));          const latestTelemetry = nextOperations.find((operation) => operation.validacao_ia);         if (latestTelemetry?.validacao_ia) {           setLiveInsightBundle((current) => ({             summary: typeof latestTelemetry.validacao_ia.ai?.summary === "string" ? latestTelemetry.validacao_ia.ai.summary : current.summary,             notes: Array.isArray(latestTelemetry.validacao_ia.market?.notes) ? latestTelemetry.validacao_ia.market.notes : current.notes,             candles: Array.isArray(latestTelemetry.validacao_ia.market?.candles) ? latestTelemetry.validacao_ia.market.candles : current.candles,           }));         }       }       if (nextCommands) {         setLiveCommandStatuses(nextCommands as DashboardCommandStatus[]);       }     });   });    useEffect(() => {     const interval = window.setInterval(() => setLiveNow(new Date()), 1000);     return () => window.clearInterval(interval);   }, []);    useEffect(() => {     const channel = supabase       .channel(`lumitrader-account-${selectedAccountId}`)       .on("postgres_changes", { event: "*", schema: "public", table: "contas_trading", filter: `id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .on("postgres_changes", { event: "*", schema: "public", table: "configuracoes_sessao", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .on("postgres_changes", { event: "*", schema: "public", table: "estatisticas", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .on("postgres_changes", { event: "*", schema: "public", table: "operacoes", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .on("postgres_changes", { event: "*", schema: "public", table: "licencas", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .on("postgres_changes", { event: "*", schema: "public", table: "comandos_trading", filter: `conta_trading_id=eq.${selectedAccountId}` }, () => {         void refreshSnapshot();       })       .subscribe();      return () => {       void supabase.removeChannel(channel);     };   }, [selectedAccountId, supabase]);    const floatingProfit = (liveAccount.equity ?? 0) - (liveAccount.saldo_atual ?? 0);   const isSystemOnline = liveConfig.sistema_ligado && Boolean(liveAccount.ativo);   const brasiliaClockLabel = new Intl.DateTimeFormat("pt-BR", {     day: "2-digit",     month: "2-digit",     year: "numeric",     hour: "2-digit",     minute: "2-digit",     second: "2-digit",     hour12: false,     timeZone: "America/Sao_Paulo",   }).format(liveNow);   const serverClock = new Date(liveNow.getTime() + 6 * 60 * 60 * 1000);   const serverClockLabel = new Intl.DateTimeFormat("pt-BR", {     day: "2-digit",     month: "2-digit",     year: "numeric",     hour: "2-digit",     minute: "2-digit",     second: "2-digit",     hour12: false,     timeZone: "America/Sao_Paulo",   }).format(serverClock);   const latestCommand = liveCommandStatuses[0] ?? null;   const commandTypeLabel = latestCommand     ? latestCommand.tipo === "open_buy"       ? "Compra"       : latestCommand.tipo === "open_sell"         ? "Venda"         : "Fechamento"     : null;   const commandStatusLabel = latestCommand     ? latestCommand.status === "pending"       ? "Na fila"       : latestCommand.status === "processing"         ? "Executando na VPS"         : latestCommand.status === "executed"           ? "Executado"           : latestCommand.status === "failed"             ? "Falhou"             : "Cancelado"     : "Sem comandos recentes";   const commandStatusTone = latestCommand     ? latestCommand.status === "executed"       ? "text-lime-300"       : latestCommand.status === "failed"         ? "text-red-300"         : "text-amber-200"     : "text-slate-300";    return (     <div className="mt-5 grid gap-6">       <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-5">         <HeaderPill label="Ativo" value={liveConfig.ativo} />         <HeaderPill label="Modo" value={liveConfig.modo === "agressivo" ? "Agressivo" : "Conservador"} />         <HeaderPill label="Conta" value={liveAccount.numero_conta} />         <HeaderPill label="Licenca" value={selectedLicense.nome_plano} />         <HeaderPill label="Status" value={isSystemOnline ? "ONLINE" : "PAUSADO"} online={isSystemOnline} />       </div>        <div className="rounded-[28px] border border-white/8 bg-white/4 p-4">         <div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">           <div>             <p className="font-mono text-xs uppercase tracking-[0.26em] text-cyan-200/70">Seletor de licenca</p>             <p className="mt-2 text-sm text-slate-300">Cada conta MT5 opera de forma independente com limites, saldo, equity e metricas proprios.</p>           </div>           <form action="/dashboard" className="flex flex-col gap-3 sm:flex-row sm:items-center">             <select name="account" defaultValue={selectedAccountId} onChange={(event) => event.currentTarget.form?.requestSubmit()} className="rounded-[18px] border border-white/10 bg-slate-950/60 px-4 py-3 text-white outline-none">               {accounts.map((item) => (                 <option key={item.id} value={item.id}>                   {item.numero_conta} · {item.nome_cliente} · expira {formatDate(item.license.data_expiracao)}                 </option>               ))}             </select>           </form>         </div>       </div>        <div className="grid gap-4 xl:grid-cols-[0.95fr_1.05fr]">         <div className="space-y-4">           <div className="grid gap-4 sm:grid-cols-2 md:grid-cols-3">             <InfoCard eyebrow="Cliente" title={liveAccount.nome_cliente ?? profile.nome ?? "Trader"} detail={`Email: ${profile.email ?? "nao informado"}`} />             <InfoCard eyebrow="Licenca" title={`${selectedLicense.status.toUpperCase()} ate ${formatDate(selectedLicense.data_expiracao)}`} detail={`Valor ${formatLicenseCurrency(selectedLicense.valor)}`} />             <InfoCard eyebrow="Janela Operacional" title={formatTimeRange(liveConfig.horario_inicio, liveConfig.horario_fim)} detail={`${liveConfig.breakeven_ativo ? "Breakeven ON" : "Breakeven OFF"} / ${liveConfig.trailing_stop_ativo ? "Trailing ON" : "Trailing OFF"}`} />           </div>            <div className="grid gap-4 md:grid-cols-[1.08fr_0.92fr]">             <div className="glass-panel rounded-[28px] p-4 sm:p-5">               <div className="flex flex-wrap items-center justify-between gap-3">                 <div>                   <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Controle Central</p>                   <h2 className="mt-1 text-2xl font-semibold">{isSystemOnline ? "Auto trader armado" : "Sistema aguardando backend operacional"}</h2>                 </div>                 <div className="rounded-[18px] border border-lime-400/30 bg-lime-400/10 px-3 py-2 text-xs text-lime-300">                   <div>Servidor: {serverClockLabel}</div>                   <div>Brasilia: {brasiliaClockLabel}</div>                 </div>               </div>                <div className="mt-5 grid gap-3 sm:grid-cols-2">                 <ToggleCard label="Breakeven" value={liveConfig.breakeven_ativo ? "ON" : "OFF"} />                 <ToggleCard label="Trailing Stop" value={liveConfig.trailing_stop_ativo ? "ON" : "OFF"} />               </div>                <form className="mt-5">                 <input type="hidden" name="config_id" value={liveConfig.id ?? ""} />                 <input type="hidden" name="conta_trading_id" value={selectedAccountId} />                 <input type="hidden" name="current_state" value={String(liveConfig.sistema_ligado)} />                 <input type="hidden" name="ativo" value={liveConfig.ativo} />                 <input type="hidden" name="timeframe" value={liveConfig.timeframe} />                 <button formAction={toggleSystemState} className="flex w-full items-center justify-center gap-3 rounded-[24px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-6 py-5 text-2xl font-semibold text-slate-950 shadow-[0_20px_50px_rgba(157,232,51,0.28)] transition-transform duration-200 hover:-translate-y-0.5">                   <span className="text-3xl">{liveConfig.sistema_ligado ? "■" : "▶"}</span>                   {liveConfig.sistema_ligado ? "Pausar" : "Play"}                 </button>               </form>                <div className="mt-5 grid gap-3 rounded-[24px] border border-white/8 bg-white/4 p-4 sm:grid-cols-2 md:grid-cols-3">                 <QuickFact label="Timeframe" value={liveConfig.timeframe} />                 <QuickFact label="Meta diaria" value={formatAccountCurrency(liveConfig.meta_lucro_diaria, liveAccount)} />                 <QuickFact label="Perda maxima" value={formatAccountCurrency(liveConfig.perda_maxima_diaria, liveAccount)} />               </div>                <div className="mt-4 rounded-[24px] border border-white/8 bg-slate-950/35 p-4">                 <p className="text-sm text-slate-300">Limite de operacoes por dia</p>                 <p className="mt-2 text-xl font-semibold">{liveConfig.limite_operacoes_ativo ? String(liveConfig.limite_operacoes_diaria ?? 0) : "Desativado"}</p>               </div>                <div className="mt-4 rounded-[24px] border border-white/8 bg-slate-950/35 p-4">                 <p className="text-sm text-slate-300">Canal operacional MT5</p>                 <p className={`mt-2 text-lg font-semibold ${commandStatusTone}`}>{commandStatusLabel}</p>                 <p className="mt-1 text-sm text-slate-400">                   {liveAccount.ultima_sincronizacao                     ? `Ultima leitura: ${new Intl.DateTimeFormat("pt-BR", {                         day: "2-digit",                         month: "2-digit",                         year: "numeric",                         hour: "2-digit",                         minute: "2-digit",                         second: "2-digit",                         hour12: false,                       }).format(new Date(liveAccount.ultima_sincronizacao))}`                     : "Aguardando primeira sincronizacao da VPS."}                 </p>                 {latestCommand ? (                   <p className="mt-2 text-sm text-slate-300">                     Ultimo comando: {commandTypeLabel} em {new Intl.DateTimeFormat("pt-BR", {                       day: "2-digit",                       month: "2-digit",                       year: "numeric",                       hour: "2-digit",                       minute: "2-digit",                       second: "2-digit",                       hour12: false,                     }).format(new Date(latestCommand.solicitado_em))}                   </p>                 ) : null}                 {latestCommand?.erro ? <p className="mt-2 text-sm text-red-300">{latestCommand.erro}</p> : null}               </div>             </div>              <div className="glass-panel rounded-[28px] p-4 sm:p-5">               <div className="flex items-center justify-between">                 <div>                   <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Conta de Trading</p>                   <h2 className="mt-1 text-2xl font-semibold">{liveAccount.corretora ?? "Corretora nao informada"}</h2>                 </div>                 <span className="rounded-full bg-cyan-400/10 px-3 py-1 text-sm text-cyan-100">{liveAccount.numero_conta}</span>               </div>                <div className="mt-5 space-y-3 rounded-[24px] border border-white/8 bg-slate-950/40 p-4">                 <DataRow label="Nome do cliente" value={liveAccount.nome_cliente} />                 <DataRow label="Moeda" value={`${liveAccount.moeda_simbolo} / ${liveAccount.moeda_codigo}`} />                 <DataRow label="Saldo atual" value={formatAccountCurrency(liveAccount.saldo_atual ?? 0, liveAccount)} />                 <DataRow label="Equity" value={formatAccountCurrency(liveAccount.equity ?? 0, liveAccount)} />                 <DataRow label="Margem livre" value={formatAccountCurrency(liveAccount.margem_livre ?? 0, liveAccount)} />               </div>                <div className="mt-5 rounded-[24px] border border-lime-400/20 bg-lime-400/8 p-4">                 <p className="text-sm text-lime-200/80">Resultado flutuante</p>                 <p className="mt-2 text-4xl font-bold text-lime-300">{floatingProfit >= 0 ? "+" : "-"}{formatAccountCurrency(Math.abs(floatingProfit), liveAccount)}</p>                 <p className="mt-2 text-sm text-slate-300">Nivel de margem: {liveAccount.nivel_margem ?? 0}%.</p>               </div>             </div>           </div>            <div className="glass-panel rounded-[28px] p-5">             <p className="font-mono text-xs uppercase tracking-[0.28em] text-cyan-200/70">Parametros desta conta</p>             <form className="mt-4 grid gap-4 md:grid-cols-2">               <input type="hidden" name="config_id" value={liveConfig.id ?? ""} />               <input type="hidden" name="conta_trading_id" value={selectedAccountId} />               <input type="hidden" name="current_system_state" value={String(liveConfig.sistema_ligado)} />               <SettingsField label="Ativo" name="ativo" defaultValue={liveConfig.ativo} />               <label className="grid gap-2">                 <span className="text-sm text-slate-300">Timeframe</span>                 <select name="timeframe" defaultValue={liveConfig.timeframe} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">                   {['M1', 'M5', 'M15', 'M30', 'H1'].map((timeframe) => <option key={timeframe} value={timeframe}>{timeframe}</option>)}                 </select>               </label>               <label className="grid gap-2">                 <span className="text-sm text-slate-300">Modo</span>                 <select name="modo" defaultValue={liveConfig.modo} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none">                   <option value="agressivo">Agressivo</option>                   <option value="conservador">Conservador</option>                 </select>               </label>               <SettingsField label="Horario inicio" name="horario_inicio" type="time" defaultValue={liveConfig.horario_inicio.slice(0, 5)} />               <SettingsField label="Horario fim" name="horario_fim" type="time" defaultValue={liveConfig.horario_fim.slice(0, 5)} />               <SettingsField label="Meta diaria" name="meta_lucro_diaria" type="number" defaultValue={String(liveConfig.meta_lucro_diaria)} />               <SettingsField label="Perda maxima diaria" name="perda_maxima_diaria" type="number" defaultValue={String(liveConfig.perda_maxima_diaria)} />               <SettingsField label="Limite de operacoes" name="limite_operacoes_diaria" type="number" defaultValue={String(liveConfig.limite_operacoes_diaria ?? "")} />               <div className="grid gap-3 rounded-[18px] border border-white/8 bg-slate-950/35 p-4">                 <CheckBox label="Breakeven ativo" name="breakeven_ativo" defaultChecked={liveConfig.breakeven_ativo} />                 <CheckBox label="Trailing stop ativo" name="trailing_stop_ativo" defaultChecked={liveConfig.trailing_stop_ativo} />                 <CheckBox label="Limite de operacoes ativo" name="limite_operacoes_ativo" defaultChecked={liveConfig.limite_operacoes_ativo} />               </div>               <div className="flex justify-stretch md:col-span-2 md:justify-end">                 <button formAction={saveTradingSettings} className="rounded-[18px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 text-sm font-semibold text-slate-950">                   Salvar parametros                 </button>               </div>             </form>           </div>         </div>          <div className="space-y-4">           <div className="glass-panel rounded-[28px] p-4 sm:p-5">             <div className="flex items-center justify-between">               <div>                 <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Grafico em Tempo Real</p>                 <h2 className="mt-1 text-2xl font-semibold">Painel tecnico {liveConfig.ativo}</h2>               </div>               <span className="w-fit rounded-full border border-cyan-400/20 bg-cyan-400/10 px-3 py-1 text-xs uppercase tracking-[0.25em] text-cyan-200">{liveConfig.timeframe}</span>             </div>              <TradingViewChart initialSymbol={liveConfig.ativo} initialTimeframe={liveConfig.timeframe} />           </div>             <div className="glass-panel rounded-[28px] p-5">             <div className="flex items-center justify-between gap-3">               <div>                 <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Operacao Atual</p>                 <h2 className="mt-1 text-2xl font-semibold">{liveOpenOperation ? `${liveOpenOperation.direction === "buy" ? "Compra" : "Venda"} ${liveOpenOperation.symbol}` : "Nenhuma operacao aberta"}</h2>               </div>               {liveOpenOperation ? <span className={`rounded-full px-3 py-1 text-sm font-semibold ${floatingProfit >= 0 ? "bg-lime-400/12 text-lime-300" : "bg-red-400/12 text-red-300"}`}>{floatingProfit >= 0 ? "No lucro" : "No prejuizo"}</span> : null}             </div>              {liveOpenOperation ? (               <div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">                 <StatRow label="Direcao" value={liveOpenOperation.direction === "buy" ? "Compra" : "Venda"} />                 <StatRow label="Lote" value={liveOpenOperation.lot.toFixed(2)} />                 <StatRow label="Entrada" value={formatAccountCurrency(liveOpenOperation.entryPrice, liveAccount)} />                 <StatRow label="Timeframe" value={liveOpenOperation.timeframe} />                 <StatRow label="P/L em tempo real" value={`${floatingProfit >= 0 ? "+" : "-"}${formatAccountCurrency(Math.abs(floatingProfit), liveAccount)}`} valueTone={floatingProfit >= 0 ? "text-lime-400" : "text-red-400"} />                 <StatRow label="PnL" value={`${liveAccount.saldo_atual ? ((floatingProfit / Math.max(liveAccount.saldo_atual, 1)) * 100).toFixed(2) : "0.00"}%`} valueTone={floatingProfit >= 0 ? "text-lime-400" : "text-red-400"} />                 <StatRow label="Stop Loss" value={liveOpenOperation.stopLoss != null ? formatAccountCurrency(liveOpenOperation.stopLoss, liveAccount) : "--"} />                 <StatRow label="Take Profit" value={liveOpenOperation.takeProfit != null ? formatAccountCurrency(liveOpenOperation.takeProfit, liveAccount) : "--"} />               </div>             ) : (               <div className="mt-5 rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-400">Assim que houver uma posicao aberta nesta conta, o painel mostra direcao, lote, entrada, P/L e PnL em tempo real.</div>             )}           </div>             <div className="glass-panel rounded-[28px] p-5">             <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Execucao Manual</p>             <form className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-[1fr_1fr_1fr_auto_auto_auto]">               <input type="hidden" name="conta_trading_id" value={selectedAccountId} />               <input type="hidden" name="ativo" value={liveConfig.ativo} />               <input type="hidden" name="timeframe" value={liveConfig.timeframe} />               <input type="hidden" name="ticket_referencia" value={liveOpenOperation?.id ?? ""} />               <SettingsField label="Lote" name="lote" type="number" defaultValue={liveOpenOperation ? liveOpenOperation.lot.toFixed(2) : "0.10"} />               <SettingsField label="Stop Loss" name="stop_loss" type="number" defaultValue={liveOpenOperation?.stopLoss != null ? String(liveOpenOperation.stopLoss) : ""} />               <SettingsField label="Take Profit" name="take_profit" type="number" defaultValue={liveOpenOperation?.takeProfit != null ? String(liveOpenOperation.takeProfit) : ""} />               <div className="flex items-end"><button formAction={submitTradeCommand} name="trade_action" value="buy" className="w-full rounded-[18px] bg-lime-400/12 px-4 py-3 text-sm font-semibold text-lime-200">Comprar</button></div>               <div className="flex items-end"><button formAction={submitTradeCommand} name="trade_action" value="sell" className="w-full rounded-[18px] bg-red-400/12 px-4 py-3 text-sm font-semibold text-red-200">Vender</button></div>               <div className="flex items-end"><button formAction={submitTradeCommand} name="trade_action" value="close" className="w-full rounded-[18px] border border-white/10 bg-white/5 px-4 py-3 text-sm font-semibold text-slate-100">Fechar posicao</button></div>             </form>           </div>            <div className="grid gap-4 sm:grid-cols-2">             {metrics.map((item) => (               <div key={item.label} className="glass-panel rounded-[24px] px-5 py-4">                 <p className="text-sm text-slate-300">{item.label}</p>                 <p className={`mt-4 text-3xl font-bold ${item.tone}`}>{item.value}</p>               </div>             ))}           </div>            <div className="glass-panel rounded-[32px] p-5">             <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Insights Operacionais</p>             <div className="mt-5 grid gap-3">               {liveInsightBundle.summary ? (                 <div className="rounded-[24px] border border-cyan-400/20 bg-cyan-400/10 p-4 text-slate-100">{liveInsightBundle.summary}</div>               ) : (                 <div className="rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-400">Aguardando primeiro ciclo real do backend operacional para esta conta.</div>               )}               {liveInsightBundle.notes.map((insight) => (                 <div key={insight} className="rounded-[24px] border border-white/8 bg-white/4 p-4 text-slate-100"><span className="mr-3 text-lime-300">✓</span>{insight}</div>               ))}             </div>           </div>         </div>       </div>        <section className="grid gap-6 xl:grid-cols-[0.95fr_1.05fr]">         <div className="glass-panel rounded-[32px] p-5">           <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Estatisticas da conta selecionada</p>           <div className="mt-5 space-y-4">             <StatRow label="Operacoes no periodo" value={String(liveStats.operacoes_total)} />             <StatRow label="Vitorias" value={String(liveStats.vitorias)} />             <StatRow label="Derrotas" value={String(liveStats.derrotas)} valueTone="text-red-400" />             <StatRow label="Maior ganho" value={formatAccountCurrency(liveStats.melhor_operacao, liveAccount)} />             <StatRow label="Maior perda" value={formatAccountCurrency(liveStats.pior_operacao, liveAccount)} valueTone="text-red-400" />             <StatRow label="Drawdown atual" value={`${liveStats.drawdown}%`} />           </div>         </div>          <div className="glass-panel rounded-[32px] p-5">           <div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between">             <div>               <p className="font-mono text-xs uppercase tracking-[0.3em] text-cyan-200/70">Historico de Operacoes</p>               <h2 className="mt-1 text-2xl font-semibold">Timeline operacional da conta selecionada</h2>             </div>             <p className="text-sm text-slate-400">Licenca e dados desta conta sao atualizados em tempo real.</p>           </div>            <form action="/dashboard" className="mt-5 grid gap-3 rounded-[24px] border border-white/8 bg-white/4 p-4 md:grid-cols-[1fr_1fr_0.8fr_0.8fr_auto]">             <input type="hidden" name="account" value={selectedAccountId} />             <SettingsField label="De" name="from" type="date" defaultValue={historyFilters.from} />             <SettingsField label="Ate" name="to" type="date" defaultValue={historyFilters.to} />             <label className="grid gap-2"><span className="text-sm text-slate-300">Tipo</span><select name="type" defaultValue={historyFilters.type} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none"><option value="all">Todos</option><option value="compra">Compra</option><option value="venda">Venda</option></select></label>             <label className="grid gap-2"><span className="text-sm text-slate-300">Resultado</span><select name="result" defaultValue={historyFilters.result} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none"><option value="all">Todos</option><option value="gain">Ganho</option><option value="loss">Perda</option></select></label>             <div className="flex items-end"><button className="w-full rounded-[18px] border border-cyan-400/30 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">Filtrar</button></div>           </form>            <div className="mt-5 overflow-x-auto rounded-[28px] border border-white/8">             <table className="min-w-[720px] w-full border-separate border-spacing-0 overflow-hidden">               <thead className="bg-white/6 text-left text-sm uppercase tracking-[0.22em] text-slate-400">                 <tr>                   <th className="px-4 py-4">Hora</th>                   <th className="px-4 py-4">Tipo</th>                   <th className="px-4 py-4">Lote</th>                   <th className="px-4 py-4">Entrada</th>                   <th className="px-4 py-4">Resultado</th>                 </tr>               </thead>               <tbody className="bg-slate-950/30">                 {liveHistory.length > 0 ? (                   liveHistory.map((row) => (                     <tr key={row.id} className="text-base even:bg-white/3">                       <td className="px-4 py-4 text-slate-300">{row.time}</td>                       <td className="px-4 py-4">{row.type}</td>                       <td className="px-4 py-4">{row.lot}</td>                       <td className="px-4 py-4 text-slate-300">{row.entry}</td>                       <td className={`px-4 py-4 font-semibold ${row.resultTone}`}>{liveAccount.moeda_simbolo} {row.result}</td>                     </tr>                   ))                 ) : (                   <tr>                     <td colSpan={5} className="px-4 py-8 text-center text-slate-400">Nenhuma operacao encontrada para os filtros selecionados.</td>                   </tr>                 )}               </tbody>             </table>           </div>         </div>       </section>     </div>   ); }  function HeaderPill({ label, value, online = false }: { label: string; value: string; online?: boolean }) {   return <div className="rounded-[24px] border border-white/8 bg-white/4 px-4 py-3"><p className="text-xs uppercase tracking-[0.22em] text-slate-400">{label}</p><div className="mt-2 flex items-center gap-2 text-lg font-semibold">{online ? <span className="h-2.5 w-2.5 rounded-full bg-lime-400 shadow-[0_0_18px_rgba(157,232,51,0.9)]" /> : null}<span>{value}</span></div></div>; }  function InfoCard({ eyebrow, title, detail }: { eyebrow: string; title: string; detail: string }) {   return <div className="rounded-[24px] border border-white/8 bg-white/4 p-4"><p className="font-mono text-xs uppercase tracking-[0.26em] text-cyan-200/70">{eyebrow}</p><p className="mt-3 text-xl font-semibold">{title}</p><p className="mt-2 text-sm text-slate-400">{detail}</p></div>; }  function ToggleCard({ label, value }: { label: string; value: string }) {   return <div className="flex items-center justify-between rounded-[24px] border border-white/8 bg-slate-950/40 px-4 py-3"><span className="text-lg">{label}</span><span className="rounded-full bg-lime-400/12 px-3 py-1 text-sm font-semibold text-lime-300">{value}</span></div>; }  function QuickFact({ label, value }: { label: string; value: string }) {   return <div><p className="text-xs uppercase tracking-[0.2em] text-slate-500">{label}</p><p className="mt-2 text-lg font-semibold">{value}</p></div>; }  function DataRow({ label, value }: { label: string; value: string }) {   return <div className="flex items-center justify-between border-b border-white/6 pb-3 last:border-b-0 last:pb-0"><span className="text-slate-400">{label}</span><span className="font-semibold text-right">{value}</span></div>; }  function StatRow({ label, value, valueTone = "text-white" }: { label: string; value: string; valueTone?: string }) {   return <div className="flex items-center justify-between rounded-[24px] border border-white/8 bg-white/4 px-4 py-4"><span className="text-slate-300">{label}</span><span className={`text-xl font-semibold ${valueTone}`}>{value}</span></div>; }  function SettingsField({ label, name, defaultValue, type = "text" }: { label: string; name: string; defaultValue: string; type?: string }) {   return <label className="grid gap-2"><span className="text-sm text-slate-300">{label}</span><input name={name} type={type} defaultValue={defaultValue} className="rounded-[18px] border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none" /></label>; }  function CheckBox({ label, name, defaultChecked }: { label: string; name: string; defaultChecked: boolean }) {   return <label className="flex items-center gap-3 text-sm text-slate-200"><input type="checkbox" name={name} defaultChecked={defaultChecked} className="h-4 w-4 rounded border-white/20 bg-slate-950/60" />{label}</label>; }
</file>

<file path="src/app/dashboard/page.tsx">
import Link from "next/link";
import { redirect } from "next/navigation";
import { signout } from "@/app/login/actions";
import { DashboardRealtimeFixed } from "@/components/dashboard/dashboard-realtime-fixed";
import { SignoutButton } from "@/components/auth/signout-button";
import { requireAuthenticatedUser } from "@/lib/auth";
import type { MarketCandle } from "@/lib/backend/types";

type DashboardPageProps = {
  searchParams: Promise<{
    account?: string;
    from?: string;
    to?: string;
    type?: string;
    result?: string;
  }>;
};

export type DashboardLicense = {
  id: string;
  user_id: string;
  conta_trading_id: string;
  nome_plano: string;
  status: "ativa" | "expirada" | "bloqueada" | "cancelada" | "pendente";
  valor: number;
  data_expiracao: string;
};

export type DashboardAccount = {
  id: string;
  user_id: string;
  nome_cliente: string;
  numero_conta: string;
  corretora: string | null;
  moeda_codigo: string;
  moeda_simbolo: string;
  saldo_atual: number;
  equity: number;
  margem_livre: number | null;
  nivel_margem: number | null;
  ativo: boolean;
  atualizado_em: string;
  server_time: string | null;
  mercado_snapshot: {
    notes?: string[];
    candles?: MarketCandle[];
  } | null;
  insight_atual: string | null;
  ultima_sincronizacao: string | null;
};

export type DashboardConfig = {
  id?: string;
  conta_trading_id: string;
  ativo: string;
  timeframe: string;
  sistema_ligado: boolean;
  modo: "agressivo" | "conservador";
  breakeven_ativo: boolean;
  trailing_stop_ativo: boolean;
  horario_inicio: string;
  horario_fim: string;
  meta_lucro_diaria: number;
  perda_maxima_diaria: number;
  limite_operacoes_ativo: boolean;
  limite_operacoes_diaria: number | null;
};

export type DashboardStats = {
  conta_trading_id: string;
  operacoes_total: number;
  vitorias: number;
  derrotas: number;
  win_rate: number;
  lucro_total: number;
  prejuizo_total: number;
  drawdown: number;
  melhor_operacao: number;
  pior_operacao: number;
};

export type DashboardHistoryRow = {
  id: string;
  time: string;
  type: string;
  lot: string;
  entry: string;
  result: string;
  resultTone: string;
};

export type DashboardHistoryFilters = {
  from: string;
  to: string;
  type: string;
  result: string;
};

export type DashboardOpenOperation = {
  id: string;
  ticket?: string | null;
  direction: "buy" | "sell";
  lot: number;
  entryPrice: number;
  stopLoss: number | null;
  takeProfit: number | null;
  openedAt: string;
  timeframe: string;
  symbol: string;
  profitLoss: number;
};

export type DashboardInsightBundle = {
  summary: string | null;
  notes: string[];
  candles: MarketCandle[];
};

export type DashboardCommandStatus = {
  id: string;
  tipo: "open_buy" | "open_sell" | "close_position" | "partial_close_position";
  status: "pending" | "processing" | "executed" | "failed" | "cancelled";
  erro: string | null;
  solicitado_em: string;
  processado_em: string | null;
};

function normalizeLicenseStatus(license: DashboardLicense) {
  const today = new Date().toISOString().slice(0, 10);
  if (license.status === "ativa" && license.data_expiracao < today) {
    return "expirada" as const;
  }
  return license.status;
}

export default async function DashboardPage({ searchParams }: DashboardPageProps) {
  const { supabase, profile } = await requireAuthenticatedUser();
  const params = await searchParams;

  if (!profile.acesso_ativo) {
    return <BlockedState title="Acesso do usuario bloqueado" description="Seu acesso ao SaaS foi desativado pelo administrador. Entre em contato para regularizacao." canManage={profile.role === "admin"} />;
  }

  const [{ data: accounts }, { data: licenses }] = await Promise.all([
    supabase
      .from("contas_trading")
      .select("id, user_id, nome_cliente, numero_conta, corretora, moeda_codigo, moeda_simbolo, saldo_atual, equity, margem_livre, nivel_margem, ativo, atualizado_em, server_time, mercado_snapshot, insight_atual, ultima_sincronizacao")
      .eq("user_id", profile.id)
      .order("criado_em", { ascending: false }),
    supabase
      .from("licencas")
      .select("id, user_id, conta_trading_id, nome_plano, status, valor, data_expiracao")
      .eq("user_id", profile.id)
      .order("data_expiracao", { ascending: true }),
  ]);

  const validAccounts = ((accounts ?? []) as DashboardAccount[])
    .map((account) => {
      const license = ((licenses ?? []) as DashboardLicense[]).find((item) => item.conta_trading_id === account.id);
      if (!license) {
        return null;
      }
      const normalizedStatus = normalizeLicenseStatus(license);
      if (normalizedStatus !== "ativa") {
        return null;
      }
      return {
        ...account,
        license: {
          ...license,
          status: normalizedStatus,
        },
      };
    })
    .filter(Boolean) as Array<DashboardAccount & { license: DashboardLicense }>;

  if (validAccounts.length === 0) {
    if (profile.role === "admin") {
      return <BlockedState title="Nenhuma conta licenciada para este admin" description="Voce ja pode operar o SaaS, mas precisa que a sua propria conta tambem tenha uma licenca ativa se quiser testar o dashboard operacional com o usuario admin." canManage />;
    }

    return <BlockedState title="Nenhuma licenca ativa disponivel" description="Este usuario nao possui conta MT5 licenciada no momento. O dashboard fica bloqueado ate que exista ao menos uma licenca valida." canManage={false} />;
  }

  const selectedAccount = validAccounts.find((item) => item.id === params.account) ?? validAccounts[0];
  if (!selectedAccount) {
    redirect("/dashboard");
  }

  const selectedLicense = selectedAccount.license;
  const historyFilters: DashboardHistoryFilters = {
    from: params.from ?? "",
    to: params.to ?? "",
    type: params.type ?? "all",
    result: params.result ?? "all",
  };

  const configQuery = supabase
    .from("configuracoes_sessao")
    .select("id, conta_trading_id, ativo, timeframe, sistema_ligado, modo, breakeven_ativo, trailing_stop_ativo, horario_inicio, horario_fim, meta_lucro_diaria, perda_maxima_diaria, limite_operacoes_ativo, limite_operacoes_diaria")
    .eq("user_id", profile.id)
    .eq("conta_trading_id", selectedAccount.id)
    .order("atualizado_em", { ascending: false })
    .limit(1)
    .maybeSingle<DashboardConfig>();

  const statsQuery = supabase
    .from("estatisticas")
    .select("conta_trading_id, operacoes_total, vitorias, derrotas, win_rate, lucro_total, prejuizo_total, drawdown, melhor_operacao, pior_operacao")
    .eq("user_id", profile.id)
    .eq("conta_trading_id", selectedAccount.id)
    .order("periodo", { ascending: false })
    .limit(1)
    .maybeSingle<DashboardStats>();

  const openOperationQuery = supabase
    .from("operacoes")
    .select("id, direcao, lote, preco_entrada, stop_loss, take_profit, lucro_prejuizo, aberta_em, timeframe, ativo, validacao_ia")
    .eq("user_id", profile.id)
    .eq("conta_trading_id", selectedAccount.id)
    .eq("status", "aberta")
    .order("aberta_em", { ascending: false })
    .limit(1)
    .maybeSingle();

  const commandStatusQuery = supabase
    .from("comandos_trading")
    .select("id, tipo, status, erro, solicitado_em, processado_em")
    .eq("user_id", profile.id)
    .eq("conta_trading_id", selectedAccount.id)
    .order("solicitado_em", { ascending: false })
    .limit(5);

  let operationsQuery = supabase
    .from("operacoes")
    .select("id, direcao, status, lote, preco_entrada, preco_saida, stop_loss, take_profit, lucro_prejuizo, aberta_em, fechada_em, timeframe, ativo, validacao_ia")
    .eq("user_id", profile.id)
    .eq("conta_trading_id", selectedAccount.id)
    .order("aberta_em", { ascending: false })
    .limit(50);

  if (historyFilters.from) {
    operationsQuery = operationsQuery.gte("aberta_em", `${historyFilters.from}T00:00:00.000Z`);
  }
  if (historyFilters.to) {
    operationsQuery = operationsQuery.lte("aberta_em", `${historyFilters.to}T23:59:59.999Z`);
  }
  if (historyFilters.type === "compra") {
    operationsQuery = operationsQuery.eq("direcao", "compra");
  }
  if (historyFilters.type === "venda") {
    operationsQuery = operationsQuery.eq("direcao", "venda");
  }
  if (historyFilters.result === "gain") {
    operationsQuery = operationsQuery.gt("lucro_prejuizo", 0);
  }
  if (historyFilters.result === "loss") {
    operationsQuery = operationsQuery.lt("lucro_prejuizo", 0);
  }

  const [{ data: config }, { data: stats }, { data: operations }, { data: openOperation }, { data: commandStatuses }] = await Promise.all([configQuery, statsQuery, operationsQuery, openOperationQuery, commandStatusQuery]);

  const resolvedConfig: DashboardConfig = config ?? {
    conta_trading_id: selectedAccount.id,
    ativo: profile.ativo_padrao,
    timeframe: profile.timeframe_padrao,
    sistema_ligado: false,
    modo: "agressivo",
    breakeven_ativo: true,
    trailing_stop_ativo: true,
    horario_inicio: "09:00:00",
    horario_fim: "17:00:00",
    meta_lucro_diaria: 1000,
    perda_maxima_diaria: 500,
    limite_operacoes_ativo: false,
    limite_operacoes_diaria: null,
  };

  const resolvedStats: DashboardStats = stats ?? {
    conta_trading_id: selectedAccount.id,
    operacoes_total: 0,
    vitorias: 0,
    derrotas: 0,
    win_rate: 0,
    lucro_total: 0,
    prejuizo_total: 0,
    drawdown: 0,
    melhor_operacao: 0,
    pior_operacao: 0,
  };

  const latestOperationWithAi = (operations ?? []).find((operation) => operation.validacao_ia);
  const accountNotes = Array.isArray(selectedAccount.mercado_snapshot?.notes) ? selectedAccount.mercado_snapshot.notes : [];
  const accountCandles = Array.isArray(selectedAccount.mercado_snapshot?.candles) ? selectedAccount.mercado_snapshot.candles : [];
  const insightBundle: DashboardInsightBundle = {
    summary: selectedAccount.insight_atual ?? (typeof latestOperationWithAi?.validacao_ia?.ai?.summary === "string" ? latestOperationWithAi.validacao_ia.ai.summary : null),
    notes: accountNotes.length > 0 ? accountNotes : (Array.isArray(latestOperationWithAi?.validacao_ia?.market?.notes) ? latestOperationWithAi.validacao_ia.market.notes : []),
    candles: accountCandles.length > 0 ? accountCandles : (Array.isArray(latestOperationWithAi?.validacao_ia?.market?.candles) ? latestOperationWithAi.validacao_ia.market.candles : []),
  };

  const resolvedOpenOperation: DashboardOpenOperation | null = openOperation ? {
    id: openOperation.id,
    ticket: typeof openOperation.validacao_ia?.ticket === "string" ? openOperation.validacao_ia.ticket : null,
    direction: openOperation.direcao === "compra" ? "buy" : "sell",
    lot: Number(openOperation.lote),
    entryPrice: Number(openOperation.preco_entrada),
    stopLoss: openOperation.stop_loss != null ? Number(openOperation.stop_loss) : null,
    takeProfit: openOperation.take_profit != null ? Number(openOperation.take_profit) : null,
    openedAt: openOperation.aberta_em,
    timeframe: openOperation.timeframe,
    symbol: openOperation.ativo,
    profitLoss: Number(openOperation.lucro_prejuizo ?? 0),
  } : null;

  const resolvedHistory: DashboardHistoryRow[] = operations?.length
    ? operations.filter((operation) => operation.status !== "aberta").map((operation) => {
        const resultValue = Number(operation.lucro_prejuizo ?? 0);
        return {
          id: operation.id,
          time: new Intl.DateTimeFormat("pt-BR", {
            hour: "2-digit",
            minute: "2-digit",
            hour12: false,
          }).format(new Date(operation.fechada_em ?? operation.aberta_em)),
          type: operation.direcao === "compra" ? "Compra" : "Venda",
          lot: Number(operation.lote).toFixed(2),
          entry: Number(operation.preco_entrada).toFixed(2),
          result: `${resultValue >= 0 ? "+" : "-"}${Math.abs(resultValue).toFixed(2)}`,
          resultTone: resultValue >= 0 ? "text-lime-400" : "text-red-400",
        };
      })
    : [];

  return (
    <main className="min-h-screen overflow-x-hidden px-4 py-6 text-white sm:px-6 lg:px-8">
      <div className="mx-auto flex w-full max-w-none flex-col gap-6 2xl:px-4">
        <section className="glass-panel rounded-[32px] p-4 sm:p-6">
          <div className="flex flex-col gap-4 border-b border-white/8 pb-5 lg:flex-row lg:items-center lg:justify-between">
            <div className="flex flex-wrap items-center gap-3">
              <div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-lime-400/12 text-2xl text-lime-300 glow-ring">L</div>
              <div>
                <p className="font-mono text-xs uppercase tracking-[0.32em] text-cyan-200/70">Plataforma SaaS de Trading</p>
                <h1 className="font-mono text-2xl font-bold tracking-tight sm:text-3xl">Lumitrader</h1>
              </div>
            </div>

            <div className="flex flex-wrap items-center gap-3">
              <div className="rounded-[24px] border border-white/8 bg-white/4 px-4 py-3">
                <p className="text-xs uppercase tracking-[0.22em] text-slate-400">Usuario</p>
                <p className="mt-2 text-lg font-semibold">{profile.nome ?? profile.email ?? "Trader"}</p>
              </div>
              {profile.role === "admin" ? (
                <Link href="/admin" className="rounded-[22px] border border-cyan-400/20 bg-cyan-400/10 px-4 py-3 text-sm font-semibold text-cyan-100">
                  Gestao SaaS
                </Link>
              ) : null}
              <SignoutButton action={signout} className="rounded-[22px] border border-white/10 bg-white/5 px-4 py-3 text-sm font-semibold text-slate-200 transition-colors hover:bg-white/10" />
            </div>
          </div>

          <DashboardRealtimeFixed
            profile={profile}
            accounts={validAccounts}
            selectedAccountId={selectedAccount.id}
            selectedLicense={selectedLicense}
            account={selectedAccount}
            config={resolvedConfig}
            stats={resolvedStats}
            history={resolvedHistory}
            historyFilters={historyFilters}
            insightBundle={insightBundle}
            openOperation={resolvedOpenOperation}
            commandStatuses={(commandStatuses ?? []) as DashboardCommandStatus[]}
          />
        </section>
      </div>
    </main>
  );
}

function BlockedState({ title, description, canManage }: { title: string; description: string; canManage: boolean }) {
  return (
    <main className="min-h-screen px-4 py-6 text-white sm:px-6 lg:px-8">
      <div className="mx-auto flex min-h-[calc(100vh-3rem)] max-w-4xl items-center">
        <section className="glass-panel w-full rounded-[32px] p-8">
          <p className="font-mono text-xs uppercase tracking-[0.3em] text-red-200/80">Acesso bloqueado</p>
          <h1 className="mt-4 text-4xl font-semibold">{title}</h1>
          <p className="mt-4 max-w-2xl text-slate-300">{description}</p>
          <div className="mt-8 flex flex-wrap gap-3">
            <SignoutButton action={signout} label="Voltar ao login" className="rounded-[20px] bg-linear-to-r from-lime-500 via-lime-400 to-emerald-400 px-5 py-3 font-semibold text-slate-950" />
            {canManage ? (
              <Link href="/admin" className="rounded-[20px] border border-cyan-400/30 bg-cyan-400/10 px-5 py-3 font-semibold text-cyan-100">Abrir gestao SaaS</Link>
            ) : null}
          </div>
        </section>
      </div>
    </main>
  );
}
</file>

</files>
